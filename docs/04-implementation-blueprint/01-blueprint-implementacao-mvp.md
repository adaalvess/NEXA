# NEXA — Blueprint de Implementação do MVP

| | |
|---|---|
| **Documento** | Blueprint de Implementação do MVP (compactação das Fases 4-6) |
| **Fase** | 4-6 compactadas — Planeamento, Arquitetura Técnica Detalhada, UI/UX |
| **Versão** | 3.23 |
| **Estado** | ✅ Aprovado — vivo; M1, M2, M3, M4, M5 e M6 formalmente concluídos; M7 (Deploy em Staging) aprovado e em curso — Passos 39, 40 e 41 formalmente concluídos (repositório remoto GitHub ativo; base de dados Neon em staging operacional, região UE; backend NestJS em produção de staging no Render, região UE) |
| **Owner** | CTO / Arquiteto Principal / Fundadora / CEO |
| **Documentos de referência** | Todos os documentos aprovados (Fases 1, 2, 3); Especificações Técnicas dos Passos 3 a 33, 39, 40 e 41; Validação Manual UC-01/UC-02 (Passo 34), UC-03/UC-04 (Passo 35), UC-05/UC-06 (Passo 36), UC-07/UC-08 (Passo 37); Relatório Final de Encerramento do M6 (Passo 38); ADR-007 (v1.2) |
| **Última atualização** | 2026-07-14 |

---

## 1. Objetivo

Este documento é a **ponte direta entre a documentação e o código** — não introduz decisões novas, traduz o que já está aprovado (Data Model Conceptual, Functional Specifications, Information Architecture, Brand Book, 8 ADRs) num formato pronto a ser lido e executado por quem (ou o quê) for construir a plataforma. Substitui, de forma compactada e proporcional ao objetivo de velocidade com solidez, o que seriam as Fases 4 (Planeamento), 5 (Arquitetura Técnica Detalhada) e 6 (UI/UX) tratadas isoladamente.

### Nota de Clarificação de Âmbito

Este documento não é código — é a especificação que o código vai seguir. O schema Prisma e a lista de endpoints aqui presentes são a **primeira versão real**, esperada evoluir durante a construção (Coding Standards, documento vivo) — não um contrato imutável.

---

## 2. Épicos e Milestones

### 2.1 Épicos

| ID | Épico | Deriva de | Módulo |
|---|---|---|---|
| EP-01 | Fundação — Autenticação, RBAC, Multi-Tenant, Auditoria, Partilha | FR-01 a FR-10, FR-35; ADR-001, 003, 004 | Fundação |
| EP-02 | Dashboard Inteligente | FR-11 a FR-13, FR-36 | Dashboard |
| EP-03 | Gestão de Processos e Tarefas | FR-14 a FR-18 | Processos |
| EP-04 | CRM Inteligente | FR-19 a FR-22 | CRM |
| EP-05 | Assistente de IA | FR-23 a FR-28; ADR-005 | Assistente de IA |
| EP-06 | Comercial — Planos, Subscrições, Pagamentos | FR-29 a FR-31; ADR-008 | Comercial |
| EP-07 | Camada Comercial e Produto — Landing, Pricing, Área Administrativa, Centro de Ajuda | PRD v1.1 | Produto |

### 2.2 Milestones (Ordem de Construção) e Critérios de Conclusão (Definition of Done)

Consistente com o Product Roadmap (Etapas 1.1-1.4) e orientado por resultados, não por calendário (mesmo princípio já fixado):

| Milestone | Conteúdo | Depende de | Definition of Done |
|---|---|---|---|
| M1 | EP-01 — schema de base de dados, autenticação, RBAC, middleware de tenant, auditoria | — (fundação de tudo) | Registo/login funcionais; isolamento multi-tenant verificado por teste (NFR-17); todos os 5 papéis RBAC atribuíveis e a restringir acesso corretamente; Registo de Auditoria a gravar em toda ação de escrita |
| M2 | EP-02, EP-03, EP-04 — módulos core, construídos sobre M1 | M1 | Os 3 módulos operacionais com CRUD completo conforme Functional Specifications; visibilidade RBAC verificada em cada módulo; estado inicial guiado presente em todos os ecrãs sem dados |
| M3 | EP-05 — Assistente de IA, construído sobre M1+M2 | M1, M2 | Pergunta livre e sugestão de ação funcionais; nenhuma ação executada sem confirmação explícita (RN-08 verificado por teste); AI Gateway a aplicar corretamente o escopo RBAC do Utilizador |
| M4 | EP-06 — Comercial e Pagamentos | M1 | Checkout Stripe funcional em ambiente de teste; webhooks verificados e idempotentes; limites de plano aplicados conforme RN-10/RN-11 |
| M5 | EP-07 — Camada Comercial e Produto | M1 a M4 | Um utilizador consegue percorrer sozinho Landing → Trial → Pagamento → Uso, sem intervenção manual da equipa |
| M6 | Testes dos 4 fluxos críticos (NFR-17) + validação manual dos Use Cases principais | M1 a M5 | ✅ **Cumprido** (Passo 38, 2026-07-11) — cobertura de teste automatizado presente e a passar nos 4 fluxos críticos; os 8 Use Cases reais do Use Cases v1.0 validados manualmente pelo menos uma vez (o documento afirma "9 Use Cases" mas só define 8, UC-01 a UC-08 — inconsistência já registada como achado B da Proposta do M6, corrigida aqui por referir-se ao número correto, sem alterar o Use Cases v1.0) |
| M7 | Deploy em staging (ADR-007) → validação técnica → produção | M6 | 🔄 **Em curso** (Passo 39, 2026-07-11) — âmbito confirmado exclusivamente a staging (produção fica fora, Proposta do M7). Ambiente de staging espelha produção; teste de recuperação de backup executado com sucesso (ADR-007, 3.8); disponibilidade monitorizada (uptime) ativa antes do lançamento com empresas piloto |

---

## 3. Esquema de Base de Dados (Prisma) — Versão Inicial

Tradução direta do Data Model Conceptual + ADR-001 (tenant_id) + ADR-003 (PostgreSQL/Prisma). Campos de auditoria padrão (`createdAt`, `updatedAt`) omitidos por brevidade, mas obrigatórios em todas as entidades (Data & Consistency Rules, 3.7).

> **Regra de rastreabilidade obrigatória:** qualquer alteração estrutural relevante a este schema (nova entidade, nova relação, alteração ao modelo de isolamento) tem de manter rastreabilidade explícita com o Data Model Conceptual e com os ADRs já aprovados — nunca introduzida apenas no código sem correspondência documental. Alterações de detalhe (novo campo opcional, ajuste de tipo) não exigem atualização formal de documentação; alterações estruturais exigem-na, seguindo a mesma disciplina já aplicada a toda a Fase 3.

```prisma
// Fundação
model Empresa {
  id              String   @id @default(cuid())
  nome            String
  pais            String
  setor           String?
  estadoSubscricao String  @default("trial") // trial | ativa | limitada | cancelada
  utilizadores    Utilizador[]
  departamentos   Departamento[]
  // ... relações com todas as entidades de negócio via tenantId
}

model Utilizador {
  id          String   @id @default(cuid())
  empresaId   String
  empresa     Empresa  @relation(fields: [empresaId], references: [id])
  nome        String
  email       String
  passwordHash String  // Argon2id (ADR-004)
  papel       String   // super_admin | admin_empresa | gestor | colaborador | convidado
  departamentoId String?

  @@unique([empresaId, email])
  @@index([empresaId])
}

model Sessao {
  id           String   @id @default(cuid())
  utilizadorId String
  expiraEm     DateTime // renovação deslizante, 7 dias (ADR-007, 3.5)

  @@index([utilizadorId])
}

model Departamento {
  id        String  @id @default(cuid())
  empresaId String
  empresa   Empresa @relation(fields: [empresaId], references: [id])
  nome      String

  @@index([empresaId])
}

model RegistoAuditoria {
  id         String   @id @default(cuid())
  empresaId  String   // particionado por intervalo de tempo (ADR-007, 3.7)
  ator       String   // utilizadorId ou "ia"
  acao       String
  entidade   String
  entidadeId String
  timestamp  DateTime @default(now())
  // append-only — nunca UPDATE nem DELETE (Data & Consistency Rules, 3.3)

  @@index([empresaId, timestamp])
}

model Partilha {
  id             String @id @default(cuid())
  empresaId      String
  entidadeTipo   String // "cliente" | "processo"
  entidadeId     String
  convidadoId    String // Utilizador com papel "convidado"
  concedidoPorId String

  @@index([empresaId, entidadeTipo, entidadeId])
}

// Processos e Tarefas
model Processo {
  id             String   @id @default(cuid())
  empresaId      String
  titulo         String
  responsavelId  String
  departamentoId String?
  clienteId      String?
  estado         String   @default("por_fazer") // por_fazer | em_curso | concluida
  prazo          DateTime?

  @@index([empresaId])
}

// CRM
model Cliente {
  id            String  @id @default(cuid())
  empresaId     String
  nome          String
  tipo          String  // empresa_cliente | contacto_individual
  ownerId       String
  estadoOportunidade String?

  @@index([empresaId])
}

model Interacao {
  id        String   @id @default(cuid())
  empresaId String
  clienteId String
  tipo      String   // chamada | reuniao | nota | outro
  data      DateTime @default(now())

  @@index([empresaId, clienteId])
}

// Assistente de IA
model SugestaoIA {
  id             String   @id @default(cuid())
  empresaId      String
  utilizadorId   String
  tipo           String   // pergunta | sugestao_acao
  entidadeRef    String?
  estado         String   @default("pendente") // pendente | aceite | rejeitada
  fornecedorUsado String

  @@index([empresaId])
}

// Dashboard / Transversal
model Notificacao {
  id            String   @id @default(cuid())
  empresaId     String
  destinatarioId String
  tipoEvento    String
  entidadeOrigemId String
  lida          Boolean  @default(false)

  @@index([empresaId, destinatarioId])
}

// Comercial
model SubscricaoPlano {
  id                String   @id @default(cuid())
  empresaId         String   @unique
  plano             String   // starter | professional | enterprise
  estado            String   @default("trial")
  stripeCustomerId  String?
  stripeSubscriptionId String?
  limiteUtilizadores Int
  limiteArmazenamentoMb Int
  limiteUsoIA       Int
  trialIniciadoEm   DateTime @default(now())
}
```

---

## 3a. Estado de Implementação do Schema — Passo 2 (M1) — ✅ Concluído

*Adicionado após a execução real do Passo 2 (M1), preenchendo a rastreabilidade entre este schema inicial (secção 3) e o código efetivamente implementado — sem esta secção, a nota de âmbito do documento ("primeira versão real, não contrato final") ficaria sem registo concreto de como evoluiu.*

**Ficheiro real:** `apps/api/prisma/schema.prisma`. **Migração aplicada:** `20260706095205_init_fundacao_processos_crm_ia_comercial`, validada localmente contra PostgreSQL 17 (ambiente local — transição para Neon, ADR-007, adiada para depois do M1 estar validado, decisão explícita da Fundadora/CEO).

**Refinamentos face ao texto literal desta secção 3** (todos classificados como alteração de detalhe, não estrutural — D4 abaixo):

| Refinamento | Justificação documental |
|---|---|
| `criadoPor` / `atualizadoPor` em toda entidade de negócio, além de `createdAt`/`updatedAt` | Data & Consistency Rules, 3.7 — já exigia "quando foi criada, por quem, quando foi alterada, por quem"; o SQL desta secção só citava 3.7 mas omitira os campos de autoria "por brevidade" |
| `eliminadoEm` (soft-delete) em `Utilizador`, `Departamento`, `Processo`, `Cliente` | Data & Consistency Rules, 3.4 — soft-delete já é o comportamento por defeito aprovado (a tensão com RGPD/hard-delete continua em PSD-001, sem impacto neste campo) |
| `Sessao` passou a incluir `empresaId` (ausente no literal desta secção) | ADR-004, Validação Arquitetural Final, ponto 5 — exige explicitamente que a sessão herde o isolamento por tenant |
| Relações entre entidades de negócio (`Processo.responsavelId`, `Processo.clienteId`, `Cliente.ownerId`, `Partilha.convidadoId`, etc.) implementadas como **chaves estrangeiras compostas** `(id, empresaId)`, não FKs simples | Implementação direta de ADR-003, 3.3 (Camada 3 de Defense in Depth) — torna estruturalmente impossível, ao nível da própria base de dados, uma entidade de uma Empresa referenciar uma entidade de outra. Verificado por teste manual: uma tentativa de referência cruzada foi rejeitada pela constraint |

**Dependência explícita registada para o Passo 4:** as políticas de Row-Level Security (Camada 2, ADR-001 §3.3) **não foram ativadas** nesta migração. RLS só é uma segunda camada real — não meramente simbólica — quando a aplicação se liga com um utilizador de base de dados dedicado, não-owner (o Postgres não aplica RLS ao dono da tabela por defeito), e quando o middleware de tenant (Camada 1, Passo 4) injeta a variável de sessão `tenant_id` em cada transação. Ativar políticas RLS antes de essas duas condições existirem daria falsa sensação de segurança sem proteção real. Decisão confirmada explicitamente com a Fundadora/CEO durante o Passo 2.

**Entidades do Data Model Conceptual deliberadamente não incluídas ainda:** `Favorito` e `Item Recente` (3.3 desse documento) — descritas ali como "capacidade futura, não implementada no MVP", coerente com o schema desta secção 3, que também não as incluía.

---

## 4. Superfície de API — Endpoints por Módulo

Todos os endpoints exigem sessão autenticada (ADR-004) e passam pelo serviço único de autorização (Security & Access Principles, 3.1), salvo indicação contrária.

> **Nota de âmbito:** esta lista representa o **conjunto mínimo de endpoints do MVP** — o necessário e suficiente para cumprir os Functional Requirements já aprovados. Novos endpoints podem ser adicionados durante o desenvolvimento, desde que respeitem a arquitetura já decidida (ponto único de autorização, isolamento multi-tenant), os princípios de segurança (Security & Access Principles), e derivem de um requisito funcional já aprovado ou formalmente adicionado — nunca introduzidos apenas por conveniência de implementação.

| Módulo | Método | Endpoint | Referência |
|---|---|---|---|
| Fundação | POST | `/auth/registar` | UC-01 — ✅ Implementado (Passo 3) |
| Fundação | POST | `/auth/login` | ADR-004 — ✅ Implementado (Passo 3) |
| Fundação | POST | `/auth/logout` | ADR-004 — ✅ Implementado (Passo 14, 3.14 — lacuna real do Passo 3) |
| Fundação | POST | `/convites` | UC-02, FR-02/03 — ✅ Implementado (Passo 30) — nova permissão `fundacao.convidar_utilizador`, substitui a rota planeada `/empresas/:id/utilizadores/convidar` (nunca implementada) |
| Fundação | GET | `/convites/:token` | Especificação Técnica do Passo 30, Decisão A — ✅ Implementado (Passo 30) — pré-visualização pública antes da aceitação, nunca expõe `token` |
| Fundação | POST | `/convites/:token/aceitar` | UC-02 — ✅ Implementado (Passo 30) — público, cria o Utilizador, convidado define a própria palavra-passe (Proposta do M5, Decisão D) |
| Fundação | GET | `/utilizadores` | Passo 14, 3.2.1 — ✅ Implementado (listagem reduzida para seletores de responsável/owner) |
| Fundação | PATCH | `/utilizadores/me` | Proposta do M5, Decisão C — ✅ Implementado (Passo 27) — self-edit de nome/palavra-passe, primeira mutação sem `PermissaoGuard` (alvo sempre `ctx.utilizadorId`) |
| Fundação | PATCH | `/utilizadores/:id/papel` | FR-03/04 — ✅ Implementado (Passo 5) |
| Fundação | GET | `/auditoria` | FR-07 — ✅ Implementado (Passo 6) |
| Fundação | POST | `/partilhas` | FR-35 — ✅ Implementado (Passo 7) |
| Fundação | DELETE | `/partilhas/:id` | FR-35 — ✅ Implementado (Passo 7) |
| Fundação | GET | `/partilhas` | FR-35 — ✅ Implementado (Passo 7) |
| Fundação | POST | `/departamentos` | FR-05 — ✅ Implementado (Passo 8) |
| Fundação | GET | `/departamentos` | FR-05 — ✅ Implementado (Passo 8) |
| Fundação | PATCH | `/departamentos/:id` | FR-05 — ✅ Implementado (Passo 8) |
| Fundação | DELETE | `/departamentos/:id` | FR-05 — ✅ Implementado (Passo 8) |
| Fundação | PATCH | `/utilizadores/:id/departamento` | FR-05 — ✅ Implementado (Passo 8) |
| Dashboard | GET | `/dashboard` | FR-11/12 — ✅ Implementado (Passo 12) |
| Dashboard | GET | `/notificacoes` | FR-36 — ✅ Implementado (Passo 12) |
| Dashboard | PATCH | `/notificacoes/:id/lida` | FR-36 — ✅ Implementado (Passo 12) |
| Processos | POST | `/processos` | FR-14 a FR-16 — ✅ Implementado (Passo 9) |
| Processos | GET | `/processos` | FR-17 — ✅ Implementado (Passo 9) |
| Processos | GET | `/processos/:id` | FR-17/18 — ✅ Implementado (Passo 9) |
| Processos | PATCH | `/processos/:id` | FR-14 — ✅ Implementado (Passo 9) |
| Processos | DELETE | `/processos/:id` | FR-14 — ✅ Implementado (Passo 9) |
| CRM | POST | `/clientes` | FR-19 — ✅ Implementado (Passo 10) |
| CRM | GET | `/clientes` | FR-21 — ✅ Implementado (Passo 10) |
| CRM | GET | `/clientes/:id` | FR-21 — ✅ Implementado (Passo 10) |
| CRM | PATCH | `/clientes/:id` | FR-19 — ✅ Implementado (Passo 10) |
| CRM | POST | `/clientes/:id/interacoes` | FR-20 — ✅ Implementado (Passo 10) |
| CRM | GET | `/clientes/:id/interacoes` | FR-20 — ✅ Implementado (Passo 10) |
| CRM | GET | `/pipeline` | FR-22 — ✅ Implementado (Passo 10) |
| Assistente de IA | POST | `/ia/perguntar` | FR-23, UC-05 — ✅ Implementado (Passo 16) |
| Assistente de IA | POST | `/ia/sugestoes` | FR-24/25, UC-06 — ✅ Implementado (Passo 17) — geração determinística; extensão aditiva à superfície mínima (Blueprint §4, nota de âmbito), necessária para UC-06 ter algo a confirmar/rejeitar |
| Assistente de IA | GET | `/ia/sugestoes` | FR-24/25, UC-06 — ✅ Implementado (Passo 18) — extensão aditiva, herdada da Decisão F do Passo 17, necessária para a área "Sugestões Pendentes" do ecrã |
| Assistente de IA | POST | `/ia/sugestoes/:id/confirmar` | FR-25, UC-06 — ✅ Implementado (Passo 17) |
| Assistente de IA | POST | `/ia/sugestoes/:id/rejeitar` | FR-25, UC-06 — ✅ Implementado (Passo 17) |
| Comercial | GET | `/planos` | FR-29 — ✅ Implementado (Passo 19) |
| Comercial | GET | `/planos/publico` *(sem sessão)* | FR-29, PRD "Camada Comercial e Produto" — ✅ Implementado (Passo 24) — extensão aditiva, reaproveita `listarPlanos()` literalmente, consumida pela página pública `/precos` |
| Comercial | GET | `/subscricao` | UC-07/UC-08, US-19 — ✅ Implementado (Passo 23) — extensão aditiva, resumo de subscrição para o ecrã de frontend, reaproveita `comercial.ver_planos` |
| Comercial | POST | `/subscricao/checkout` | ADR-008, UC-07 — ✅ Implementado (Passo 21) |
| Comercial | POST | `/webhooks/stripe` *(sem sessão — verificado por assinatura)* | ADR-008, 3.4 — ✅ Implementado (Passo 22) |

**Especificação técnica detalhada do Passo 3 (Autenticação):** ver [Especificação Técnica do Passo 3](02-especificacao-tecnica-passo-3-autenticacao.md) — fluxo completo, arquitetura de segurança, e evidências de validação (T1-T10, S1-S4). O módulo real está implementado em `apps/api/src/modules/fundacao/`, incluindo um endpoint adicional de verificação técnica (`GET /auth/eu`, não listado acima por não pertencer à superfície mínima do produto — ver Questão 4 desse documento).

**Especificação técnica detalhada do Passo 4 (Camada 1):** ver [Especificação Técnica do Passo 4](03-especificacao-tecnica-passo-4-camada1-autorizacao.md) — middleware de tenant, `TenantPrismaService`, ativação de RLS, três roles de BD.

**Especificação técnica detalhada do Passo 5 (RBAC granular):** ver [Especificação Técnica do Passo 5](04-especificacao-tecnica-passo-5-rbac.md) — modelo `RegraPermissao`, serviço único de autorização, definição inequívoca da autoridade para alterar papéis (6 limites explícitos), renovação deslizante de sessão.

**Especificação técnica detalhada do Passo 6 (Registo de Auditoria):** ver [Especificação Técnica do Passo 6](05-especificacao-tecnica-passo-6-auditoria.md) — mecanismo orientado a eventos (`emitAsync`), campo `detalhe` (jsonb), trigger de imutabilidade, role `nexa_auditoria_interna` para a consulta cross-tenant do Super Admin. Definition of Done do M1 tecnicamente completo a partir deste passo.

**Especificação técnica detalhada do Passo 7 (Partilha):** ver [Especificação Técnica do Passo 7](06-especificacao-tecnica-passo-7-partilha.md) — `AuthorizationService.podeAcederViaPartilha`, regras de autoridade P1-P5, campos `nivelAcesso`/`revogadoEm`, endpoints `/partilhas`. **Com este passo aprovado, o Milestone M1 (Fundação) está formalmente concluído** (Fundadora/CEO, 2026-07-06) — todos os passos previstos no Blueprint (0-7) implementados, validados e aprovados.

**Proposta e início do Milestone M2 (Módulos Core):** proposta completa (objetivos, âmbito, sequência de passos 8-14, dependências, riscos, DoD, decisões arquitetónicas A-C) apresentada e aprovada pela Fundadora/CEO em 2026-07-06 — numeração de passos continua a partir do M1.

**Especificação técnica detalhada do Passo 8 (Departamento):** ver [Especificação Técnica do Passo 8](07-especificacao-tecnica-passo-8-departamento.md) — CRUD completo de `Departamento`, atribuição de Utilizador a Departamento, regras RD-01 a RD-04. Endpoints planos (`/departamentos`, `/utilizadores/:id/departamento`), substituindo o literal `/empresas/:id/departamentos` (nunca implementado) — mesma decisão já tomada no Passo 5 para `/utilizadores/:id/papel`.

**Especificação técnica detalhada do Passo 9 (Processos e Tarefas):** ver [Especificação Técnica do Passo 9](08-especificacao-tecnica-passo-9-processos.md) — primeiro módulo de negócio fora da Fundação (`apps/api/src/modules/processos/`); `AuthorizationService` estendido com `obterRelacaoEntidade`/`podeAgirSobreEntidade`/`obterEscopoVisibilidade` (visibilidade RBAC centralizada, Decisão B do M2), com o `PartilhaService` (Passo 7) refatorado para os consumir em vez de manter cópia própria; `Processo.estado` promovido a `enum` (Decisão C do M2); primeiro consumidor real de `podeAcederViaPartilha`.

**Especificação técnica detalhada do Passo 10 (CRM):** ver [Especificação Técnica do Passo 10](09-especificacao-tecnica-passo-10-crm.md) — segundo módulo de negócio (`apps/api/src/modules/crm/`); **zero alterações ao `AuthorizationService`**, confirmando na prática que a Decisão B do M2 (centralização) funciona sem duplicação; `Cliente.estadoOportunidade` promovido a `enum`; sem eliminação de Cliente (decisão deliberada, entidade estrutural do negócio).

**Especificação técnica detalhada do Passo 11 (Notification Dispatcher):** ver [Especificação Técnica do Passo 11](10-especificacao-tecnica-passo-11-notification-dispatcher.md) — `NotificacaoListener` fire-and-forget sobre o mesmo `EVENTO_AUDITORIA` já existente (sem novo tipo de evento); 5 gatilhos mínimos (`atribuir_papel`, `atribuir_departamento`, `criar` Partilha, `criar`/`atualizar` Processo com reatribuição). **Sem nova superfície de API** — exposição de Notificações ao Utilizador fica para o Passo 12 (Dashboard).

**Especificação técnica detalhada do Passo 12 (Dashboard):** ver [Especificação Técnica do Passo 12](11-especificacao-tecnica-passo-12-dashboard.md) — terceiro módulo de negócio (`apps/api/src/modules/dashboard/`), **zero alterações ao `AuthorizationService`** (terceira confirmação prática da Decisão B do M2); indicadores agregados de Processos/Clientes/Notificações, estado inicial guiado (FR-12); `GET /notificacoes`/`PATCH .../lida` (exposição de Notificações herdada do Passo 11). **Backend do M2 concluído** — Passos 8 a 12 implementados, validados e aprovados.

**Especificação técnica detalhada do Passo 13 (Design System, frontend):** ver [Especificação Técnica do Passo 13](12-especificacao-tecnica-passo-13-design-system.md) — primeiro passo de `apps/web` desde o scaffolding do Passo 1: estrutura de pastas `components/ui|layout/`, `lib/`, `hooks/` (resolve ADR-006 Q1); 11 componentes base (Botão, Input, Select, Modal, MenuDropdown, TabelaDados, Cartão, NotificacaoToast, BadgeEstado, Avatar, EstadoVazioGuiado) sobre primitivas Radix UI, nomenclatura em português; tokens de marca estendidos (escala tipográfica completa, `glow-purple`); modo único dark (sem light mode no MVP); vitrine interna em `/design-system` em vez de Storybook; `EstadoVazioGuiado` personalizado por módulo (resolve Information Architecture Q1). Validado por inspeção visual real no browser — cores, tipografia, teclado, responsividade — e `npm run build`/`npm run lint` sem erros. **Design System (§5.1) concluído**.

**Especificação técnica detalhada do Passo 14 (Ecrãs, frontend):** ver [Especificação Técnica do Passo 14](13-especificacao-tecnica-passo-14-ecras.md) — último passo do M2, implementado em 3 sub-entregas sequenciais aprovadas individualmente (Login+Dashboard, Processos, CRM). Ecrãs: `/login`, `/dashboard`, `/processos` (lista/criar/detalhe), `/crm` (lista/criar/detalhe/pipeline) — todos consumindo as APIs dos Passos 8-12 através dos componentes do Passo 13, sem duplicar lógica de RBAC (ADR-006 §3.7). Uma alteração mínima ao backend fora do âmbito original do M2: `GET /utilizadores` (listagem reduzida, nova permissão `fundacao.listar_utilizadores`) e `POST /auth/logout` (nunca implementado desde o Passo 3), ambos aditivos, sem alterar contratos existentes. Validado exaustivamente no browser em cada sub-entrega, incluindo RBAC por papel e por posse, CR-06, defesa em profundidade (`403` mesmo com UI a esconder ações). Endpoints `/utilizadores` e `/auth/logout` marcados implementados na Superfície de API (§4). **Com este passo aprovado, o Milestone M2 (Módulos Core) está formalmente concluído** (Fundadora/CEO, 2026-07-07) — Dashboard, Processos e CRM operacionais com CRUD completo, visibilidade RBAC verificada em cada módulo, estado inicial guiado presente em todos os ecrãs sem dados (Definition of Done, §2.2).

**Proposta e início do Milestone M3 (Assistente de IA):** proposta completa (objetivos, âmbito, arquitetura, sequência de passos 15-18, riscos, decisões técnicas, DoD, plano de validação) apresentada e aprovada pela Fundadora/CEO em 2026-07-07 — numeração de passos continua a partir do M2. Contexto orientador: Product Vision v1.2 §3.5a (princípio 40/60 organização/inteligência).

**Especificação técnica detalhada do Passo 15 (AI Gateway, backend):** ver [Especificação Técnica do Passo 15](14-especificacao-tecnica-passo-15-ai-gateway.md) — primeiro passo do M3, implementa integralmente o ADR-005 (interface própria, adaptador Anthropic único, timeout/circuit breaker/quota, neutralidade de tipos, distinção sugestão/execução ao nível de tipos). Novo módulo `apps/api/src/modules/ia/`, sem endpoint de produto neste passo (fundação para os Passos 16/17). **Descoberta real**: `SubscricaoPlano.limiteUsoIA` é um teto, nunca um contador — novo modelo aditivo `UsoIAMensal` resolve o contador de consumo sem reabrir a decisão de planos comerciais, deliberadamente adiada para o M4. Testes exclusivamente com `FakeAdapter`, zero dependência de rede/credenciais reais (condição explícita da aprovação do M3). 116/116 testes (109 herdados + 7 novos).

**Especificação técnica detalhada do Passo 16 (módulo `ia` — pergunta livre):** ver [Especificação Técnica do Passo 16](15-especificacao-tecnica-passo-16-ia-perguntar.md) — primeiro endpoint de produto do M3, `POST /ia/perguntar` (UC-05). `AuthorizationService.construirFiltroWhere` extraído do `DashboardService` (4ª confirmação da Decisão B do M2, comportamento neutro, zero alteração aos testes de Dashboard); `IaService.reunirResumoOperacional()` garante estruturalmente o cumprimento da RN-07 (dados fora do escopo RBAC nunca chegam ao Gateway); contexto da IA limitado a dados agregados (decisão consciente de âmbito do MVP, sem lookup de entidade nomeada); `IaExceptionFilter` traduz os erros tipados do Gateway (429/504/503/400/502); extensão aditiva do schema `SugestaoIA` (`conteudoPergunta`/`conteudoResposta`), retenção de conteúdo configurável (PSD-003) por ocultação-na-leitura. 122/122 testes (116 herdados + 6 novos).

**Especificação técnica detalhada do Passo 17 (sugestões de ação):** ver [Especificação Técnica do Passo 17](16-especificacao-tecnica-passo-17-ia-sugestoes.md) — `POST /ia/sugestoes` (geração), `POST /ia/sugestoes/:id/confirmar`, `POST /ia/sugestoes/:id/rejeitar` (UC-06, RN-08). Único tipo de ação suportado no MVP: reatribuição de um Processo em atraso a um Colaborador do mesmo Departamento com menor carga (deteção e texto 100% determinísticos, sem chamar o AI Gateway — preserva a quota escassa). `ProcessosModule` passa a exportar `ProcessosService`, consumido por `IaModule` para executar a reatribuição confirmada — primeira vez que um módulo de negócio importa o serviço de outro (coberto pela regra #1 do System Design Principles), evitando duplicar PR-01 a PR-07. RN-08 garantida em duas camadas independentes: tipos (`PendingSuggestion`/`ConfirmedAction`, Passo 15) e eventos (Event & Notification Architecture Rules §3.8 — "sugestão" e "execução" sempre distintas). Sugestão obsoleta na confirmação (UC-06 E1) → `409`, sem novo valor de `estado`. Extensão aditiva do schema `SugestaoIA` (`acaoPayload`). **NFR-17 ("ações de IA") fecha na íntegra** — os 4 fluxos críticos obrigatórios têm agora todos cobertura de teste automatizado. 132/132 testes (122 herdados + 10 novos).

**Especificação técnica detalhada do Passo 18 (ecrã do Assistente de IA, frontend):** ver [Especificação Técnica do Passo 18](17-especificacao-tecnica-passo-18-ecra-ia.md) — último passo do M3. `apps/web/src/app/(autenticado)/ia/page.tsx`: secção "Perguntar" (todos exceto `convidado`) e "Sugestões Pendentes" (só `admin_empresa`/`gestor`), consumindo `POST /ia/perguntar` (Passo 16) e `POST/GET /ia/sugestoes`/`.../confirmar`/`.../rejeitar` (Passo 17/18) através dos componentes do Design System (Passo 13), sem duplicar lógica de RBAC (ADR-006 §3.7). Extensão aditiva ao backend: `GET /ia/sugestoes` (herdada da Decisão F do Passo 17), nova permissão `ia.listar_sugestoes`. RN-08 refletida na interface — `Modal` de segunda confirmação antes de "Confirmar", nunca ação em lote. Cross-navegação de cada sugestão para o Processo referenciado (Information Architecture §3.5). Item "Assistente de IA" adicionado à `BarraLateralNavegacao`, oculto para `convidado`. Validado por inspeção visual real no browser em todos os 4 papéis, incluindo execução real de uma reatribuição confirmada, verificada na BD. **Com este passo aprovado, o Milestone M3 (Assistente de IA) está formalmente concluído** — NFR-17 coberto na íntegra. 135/135 testes (132 herdados + 3 novos).

**Proposta e início do Milestone M4 (Comercial e Pagamentos):** proposta completa (objetivos, âmbito, exclusões, arquitetura, sequência de passos 19-23, riscos, DoD) apresentada e aprovada pela Fundadora/CEO em 2026-07-07 — resolve formalmente o Risco R3 do Master Roadmap (valores numéricos de limites por plano). Numeração de passos continua a partir do M3.

**Especificação técnica detalhada do Passo 19 (`SubscricaoPlano` real, `GET /planos`):** ver [Especificação Técnica do Passo 19](18-especificacao-tecnica-passo-19-comercial-subscricao.md) — primeiro passo do M4, primeiro módulo `comercial` (já antecipado desde a regra não-negociável #1). `SubscricaoPlano` (scaffolded desde o Passo 2) ativado — trial de 14 dias automático no plano Professional, criado reativamente ao mesmo `EVENTO_AUDITORIA` já emitido no registo (`fundacao` nunca conhece `comercial`), via `tenantContext.run()` idempotente (`upsert`, exigência explícita da Fundadora/CEO). `plano`/`estado` promovidos a `enum`; `Empresa.estadoSubscricao` removido (nunca usado, redundante); limites `Int?` — `null` = sem limite (Enterprise), nunca um valor sentinela, exigindo correção ao `QuotaService` (Passo 15). `GET /planos` só para `admin_empresa`. 141/141 testes (135 herdados + 6 novos).

**Especificação técnica detalhada do Passo 20 (enforcement uniforme de RN-11):** ver [Especificação Técnica do Passo 20](19-especificacao-tecnica-passo-20-comercial-enforcement.md) — `SubscricaoGuard`/`SubscricaoExceptionFilter` registados globalmente (`APP_GUARD`/`APP_FILTER`, `ComercialModule`) garantem resposta uniforme (`402`, `code: SUBSCRICAO_LIMITADA`, mesma mensagem) em toda a aplicação, exigência explícita da Fundadora/CEO — decorator leve `@BloqueadoPorSubscricao()` aplicado a 5 endpoints de criação de conteúdo de negócio (`POST /processos`, `/clientes`, `/clientes/:id/interacoes`, `/ia/perguntar`, `/ia/sugestoes`), sem nenhum módulo de negócio importar `ComercialModule`. Estado efetivo de subscrição derivado dinamicamente (`SubscricaoService.obterEstadoEfetivo`), nunca escrito por scheduler. Edição/eliminação de recursos existentes, confirmação/rejeição de sugestões de IA, e ações administrativas da Fundação nunca bloqueadas (RN-10). Teste estrutural dedicado (T6) compara byte-a-byte a resposta de dois módulos distintos. 153/153 testes (141 herdados + 12 novos).

**Especificação técnica detalhada do Passo 21 (Stripe Checkout):** ver [Especificação Técnica do Passo 21](20-especificacao-tecnica-passo-21-stripe-checkout.md) — `POST /subscricao/checkout` (UC-07), primeiro contacto real com a Stripe. Só `starter`/`professional` self-service (Enterprise fica processo comercial); Stripe Price IDs via variáveis de ambiente; Cliente Stripe (`stripeCustomerId`) reaproveitado se já existir; `409` se a subscrição já está `ativa` (evita subscrição duplicada); `STRIPE_CLIENT` injetado via DI, `FakeStripeClient` nos testes, zero chamada de rede real; nunca decorado com `@BloqueadoPorSubscricao()` (Passo 20) — é a via de escape do próprio acesso limitado. `SubscricaoService.criarCheckout` único ponto responsável por esta lógica. Descoberta técnica real corrigida: SDK da Stripe exige `apiKey` não-vazia no construtor (ao contrário do SDK da Anthropic) — corrigido com valor de reserva inofensivo. 160/160 testes (153 herdados + 7 novos).

**Especificação técnica detalhada do Passo 22 (Webhooks Stripe):** ver [Especificação Técnica do Passo 22](21-especificacao-tecnica-passo-22-stripe-webhooks.md) — `POST /webhooks/stripe` (ADR-008 §3.4), fecha o ciclo completo de UC-07. Só `checkout.session.completed` tratado (decisão consciente de âmbito); `rawBody: true` (mecanismo nativo do NestJS) para a verificação de assinatura; `tenantContext.run()` reutilizado do `SubscricaoListener` (Passo 19); idempotência real via novo modelo `WebhookStripeProcessado` (verificar → mutar → registar); eventos não tratados sempre `200`; testes de assinatura com o SDK real da Stripe (`generateTestHeaderString`), prova genuína de rejeição criptográfica e de deduplicação real. `SubscricaoService.processarCheckoutConcluido` extensão do mesmo serviço. Sem descobertas técnicas emergentes. 166/166 testes (160 herdados + 6 novos).

**Especificação técnica detalhada do Passo 23 (Ecrã de Subscrição, frontend):** ver [Especificação Técnica do Passo 23](22-especificacao-tecnica-passo-23-ecra-subscricao.md) — último passo do M4. `GET /subscricao` (extensão aditiva, reaproveita `comercial.ver_planos`) agrega plano/estado/`estadoEfetivo`/limites/uso de IA, incluindo `usoIAPercentagem`/`avisoLimiteIAProximo` calculados no backend (exigência explícita da Fundadora/CEO: nenhum cálculo paralelo no frontend). `IaModule` passa a exportar `QuotaService` (`obterUsoAtual`), consumido por `ComercialModule` — primeira vez que `comercial` consome outro módulo de negócio. `apps/web/src/app/(autenticado)/subscricao/page.tsx`: plano atual, aviso a 90% de uso de IA (único limite com uso real medido), limites preparados, secção de upgrade (só quando não `ativa`, exclusivamente via `POST /subscricao/checkout` já existente), mensagem de confirmação/cancelamento. Item "Plano" na `BarraLateralNavegacao` (`admin_empresa` só); `BadgeEstado` estendido de forma aditiva com os 4 valores de `EstadoSubscricao`. Validado por inspeção visual real no browser (RBAC por navegação e por URL direto, aviso a 90%, upgrade, responsivo). **Com este passo aprovado, o Milestone M4 (Comercial e Pagamentos) está formalmente concluído** (Fundadora/CEO, 2026-07-08) — Passos 19 a 23 implementados, validados e aprovados. 171/171 testes (166 herdados + 5 novos).

**Proposta e início do Milestone M5 (Camada Comercial e Produto):** proposta completa (objetivos, âmbito em 3 blocos — A: EP-07 Landing/Pricing/Registo público, B: Configurações completa, C: UC-02 Convite por email —, exclusões, sequência de passos 24-31, riscos, DoD) apresentada e aprovada pela Fundadora/CEO em 2026-07-08, com 4 decisões adicionais validadas (fornecedor de email Resend atrás de interface própria; `GET /planos/publico` reutiliza integralmente `PLANOS_CONFIG`; Perfil edita só nome/palavra-passe com palavra-passe atual obrigatória; convidado define a própria palavra-passe ao aceitar). Numeração de passos continua a partir do M4.

**Especificação técnica detalhada do Passo 24 (`GET /planos/publico` + Página de Preços):** ver [Especificação Técnica do Passo 24](23-especificacao-tecnica-passo-24-precos-publico.md) — primeiro passo do M5. `GET /planos/publico` reaproveita `listarPlanos()` literalmente, no mesmo `ComercialController`, primeira rota pública desde o webhook Stripe (Passo 22), sem `@Throttle` dedicado (herda o limite global). Página `/precos` como Server Component — primeira página pública a usar este padrão em vez de Client Component + TanStack Query (decisão técnica, não altera o padrão dos ecrãs autenticados). **Descoberta técnica real corrigida**: o Next.js pré-renderia `/precos` estaticamente no build, congelando a resposta de `GET /planos/publico` até ao próximo deploy — corrigido com `export const dynamic = 'force-dynamic'`. `GET /planos/publico` marcado implementado na Superfície de API (§4). 173/173 testes (171 herdados + 2 novos).

**Especificação técnica detalhada do Passo 25 (Landing Page pública):** ver [Especificação Técnica do Passo 25](24-especificacao-tecnica-passo-25-landing-page.md) — segundo passo do M5, segundo passo do Bloco A. `apps/web/src/app/page.tsx` editado — comportamento com sessão inalterado (`redirect('/dashboard')`); sem sessão, renderiza a Landing Page (hero, 3 Pilares, 4 Módulos, CTA duplo `/precos`/`/login`). Toda a copy é citação direta de documentos já aprovados (Product Vision §3.1/3.5, Brand Book §3.9, PRD §3.4), sem frase nova — sem testemunhos de clientes nem citações de Persona (marcadas "ilustrativa, não literal" no documento de origem), sem concorrentes nomeados. Sem `export const dynamic` explícito — `cookies()`, já lido por `obterSessaoServidor()`, é suficiente para o Next.js optar por renderização dinâmica automaticamente (diferente do Passo 24). **Descoberta operacional real, sem impacto no código**: cache do servidor de desenvolvimento corrompido por `npm run build` a correr em paralelo com `npm run dev` — mesma classe de problema do Passo 13, corrigido apagando `.next`. Ecrãs "Landing Page" e "Pricing" marcados implementados no Inventário de Ecrãs (§5.2).

**Especificação técnica detalhada do Passo 26 (Ecrã de Registo público):** ver [Especificação Técnica do Passo 26](25-especificacao-tecnica-passo-26-registo-publico.md) — terceiro e último passo do Bloco A do M5. `apps/web/src/app/registar/page.tsx` (novo, Client Component) — `POST /auth/registar` (Passo 3, sem alteração) encadeado imediatamente com `POST /auth/login`, sem intervenção manual (Business Goals H1.4); CTAs "Começar" de `/` e `/precos` atualizados de `/login` para `/registar`. Campo "País" em texto livre (sem lista fixa de países documentada). **Sem checkbox de consentimento RGPD nesta fase — registado formalmente como bloqueador real antes de qualquer registo de cliente em produção**, confirmado pela Fundadora/CEO (Especificação Técnica do Passo 26, §5). **Descoberta técnica real corrigida**: `lib/api.ts` guardava o corpo de erro em bruto em `ApiError.message`, nunca extraído do JSON — corrigido com `extrairMensagemErro()` central, correção que beneficia todos os ecrãs, não só este passo. **Com este passo aprovado, o Bloco A do M5 (EP-07) está formalmente concluído** — percurso "Landing → Registo → Trial" operacional de ponta a ponta.

**Especificação técnica detalhada do Passo 27 (`PATCH /utilizadores/me`):** ver [Especificação Técnica do Passo 27](26-especificacao-tecnica-passo-27-perfil-self-edit.md) — primeiro passo do Bloco B (Configurações), backend puro (sem ecrã, fica para o Passo 28). Self-edit de nome/palavra-passe (nunca email/papel); primeira mutação do projeto sem `PermissaoGuard`, justificada por o alvo ser sempre `ctx.utilizadorId` (nenhuma escalada de privilégio possível). Alterar a palavra-passe invalida todas as outras sessões ativas, mantendo só a atual — feito numa transação real. **Descoberta técnica real resolvida antes de implementar**: `TenantPrismaService` incompatível com transação multi-operação genuína (cada operação sua já corre na sua própria transação interna de scoping) — resolvido com `PrismaService` bruto, mesmo padrão de `AuthService.registar` (Passo 3), scoping manual via `id_empresaId`. `PATCH /utilizadores/me` marcado implementado na Superfície de API (§4). 180/180 testes (173 herdados + 7 novos).

**Especificação técnica detalhada do Passo 28 (Ecrã "Configurações"):** ver [Especificação Técnica do Passo 28](27-especificacao-tecnica-passo-28-ecra-configuracoes.md) — último passo do Bloco B. Página única `/configuracoes` com 3 secções empilhadas (Perfil, Utilizadores/Permissões, Departamentos), cada uma um componente independente (pedido explícito da Fundadora/CEO, facilita evolução futura para separadores/sub-rotas). Item "Configurações" visível a todos os papéis, secções internas condicionadas por papel. Primeira `TabelaDados` do projeto com edição em linha (`Select` de papel/Departamento). **Descoberta real corrigida**: `SeccaoPerfil` não invalidava a query `utilizadores`, causando inconsistência visual entre secções — corrigido com `invalidateQueries`, componentes continuam independentes. Sem alteração de backend (todos os endpoints já existentes dos Passos 5/8/27). Ecrã "Configurações da Empresa / Utilizadores" marcado implementado no Inventário de Ecrãs (§5.2). **Com este passo aprovado, o Bloco B do M5 (Configurações) está formalmente concluído.**

**Especificação técnica detalhada do Passo 29 (Interface de email + `ConviteUtilizador`):** ver [Especificação Técnica do Passo 29](28-especificacao-tecnica-passo-29-email-convite.md) — primeiro passo do Bloco C (UC-02), backend-fundação puro, sem endpoint de produto neste passo (mesmo padrão sem-HTTP do AI Gateway, Passo 15) — fica para o Passo 30. Interface `EmailAdapterInterface`/`EMAIL_ADAPTER` (Regra não-negociável #5, mesmo desenho de interface completa do `AI_ADAPTER`, escolhido em vez do padrão simples da Stripe por o M5 ter aprovado explicitamente "interface própria" e não existir ADR a fixar o fornecedor de email como não-substituível); `ResendAdapter` único adaptador real, `FakeEmailAdapter` só em teste. Novo modelo `ConviteUtilizador` (tenant-scoped, RLS) com `EstadoConvite` (`pendente`/`aceite`/`revogado`, deliberadamente sem `expirado` — mesmo padrão de derivação em tempo de leitura de `SubscricaoPlano.obterEstadoEfetivo`, Passo 20); `token` gerado com `crypto.randomBytes(32).toString('hex')` (nunca `cuid()` — imprevisibilidade criptográfica exigida por ser um link mágico de criação de conta), expiração de 7 dias. **Confirmado que o bloqueador RGPD do registo público (Passo 26) não se estende ao convite por email** — bases legais distintas (consentimento no registo espontâneo vs. interesse legítimo/contrato no convite administrativo a um indivíduo já conhecido da Empresa). **Duas descobertas técnicas reais, ambas já documentadas como precedente noutros passos**: `EPERM` no ficheiro da engine do Prisma no Windows por bloqueio do servidor de preview da API a correr em simultâneo (corrigido parando o preview antes de `prisma generate`/`migrate dev`); migrações aplicadas a `nexa_dev` mas não a `nexa_test` (`npm run test:e2e` liga-se a `nexa_test`, que exige `prisma migrate deploy` explícito com a role `nexa_dev` — mesma descoberta "D9" já registada no Passo 7). 184/184 testes (180 herdados + 4 novos). Sem alteração à Superfície de API (§4) nem ao Inventário de Ecrãs (§5.2) — nenhum endpoint nem ecrã neste passo.

**Especificação técnica detalhada do Passo 30 (`POST /convites`, `GET /convites/:token`, `POST /convites/:token/aceitar`):** ver [Especificação Técnica do Passo 30](29-especificacao-tecnica-passo-30-convites-endpoints.md) — segundo passo do Bloco C (UC-02), com 3 Decisões a Validar aprovadas sem alteração: (A) `GET /convites/:token` público incluído, além dos dois endpoints literalmente fixados, para pré-visualização do convite (Empresa/papel/email, nunca `token`) antes da aceitação; (B) enforcement de `limiteUtilizadores` (RN-10/RN-11) deliberadamente **não** implementado — mecanismo genuinamente novo, distinto do `SubscricaoGuard` (que só cobre trial expirado), registado como Questão em Aberto explícita; (C) unicidade de email na aceitação global, mesmo padrão de `AuthService.registar`/UC-01 E1. `PRIVILEGIO` (hierarquia de privilégio, Passo 5) extraída para um ficheiro partilhado (`autorizacao/privilegio.ts`), reutilizada tal-e-qual por `atribuirPapel` e pelo novo `ConviteService.criar` — nunca uma segunda definição. Regras de autoridade CV-01 a CV-06 (mesmo rigor de L1-L6/P1-P5/RD-01-04). Ordem "enviar antes de persistir" — o email é enviado antes do `ConviteUtilizador` ser gravado; **observação da Fundadora/CEO sobre o cenário inverso (email entregue, persistência falhada) respondida formalmente na aprovação** (Especificação Técnica do Passo 30, §6) — cenário real e não eliminável sem um padrão de duas fases entre dois sistemas externos, escolha consciente da falha residual mais segura (link que não corresponde a nada, nunca um convite persistido mas nunca comunicado). `GET /convites/:token`/`POST /convites`/`POST /convites/:token/aceitar` marcados implementados na Superfície de API (§4), substituindo a rota planeada `/empresas/:id/utilizadores/convidar` (nunca implementada). Sem ecrã neste passo (fica para o Passo 31). 202/202 testes (184 herdados + 18 novos), estável em 2 execuções consecutivas.

**Especificação técnica detalhada do Passo 31 (Ecrã de Convite — envio + aceitação):** ver [Especificação Técnica do Passo 31](30-especificacao-tecnica-passo-31-ecra-convite.md) — último passo do Bloco C, fecha formalmente o Milestone M5, com 2 Decisões a Validar aprovadas sem alteração: (A) formulário de envio dentro de `SeccaoUtilizadores.tsx` (Configurações → Utilizadores e Permissões), local já fixado pela Information Architecture §3.4, reaproveitando o padrão `Modal` de `SeccaoDepartamentos.tsx` (Passo 28), sem rota nem nav item novos; (B) sem `GET /convites` (listagem) neste passo — mesma disciplina de Q2 do Passo 30 (reenvio/revogação), registado como melhoria futura. `PAPEIS_ATRIBUIVEIS` extraída para `apps/web/src/lib/tipos.ts`, reutilizada por `SeccaoUtilizadores.tsx` e pelo novo formulário. Seletor de papel nunca filtra por privilégio (mesmo princípio do Passo 28); seletor de Departamento oculto para `gestor` (decorre diretamente de CV-03, Passo 30). Novo `apps/web/src/app/convites/[token]/page.tsx` (público) — contexto do convite sempre visível antes do formulário de palavra-passe, três mensagens distintas para convite inexistente/expirado/já usado, aceitação encadeia `POST /auth/login` no frontend (mesmo padrão do Passo 26). Validado por inspeção visual real no browser nos papéis relevantes (`admin_empresa`, `gestor`, `colaborador`), incluindo o fluxo completo de aceitação com verificação na BD, responsivo em 375px/768px, zero erros de consola. Ecrã "Convite de Utilizador" marcado implementado no Inventário de Ecrãs (§5.2). **Com este passo aprovado, o Bloco C do M5 está formalmente concluído — e, com ele, o Milestone M5 (Camada Comercial e Produto) está formalmente concluído.**

**Proposta e início do Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases):** proposta completa (achados prévios, objetivos, critérios de conclusão, decisão sobre RN-10, sequência de passos 32-38, riscos, justificação da ordem de execução) apresentada e aprovada pela Fundadora/CEO em 2026-07-09, com 4 achados prévios: (A) cobertura automatizada do NFR-17 já existe, distribuída em M1-M5, falta consolidar; (B) inconsistência no Use Cases v1.0 ("9 Use Cases" afirmados, só 8 existem) — âmbito real são os 8 (UC-01 a UC-08), discrepância registada sem alterar o documento de origem; (C) UC-08 não pode ser validado honestamente sem resolver primeiro RN-10 (`limiteUtilizadores`); (D) FR-08 (telemetria), FR-09 (i18n), FR-27 (políticas de autonomia de IA) nunca implementados e sem decisão arquitetural — fora do âmbito dos Use Cases, registados como Questões em Aberto explícitas. Numeração de passos continua a partir do M5.

**Especificação técnica detalhada do Passo 32 (Consolidação NFR-17):** ver [Consolidação NFR-17](31-consolidacao-nfr-17.md) — primeiro passo do M6, sem Decisões a Validar (conteúdo já fixado pela Proposta do M6), com um acréscimo pedido pela Fundadora/CEO na aprovação: coluna "Lacunas identificadas" explícita por fluxo. Suite completa (202/202) corrida em 2 execuções consecutivas, estável. Mapeamento fluxo→testes→lacunas produzido para os 4 fluxos do NFR-17, com inspeção manual real das asserções e raciocínio explícito sobre o que aconteceria a cada teste perante uma regressão — confirmado que os testes falhariam genuinamente nos 4 fluxos. Duas lacunas honestas identificadas, nenhuma corrigida neste passo (âmbito de verificação): (1) isolamento multi-tenant — Camada 1 e Camada 2 (RLS) correm sempre em conjunto nos testes, nunca isoladas uma da outra (defesa em profundidade genuína, mas sem atribuição isolada por camada); (2) RN-10 sem testes, porque a funcionalidade ainda não existe — resolvido pelo Passo 33. Sem código de produção alterado, sem impacto arquitetural.

**Especificação técnica detalhada do Passo 33 (Enforcement de `limiteUtilizadores`, RN-10):** ver [Especificação Técnica do Passo 33](32-especificacao-tecnica-passo-33-limite-utilizadores.md) — segundo passo do M6, com 3 Decisões a Validar aprovadas sem alteração. Investigação prévia revelou uma tensão real com a Proposta do M6 ("reutiliza a infraestrutura comercial já existente"): `ConviteService` vive em `fundacao/`, e depender de `comercial` inverteria pela primeira vez a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`". (A) RN-10 implementado inteiramente dentro de `fundacao/convite/` — leitura direta de `SubscricaoPlano.limiteUtilizadores`, `LimiteUtilizadoresAtingidoError` própria (`402`, `code: LIMITE_UTILIZADORES_ATINGIDO`), nunca uma chamada a `comercial`, preservando a regra estrutural sem exceção; (B) contagem em `POST /convites` = Utilizadores ativos + Convites pendentes não expirados; em `POST /convites/:token/aceitar`, só Utilizadores ativos; (C) verificação nos dois pontos (convidar e aceitar), defesa em profundidade, mesma disciplina de CV-05 (Passo 30). Refinamento encontrado durante os testes (não uma alteração de decisão): aceitar um convite não liberta espaço (converte reserva em uso real, capacidade total mantém-se); só a expiração natural liberta espaço — a formulação inicial do plano de testes estava imprecisa nesse ponto, corrigida na implementação real. `convites.e2e-spec.ts` passou a importar `ComercialModule`/`IaModule` para o `SubscricaoListener` criar `SubscricaoPlano` reativamente — pré-requisito de teste, nunca ativa RN-11 em endpoints de Convite. 8 testes novos (T19-T26), suite completa em 210/210 (202 herdados + 8 novos), estável em 2 execuções consecutivas. Sem ecrã novo, sem alteração de schema. **UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados para validação manual.**

**Validação manual detalhada do Passo 34 (UC-01 + UC-02):** ver [Validação Manual UC-01 + UC-02](33-validacao-uc-01-uc-02.md) — terceiro passo do M6, sem Especificação Técnica prévia (âmbito já fixado pela Proposta do M6, sem decisão em aberto). Validação real no browser, Empresa de demonstração criada através do próprio ecrã de registo público (não via API), continuidade direta para UC-02 (Fluxo Alternativo 3a). UC-01: todos os itens confirmados (Fluxo Principal 1-5, RN-01/RN-02, Alternativo 3b — Departamento criado antes de qualquer convite, ordem nunca imposta —, Exceção E1). UC-02: todos os itens confirmados (Fluxo Principal 1-5, Exceção E1 — limite de Utilizadores, `402`/`LIMITE_UTILIZADORES_ATINGIDO` confirmado ao vivo via API e UI —, RN-03, RN-04). 4 achados registados, nenhum bloqueante: (A) o ecrã de registo nunca recolhe "setor", apesar de UC-01 o mencionar — divergência literal menor nunca antes assinalada; (B) envio real de email continua não observável neste ambiente — mesma limitação já conhecida desde o Passo 18/31; (C) Fluxo Alternativo 2a (`RegraPermissao` granular no convite) sem interface — consistente com a exclusão já decidida no M5; (D) Exceção E2 (reenvio de convite expirado) continua sem implementação — Questão em Aberto Q2 já registada, reconfirmada. Zero bugs encontrados, zero erros de consola.

**Validação manual detalhada do Passo 35 (UC-03 + UC-04):** ver [Validação Manual UC-03 + UC-04](34-validacao-uc-03-uc-04.md) — quarto passo do M6, sem Especificação Técnica prévia. UC-04 executado primeiro (criar Cliente) para viabilizar a associação em UC-03. UC-04: todos os itens confirmados (Fluxo Principal 1-4, RN-06 — Cliente existiu sem Interação, sem erro). UC-03: todos os itens confirmados (Fluxo Principal 1-3, Alternativo 3a — tarefa independente sem Cliente —, Exceção E1 — `403`, confirmada com pedido direto à API contornando o dropdown já filtrado da UI —, RN-05 — visibilidade por quem consulta, confirmada nos dois sentidos). 2 achados registados: (A, menor) Fluxo Alternativo 1a do UC-04 sem caminho de um único passo — oportunidade só editável num segundo passo; (B, **substantivo**) FR-18 (referência bidirecional Processo↔Cliente) parcialmente implementado — Processo→Cliente é texto estático, nunca um link; Cliente→Processo não mostra nada sobre Processos associados, em nenhuma forma — confirmado ao vivo, recomendado como item a corrigir num passo dedicado fora do âmbito deste M6. Zero bugs encontrados, zero erros de consola.

---

## 5. Design System e Inventário de Ecrãs

> **Princípio de UI/UX obrigatório:** todas as interfaces construídas respeitam integralmente o Brand Book já aprovado (cor, tipografia, espaçamento, tom de voz) — nunca reinterpretado por conveniência de implementação — e privilegiam uma experiência **simples, intuitiva, consistente e orientada para produtividade**, consistente com o pilar já fixado no Product Vision (3.5, "Rápido a adotar, sem fricção de implementação").

### 5.1 Componentes Base (Tailwind + Radix, ADR-006)

**✅ Implementado e aprovado (Passo 13, ver §4 acima e [Especificação Técnica do Passo 13](12-especificacao-tecnica-passo-13-design-system.md)).** Botão, Input, Select, Modal/Dialog, Menu Dropdown, Tabela de Dados, Cartão, Notificação Toast, Badge de Estado, Avatar, Estado Vazio Guiado (Information Architecture, 3.3) — todos configurados com os tokens exatos do Brand Book (cor, tipografia, espaçamento), em `apps/web/src/components/ui/`. **Barra Lateral de Navegação implementada no Passo 14** (com suporte responsivo em mobile, descoberta feita nesse passo). **Textarea adicionado no Passo 14** — extensão mínima não prevista na lista original dos 11 componentes, necessária para campos de texto multi-linha.

### 5.2 Ecrãs (a partir do Information Architecture, 3.1)

| Ecrã | Prioridade de construção |
|---|---|
| Landing Page | M5 — ✅ Implementado (Passo 25) |
| Pricing | M5 — ✅ Implementado (Passo 24) |
| Registo / Login | M1 (Login ✅ implementado no Passo 14); Registo público — M5 — ✅ Implementado (Passo 26); **fora de produção com utilizadores reais até existirem Termos de Serviço/Política de Privacidade e consentimento RGPD (Especificação Técnica do Passo 26, §5, bloqueador confirmado pela Fundadora/CEO)** |
| Dashboard | M2 — ✅ Implementado (Passo 14) |
| Processos (lista + detalhe) | M2 — ✅ Implementado (Passo 14) |
| CRM (lista + detalhe + pipeline) | M2 — ✅ Implementado (Passo 14) |
| Assistente de IA (conversa + sugestões pendentes) | M3 — ✅ Implementado (Passo 18) |
| Configurações da Empresa / Utilizadores | M5 — ✅ Implementado (Passo 28) — página única `/configuracoes` (Perfil, Utilizadores/Permissões, Departamentos), fecha o Bloco B |
| Convite de Utilizador (envio + aceitação, UC-02) | M5 — ✅ Implementado (Passo 31) — envio dentro de "Configurações → Utilizadores e Permissões" (`Modal`); aceitação pública em `/convites/:token`, fecha o Bloco C e o Milestone M5 |
| Checkout / Confirmação de Subscrição | M4 — ✅ Implementado (Checkout via Stripe Checkout hospedado, Passo 21; Confirmação via ecrã `/subscricao`, Passo 23) |
| Centro de Ajuda (estático) | M5 — fora do âmbito aprovado deste M5 (Especificação Técnica do Passo 24, §5); registado para milestone futuro |

---

## 5a. Princípio de Implementação — Simplicidade Operacional Sobre Sofisticação

*Adicionado por pedido explícito, aplicável a toda decisão técnica tomada durante a construção do MVP, não apenas às já registadas neste Blueprint.*

Sempre que existir mais do que uma solução tecnicamente válida para um problema de implementação, é privilegiada a que reunir, pela ordem de prioridade seguinte: **menor complexidade operacional**, **maior facilidade de manutenção assistida por IA**, e **maior capacidade de evolução futura** — sem nunca comprometer segurança, qualidade, ou escalabilidade. Este princípio estende à prática do dia a dia de construção o mesmo critério que já orientou todas as decisões dos 8 ADRs (ex: monólito modular em vez de microsserviços, sessões em vez de JWT, serviços geridos em vez de infraestrutura própria) — não introduz um critério novo, torna-o explícito para decisões futuras de detalhe que nenhum ADR cobre individualmente.

---

## 6. Decisões Tomadas

| # | Decisão | Justificação |
|---|---|---|
| D1 | Fases 4-6 compactadas num único documento, em vez de 3 documentos separados | Consistente com o pedido explícito de velocidade com solidez, sem burocracia desproporcional a uma equipa de uma pessoa |
| D2 | O schema Prisma e a lista de API são tratados como "primeira versão real", não contrato final | Espera-se refinamento durante a construção — documento vivo, tal como o Coding Standards |
| D3 | Cada Milestone recebe um Definition of Done explícito e verificável | Elimina ambiguidade sobre quando uma etapa está genuinamente concluída, consistente com a disciplina de Exit Criteria já aplicada no Master Roadmap |
| D4 | Alterações estruturais ao schema Prisma exigem rastreabilidade formal com o Data Model Conceptual e ADRs; alterações de detalhe não | Equilibra rigor documental com velocidade de implementação — nem tudo exige o mesmo nível de formalidade |
| D5 | A superfície de API é o conjunto mínimo, extensível durante o desenvolvimento sob condições explícitas | Evita tanto a rigidez de uma lista fechada como a deriva descontrolada de endpoints sem base em requisito aprovado |
| D6 | UI/UX obrigada a respeitar integralmente o Brand Book e a privilegiar simplicidade/produtividade | Torna explícito, ao nível de implementação, um compromisso de marca e experiência já aprovado em documentos anteriores |
| D7 | Formalizado o Princípio de Implementação de simplicidade operacional sobre sofisticação | Estende à prática diária de construção o mesmo critério que já orientou todas as decisões dos 8 ADRs |
| D8 | O Passo 2 (M1) foi executado e aprovado com refinamentos de detalhe face ao schema literal da secção 3 (ver 3a), e com a decisão explícita de adiar a ativação de RLS para o Passo 4 | Os refinamentos implementam regras já aprovadas (Data & Consistency Rules 3.4/3.7, ADR-004) sem alterar o modelo de isolamento; a decisão de RLS evita falsa sensação de segurança antes de o middleware de tenant e o utilizador de BD dedicado existirem |
| D9 | O Passo 3 (M1, Autenticação) foi precedido de uma especificação técnica formal própria, aprovada antes da implementação, e não apenas documentado retroativamente como o Passo 2 | A Fundadora/CEO pediu explicitamente esse nível de rigor para este passo (fluxo completo, arquitetura de segurança, conformidade, Exit Criteria) — passa a ser o padrão de governação para os passos seguintes do M1, não uma exceção pontual |

---

## 7. Histórico de Alterações

| Versão | Data | Alteração | Autor |
|---|---|---|---|
| 1.0 | 2026-07-02 | Criação do Blueprint, compactando Fases 4-6: Épicos/Milestones, schema Prisma inicial, superfície de API, e inventário de Design System/Ecrãs | CTO (Claude) + Fundadora/CEO |
| 1.1 | 2026-07-02 | Adicionados Definition of Done por Milestone, regra de rastreabilidade para alterações estruturais ao schema, nota de âmbito mínimo da API, princípio de UI/UX (Brand Book + produtividade), e novo Princípio de Implementação (simplicidade operacional sobre sofisticação) | CTO (Claude) + Fundadora/CEO |
| 1.1 | 2026-07-02 | **Aprovação oficial.** Documento passa a estado Aprovado — pronto para servir de base à construção do MVP | Fundadora/CEO |
| 1.2 | 2026-07-06 | Adicionada a secção 3a (Estado de Implementação do Schema — Passo 2), registando a execução real do Passo 2 do M1: ficheiro `schema.prisma`, migração aplicada, refinamentos de detalhe face ao schema literal (com justificação documental de cada um), decisão explícita de adiar RLS para o Passo 4, e confirmação de que `Favorito`/`Item Recente` continuam corretamente fora do âmbito do MVP | CTO (Claude) + Fundadora/CEO |
| 1.2 | 2026-07-06 | **Passo 2 aprovado pela Fundadora/CEO.** Implementação validada, incluindo verificação empírica do isolamento multi-tenant por constraint de chave estrangeira | Fundadora/CEO |
| 1.3 | 2026-07-06 | Passo 3 (Autenticação) marcado como implementado na Superfície de API (§4), com referência à nova Especificação Técnica do Passo 3 (documento próprio, aprovado formalmente antes da implementação); adicionada a decisão D9 | CTO (Claude) + Fundadora/CEO |
| 1.4 | 2026-07-06 | Passo 4 (Camada 1 — middleware de tenant + RLS + serviço único de autorização, o mais crítico do M1) concluído e aprovado, com Especificação Técnica própria (docs/04-implementation-blueprint/03-especificacao-tecnica-passo-4-camada1-autorizacao.md), incluindo duas correções técnicas identificadas e aprovadas durante a implementação (TenantContextMiddleware em vez de SessionGuard; role de BD `nexa_fundacao` com BYPASSRLS) | CTO (Claude) + Fundadora/CEO |
| 1.5 | 2026-07-06 | Passo 5 (RBAC granular) concluído e aprovado, com Especificação Técnica própria (docs/04-implementation-blueprint/04-especificacao-tecnica-passo-5-rbac.md), incluindo revisão exigida pela Fundadora/CEO a meio (definição inequívoca de autoridade para alterar papéis, 6 limites L1-L6) e correção de teste retroativa ao Passo 4 (ValidationPipe em falta nos harnesses Jest). `PATCH /utilizadores/:id/papel` marcado implementado na Superfície de API | CTO (Claude) + Fundadora/CEO |
| 1.6 | 2026-07-06 | Passo 6 (Registo de Auditoria) concluído e aprovado, com Especificação Técnica própria (docs/04-implementation-blueprint/05-especificacao-tecnica-passo-6-auditoria.md) — mecanismo orientado a eventos com `emitAsync`, campo `detalhe`, trigger de imutabilidade, role `nexa_auditoria_interna`. `GET /auditoria` marcado implementado. **Definition of Done do M1 (§2.2) tecnicamente completo** | CTO (Claude) + Fundadora/CEO |
| 1.7 | 2026-07-06 | **Passo 6 formalmente aprovado pela Fundadora/CEO.** Resolvida a questão em aberto sobre o encerramento do M1: decisão explícita de que o Passo 7 (Partilha) é pré-requisito para o encerramento formal do Milestone, apesar do DoD literal (§2.2) já estar tecnicamente cumprido — atualizada a referência ao Passo 6 em §4 para refletir esta decisão | CTO (Claude) + Fundadora/CEO |
| 1.8 | 2026-07-06 | **Passo 7 (Partilha) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M1 (Fundação) formalmente concluído.** `POST/DELETE/GET /partilhas` marcados implementados na Superfície de API (§4); Especificação Técnica própria (docs/04-implementation-blueprint/06-especificacao-tecnica-passo-7-partilha.md) — `AuthorizationService.podeAcederViaPartilha`, regras P1-P5, campos `nivelAcesso`/`revogadoEm`. Todos os passos previstos no Blueprint para o M1 (0-7) estão agora implementados, validados e aprovados; próximo Milestone (M2) por confirmar com a Fundadora/CEO | CTO (Claude) + Fundadora/CEO |
| 1.9 | 2026-07-06 | **Proposta do Milestone M2 (Módulos Core) apresentada e aprovada pela Fundadora/CEO** — sequência de Passos 8-14, dependências, riscos, DoD e decisões arquitetónicas A-C validadas. **Passo 8 (Departamento) concluído e formalmente aprovado** — CRUD completo + atribuição de Utilizador a Departamento, com Especificação Técnica própria (docs/04-implementation-blueprint/07-especificacao-tecnica-passo-8-departamento.md). Endpoints `/departamentos` e `/utilizadores/:id/departamento` marcados implementados na Superfície de API (§4), substituindo o literal `/empresas/:id/departamentos` (nunca implementado) | CTO (Claude) + Fundadora/CEO |
| 2.0 | 2026-07-06 | **Passo 9 (Processos e Tarefas) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro módulo de negócio fora da Fundação, com Especificação Técnica própria (docs/04-implementation-blueprint/08-especificacao-tecnica-passo-9-processos.md): `AuthorizationService` estendido (visibilidade RBAC centralizada, Decisão B do M2), `PartilhaService` refatorado para a consumir, `Processo.estado` como `enum` (Decisão C do M2), regras PR-01 a PR-07. Endpoints `/processos` marcados implementados na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 2.1 | 2026-07-06 | **Passo 10 (CRM) concluído e formalmente aprovado pela Fundadora/CEO** — segundo módulo de negócio, com Especificação Técnica própria (docs/04-implementation-blueprint/09-especificacao-tecnica-passo-10-crm.md): zero alterações ao `AuthorizationService` (confirmação prática da Decisão B do M2), `Cliente.estadoOportunidade` como `enum`, regras CR-01 a CR-06 e IR-01 a IR-03, sem eliminação de Cliente (decisão deliberada). Endpoints `/clientes` e `/pipeline` marcados implementados na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 2.2 | 2026-07-06 | **Passo 11 (Notification Dispatcher) concluído e formalmente aprovado pela Fundadora/CEO** — com Especificação Técnica própria (docs/04-implementation-blueprint/10-especificacao-tecnica-passo-11-notification-dispatcher.md): `NotificacaoListener` fire-and-forget sobre o `EVENTO_AUDITORIA` já existente, 5 gatilhos mínimos, sem nova superfície de API (exposição fica para o Passo 12) | CTO (Claude) + Fundadora/CEO |
| 2.3 | 2026-07-07 | **Passo 12 (Dashboard) concluído e formalmente aprovado pela Fundadora/CEO — backend do M2 concluído (Passos 8-12).** Com Especificação Técnica própria (docs/04-implementation-blueprint/11-especificacao-tecnica-passo-12-dashboard.md): terceiro módulo de negócio, zero alterações ao `AuthorizationService` (terceira confirmação da Decisão B do M2), `GET /notificacoes`/`PATCH .../lida` (exposição herdada do Passo 11). Endpoints `/dashboard` e `/notificacoes` marcados implementados na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 2.4 | 2026-07-07 | **Passo 13 (Design System, frontend) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo de `apps/web` desde o scaffolding do Passo 1, com Especificação Técnica própria (docs/04-implementation-blueprint/12-especificacao-tecnica-passo-13-design-system.md): 11 componentes base sobre primitivas Radix UI, tokens de marca completos, vitrine interna em `/design-system`, validado por inspeção visual real no browser. §5.1 marcado implementado; próximo e último passo do M2: Passo 14 (Ecrãs) | CTO (Claude) + Fundadora/CEO |
| 2.5 | 2026-07-07 | **Passo 14 (Ecrãs, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M2 (Módulos Core) formalmente concluído.** Implementado em 3 sub-entregas sequenciais (Login+Dashboard, Processos, CRM), cada uma validada e aprovada individualmente, com Especificação Técnica própria (docs/04-implementation-blueprint/13-especificacao-tecnica-passo-14-ecras.md): ecrãs `/login`, `/dashboard`, `/processos`, `/crm` completos; `GET /utilizadores` e `POST /auth/logout` adicionados como extensões mínimas ao backend; `Textarea` adicionado ao Design System; `TabelaDados` corrigida para scroll horizontal responsivo. §5.1/§5.2 e Superfície de API (§4) atualizados | CTO (Claude) + Fundadora/CEO |
| 2.6 | 2026-07-07 | **Proposta do Milestone M3 (Assistente de IA) aprovada pela Fundadora/CEO; Passo 15 (AI Gateway) concluído e formalmente aprovado** — primeiro passo do M3, com Especificação Técnica própria (docs/04-implementation-blueprint/14-especificacao-tecnica-passo-15-ai-gateway.md): implementa integralmente o ADR-005 (adaptador Anthropic, timeout/circuit breaker/quota, neutralidade de tipos); novo módulo `apps/api/src/modules/ia/`, sem endpoint de produto ainda; descoberta real (`UsoIAMensal`, novo modelo aditivo, já que `SubscricaoPlano.limiteUsoIA` nunca foi um contador); testes exclusivamente com `FakeAdapter` | CTO (Claude) + Fundadora/CEO |
| 2.7 | 2026-07-07 | **Passo 16 (módulo `ia` — pergunta livre) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro endpoint de produto do M3, com Especificação Técnica própria (docs/04-implementation-blueprint/15-especificacao-tecnica-passo-16-ia-perguntar.md): `POST /ia/perguntar` (UC-05); `AuthorizationService.construirFiltroWhere` extraído do `DashboardService` (4ª confirmação da Decisão B do M2); RN-07 garantida estruturalmente por `IaService.reunirResumoOperacional()`; contexto agregado (decisão consciente de âmbito); extensão aditiva do schema `SugestaoIA`; retenção de conteúdo configurável (PSD-003). `POST /ia/perguntar` marcado implementado na Superfície de API (§4). NFR-17 ("ações de IA") com a metade "pergunta" coberta; metade "sugestão/confirmação" fica para o Passo 17 | CTO (Claude) + Fundadora/CEO |
| 2.8 | 2026-07-07 | **Passo 17 (sugestões de ação) concluído e formalmente aprovado pela Fundadora/CEO — NFR-17 fecha na íntegra.** Especificação Técnica própria (docs/04-implementation-blueprint/16-especificacao-tecnica-passo-17-ia-sugestoes.md): `POST /ia/sugestoes` (geração determinística), `.../confirmar`, `.../rejeitar` (UC-06, RN-08); único tipo de ação suportado — reatribuição de Processo em atraso; `ProcessosModule` exporta `ProcessosService`, consumido por `IaModule` (primeiro módulo de negócio a consumir outro); RN-08 garantida em duas camadas (tipos + eventos); extensão aditiva do schema `SugestaoIA` (`acaoPayload`). Endpoints `/ia/sugestoes*` marcados implementados na Superfície de API (§4), incluindo a extensão aditiva `POST /ia/sugestoes` (geração) face à lista mínima original | CTO (Claude) + Fundadora/CEO |
| 2.9 | 2026-07-07 | **Passo 18 (ecrã do Assistente de IA, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M3 (Assistente de IA) formalmente concluído.** Último passo do M3, com Especificação Técnica própria (docs/04-implementation-blueprint/17-especificacao-tecnica-passo-18-ecra-ia.md): ecrã `/ia` (Perguntar + Sugestões Pendentes), `GET /ia/sugestoes` (extensão aditiva ao backend, herdada da Decisão F do Passo 17), `Modal` de segunda confirmação refletindo RN-08 na interface, item "Assistente de IA" na `BarraLateralNavegacao`. Validado por inspeção visual real no browser nos 4 papéis, incluindo execução real de uma reatribuição confirmada. `GET /ia/sugestoes` marcado implementado na Superfície de API (§4). **NFR-17 coberto na íntegra** — os 4 fluxos críticos obrigatórios do projeto têm agora todos cobertura de teste automatizado | CTO (Claude) + Fundadora/CEO |
| 3.0 | 2026-07-07 | **Proposta do Milestone M4 (Comercial e Pagamentos) aprovada pela Fundadora/CEO; Passo 19 (`SubscricaoPlano` real, `GET /planos`) concluído e formalmente aprovado** — primeiro passo do M4, primeiro módulo `comercial` construído. Resolve o Risco R3 do Master Roadmap (limites por plano: Starter 5/1024MB/50, Professional 20/10240MB/200, Enterprise sem limite). `SubscricaoPlano` ativado com trial automático de 14 dias (Professional), criado reativamente via evento existente (`fundacao` nunca conhece `comercial`), idempotente por exigência explícita da Fundadora/CEO. `plano`/`estado` promovidos a `enum`; `Empresa.estadoSubscricao` removido; limites `Int?` (`null`=sem limite); `QuotaService` corrigido. `GET /planos` marcado implementado na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 3.1 | 2026-07-08 | **Passo 20 (enforcement uniforme de RN-11) concluído e formalmente aprovado pela Fundadora/CEO.** `SubscricaoGuard`/`SubscricaoExceptionFilter` globais (`APP_GUARD`/`APP_FILTER`) garantem resposta uniforme (`402`, `code: SUBSCRICAO_LIMITADA`) em toda a aplicação — decorator leve `@BloqueadoPorSubscricao()` em 5 endpoints de criação, sem nenhum módulo de negócio importar `ComercialModule`; estado efetivo derivado dinamicamente, sem scheduler; RN-10 preservada (edição/eliminação, confirmação de sugestões, ações administrativas nunca bloqueadas). Teste estrutural dedicado (T6) prova a uniformidade byte-a-byte. Implementação estritamente conforme a especificação aprovada, sem desvios de arquitetura ou âmbito | CTO (Claude) + Fundadora/CEO |
| 3.2 | 2026-07-08 | **Passo 21 (Stripe Checkout) concluído e formalmente aprovado pela Fundadora/CEO.** `POST /subscricao/checkout` (UC-07) — primeiro contacto real com a Stripe; só Starter/Professional self-service; Price IDs via variáveis de ambiente; Cliente Stripe reaproveitado; `409` se já `ativa`; `STRIPE_CLIENT`/`FakeStripeClient` para testabilidade sem credenciais reais; nunca sujeito ao `SubscricaoGuard` (Passo 20). `SubscricaoService.criarCheckout` único ponto responsável. Descoberta técnica real corrigida (SDK Stripe exige `apiKey` não-vazia no construtor), sem alteração de âmbito ou arquitetura. `POST /subscricao/checkout` marcado implementado na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 3.3 | 2026-07-08 | **Passo 22 (Webhooks Stripe) concluído e formalmente aprovado pela Fundadora/CEO — fecha o ciclo completo de UC-07.** `POST /webhooks/stripe` (ADR-008 §3.4); só `checkout.session.completed` tratado (decisão consciente de âmbito); `rawBody: true` para verificação de assinatura; `tenantContext.run()` reutilizado do Passo 19; idempotência real via `WebhookStripeProcessado`; eventos não tratados sempre `200`; testes com o SDK real da Stripe (prova genuína de rejeição criptográfica e de deduplicação). `SubscricaoService.processarCheckoutConcluido` extensão do mesmo serviço. Sem descobertas técnicas emergentes. `POST /webhooks/stripe` marcado implementado na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 3.4 | 2026-07-08 | **Passo 23 (Ecrã de Subscrição, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M4 (Comercial e Pagamentos) formalmente concluído.** Último passo do M4, com Especificação Técnica própria (docs/04-implementation-blueprint/22-especificacao-tecnica-passo-23-ecra-subscricao.md): `GET /subscricao` (extensão aditiva, reaproveita `comercial.ver_planos`), `usoIAPercentagem`/`avisoLimiteIAProximo` calculados no backend (exigência explícita da Fundadora/CEO: nenhum cálculo paralelo no frontend); `IaModule` exporta `QuotaService`, consumido por `ComercialModule` (primeira vez que `comercial` consome outro módulo de negócio); ecrã `/subscricao` (plano atual, aviso a 90% de uso de IA, limites preparados, upgrade exclusivamente via `POST /subscricao/checkout` já existente); item "Plano" na `BarraLateralNavegacao` (`admin_empresa` só); `BadgeEstado` estendido com os 4 valores de `EstadoSubscricao`. `GET /subscricao` marcado implementado na Superfície de API (§4). Validado por inspeção visual real no browser. Todos os passos previstos para o M4 (19-23) implementados, validados e aprovados | CTO (Claude) + Fundadora/CEO |
| 3.5 | 2026-07-08 | **Proposta do Milestone M5 (Camada Comercial e Produto) aprovada pela Fundadora/CEO; Passo 24 (`GET /planos/publico` + Página de Preços) concluído e formalmente aprovado** — primeiro passo do M5, com Especificação Técnica própria (docs/04-implementation-blueprint/23-especificacao-tecnica-passo-24-precos-publico.md): âmbito do M5 em 3 blocos (EP-07 Landing/Pricing/Registo, Configurações, UC-02 Convite por email); `GET /planos/publico` reaproveita `listarPlanos()` literalmente, primeira rota pública desde o webhook Stripe; página `/precos` como Server Component. Descoberta técnica real corrigida (Next.js pré-renderaria `/precos` estaticamente no build, congelando os dados — corrigido com `dynamic = 'force-dynamic'`). `GET /planos/publico` marcado implementado na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 3.6 | 2026-07-08 | **Passo 25 (Landing Page pública) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do M5, com Especificação Técnica própria (docs/04-implementation-blueprint/24-especificacao-tecnica-passo-25-landing-page.md): `apps/web/src/app/page.tsx` editado, toda a copy citação direta de documentos já aprovados (Product Vision, Brand Book, PRD), sem testemunhos/Persona/concorrentes. Sem `export const dynamic` explícito — `cookies()` já suficiente. Descoberta operacional real sem impacto no código (cache do servidor de desenvolvimento, mesma classe do Passo 13). Ecrãs "Landing Page" e "Pricing" marcados implementados no Inventário de Ecrãs (§5.2); linhas obsoletas do mesmo inventário corrigidas (Assistente de IA, Checkout/Confirmação de Subscrição, Configurações da Empresa) | CTO (Claude) + Fundadora/CEO |
| 3.7 | 2026-07-08 | **Passo 26 (Ecrã de Registo público) concluído e formalmente aprovado pela Fundadora/CEO — Bloco A do M5 formalmente concluído.** Terceiro e último passo do Bloco A, com Especificação Técnica própria (docs/04-implementation-blueprint/25-especificacao-tecnica-passo-26-registo-publico.md): `apps/web/src/app/registar/page.tsx` (novo), `POST /auth/registar` encadeado com `POST /auth/login` sem intervenção manual; CTAs de `/` e `/precos` atualizados para `/registar`. **Sem consentimento RGPD nesta fase — bloqueador formal e explícito antes de qualquer registo real em produção**, confirmado pela Fundadora/CEO. Descoberta técnica real corrigida (`lib/api.ts` não extraía `message` do JSON de erro — corrigido centralmente, beneficia toda a aplicação). Ecrã "Registo / Login" atualizado no Inventário de Ecrãs (§5.2) com a nota de bloqueador | CTO (Claude) + Fundadora/CEO |
| 3.8 | 2026-07-08 | **Passo 27 (`PATCH /utilizadores/me`) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo do Bloco B (Configurações), com Especificação Técnica própria (docs/04-implementation-blueprint/26-especificacao-tecnica-passo-27-perfil-self-edit.md): self-edit de nome/palavra-passe, primeira mutação sem `PermissaoGuard` (alvo sempre `ctx.utilizadorId`); alterar a palavra-passe invalida todas as outras sessões ativas numa transação real. Descoberta técnica real resolvida antes de implementar (`TenantPrismaService` incompatível com transação multi-operação — resolvido com `PrismaService` bruto, mesmo padrão de `AuthService.registar`). `PATCH /utilizadores/me` marcado implementado na Superfície de API (§4) | CTO (Claude) + Fundadora/CEO |
| 3.9 | 2026-07-08 | **Passo 28 (Ecrã "Configurações") concluído e formalmente aprovado pela Fundadora/CEO — Bloco B do M5 formalmente concluído.** Último passo do Bloco B, com Especificação Técnica própria (docs/04-implementation-blueprint/27-especificacao-tecnica-passo-28-ecra-configuracoes.md): página única `/configuracoes` (Perfil, Utilizadores/Permissões, Departamentos), 3 componentes independentes (pedido da CEO); primeira `TabelaDados` com edição em linha; item "Configurações" visível a todos os papéis. Descoberta real corrigida (`SeccaoPerfil` não invalidava a query `utilizadores` — corrigido, componentes continuam independentes). Sem alteração de backend. Ecrã "Configurações da Empresa / Utilizadores" marcado implementado no Inventário de Ecrãs (§5.2) | CTO (Claude) + Fundadora/CEO |
| 3.10 | 2026-07-08 | **Passo 29 (Interface de email + `ConviteUtilizador`) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo do Bloco C (UC-02), com Especificação Técnica própria (docs/04-implementation-blueprint/28-especificacao-tecnica-passo-29-email-convite.md): `EmailAdapterInterface`/`EMAIL_ADAPTER` (interface completa, mesmo desenho do AI Gateway), `ResendAdapter` real + `FakeEmailAdapter` de teste; novo modelo `ConviteUtilizador` (tenant-scoped, RLS), `EstadoConvite` sem valor `expirado` (derivado em leitura), `token` criptográfico de 32 bytes, expiração de 7 dias. Confirmado que o bloqueador RGPD do Passo 26 não se estende ao convite por email (bases legais distintas). Duas descobertas técnicas reais, ambas precedentes já documentados noutros passos (`EPERM` do Prisma no Windows por preview a correr em simultâneo; migrações não aplicadas a `nexa_test`, mesma descoberta "D9" do Passo 7). Backend-fundação puro, sem endpoint de produto nem ecrã neste passo — sem alteração à Superfície de API (§4) nem ao Inventário de Ecrãs (§5.2). 184/184 testes | CTO (Claude) + Fundadora/CEO |
| 3.11 | 2026-07-08 | **Passo 30 (`POST /convites`, `GET /convites/:token`, `POST /convites/:token/aceitar`) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do Bloco C, com Especificação Técnica própria (docs/04-implementation-blueprint/29-especificacao-tecnica-passo-30-convites-endpoints.md) e 3 Decisões a Validar aprovadas sem alteração (A: `GET /convites/:token` público para pré-visualização; B: `limiteUtilizadores` deliberadamente não implementado, Questão em Aberto explícita; C: unicidade de email global na aceitação, mesmo padrão do registo). `PRIVILEGIO` (Passo 5) extraída para ficheiro partilhado, reutilizada sem duplicação. Regras de autoridade CV-01 a CV-06. Ordem "enviar antes de persistir" — observação da Fundadora/CEO sobre o cenário de email entregue com persistência falhada respondida formalmente na aprovação (§6 da Especificação Técnica): risco real e inerente à ausência de atomicidade entre dois sistemas externos, mitigado com `try/catch` dedicado, `Logger.error` para seguimento operacional e `500` explícito, nunca um `201` enganador. Sem auto-login no endpoint de aceitação — o Passo 31 encadeia `POST /auth/login` no frontend, mesmo padrão já aprovado no Passo 26. 3 endpoints marcados implementados na Superfície de API (§4), substituindo a rota planeada nunca implementada. Sem ecrã neste passo (fica para o Passo 31). 202/202 testes (184 herdados + 18 novos) | CTO (Claude) + Fundadora/CEO |
| 3.12 | 2026-07-08 | **Passo 31 (Ecrã de Convite — envio + aceitação) concluído e formalmente aprovado pela Fundadora/CEO — último passo do Bloco C, Milestone M5 formalmente concluído.** Com Especificação Técnica própria (docs/04-implementation-blueprint/30-especificacao-tecnica-passo-31-ecra-convite.md) e 2 Decisões a Validar aprovadas sem alteração (A: formulário de envio dentro de Configurações → Utilizadores e Permissões, local já fixado pela Information Architecture; B: sem `GET /convites` de listagem neste passo, registado como melhoria futura). `PAPEIS_ATRIBUIVEIS` extraída para `apps/web/src/lib/tipos.ts`. Seletor de papel nunca filtra por privilégio (mesmo princípio do Passo 28); seletor de Departamento oculto para `gestor` (decorre de CV-03, Passo 30). Novo `apps/web/src/app/convites/[token]/page.tsx` público — contexto do convite sempre visível antes da palavra-passe, três mensagens distintas para convite inexistente/expirado/já usado, aceitação encadeia login no frontend (mesmo padrão do Passo 26). Validado por inspeção visual real no browser (`admin_empresa`/`gestor`/`colaborador`, fluxo completo de aceitação verificado na BD, responsivo, zero erros de consola). Ecrã "Convite de Utilizador" marcado implementado no Inventário de Ecrãs (§5.2). **Milestone M5 (Camada Comercial e Produto) formalmente concluído** — Passos 24-31 implementados, validados e aprovados, os 3 blocos (A, B, C) operacionais de ponta a ponta | CTO (Claude) + Fundadora/CEO |
| 3.13 | 2026-07-09 | **Proposta do Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) aprovada pela Fundadora/CEO; Passo 32 (Consolidação NFR-17) concluído e formalmente aprovado** — primeiro passo do M6. 4 achados prévios: cobertura NFR-17 já existe distribuída, falta consolidar; inconsistência no Use Cases v1.0 (8 UCs reais, não 9); UC-08 exige resolver RN-10 primeiro; FR-08/09/27 registados como Questões em Aberto, fora do âmbito dos Use Cases. Suite completa (202/202) em 2 execuções consecutivas; mapeamento fluxo→testes→lacunas com inspeção manual real das asserções, coluna "Lacunas identificadas" por pedido explícito da Fundadora/CEO. Duas lacunas honestas registadas (isolamento — Camadas 1/2 nunca testadas isoladamente; RN-10 sem testes por ainda não existir), nenhuma corrigida neste passo. Sem código de produção alterado | CTO (Claude) + Fundadora/CEO |
| 3.14 | 2026-07-09 | **Passo 33 (Enforcement de `limiteUtilizadores`, RN-10) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do M6, com Especificação Técnica própria (docs/04-implementation-blueprint/32-especificacao-tecnica-passo-33-limite-utilizadores.md) e 3 Decisões a Validar aprovadas sem alteração (A: RN-10 implementado inteiramente dentro de `fundacao/convite/`, sem depender de `comercial`, preservando a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`" sem exceção — tensão real encontrada e trazida de volta para validação; B: contagem em `POST /convites` inclui Convites pendentes, em `POST /convites/:token/aceitar` só Utilizadores ativos; C: verificação nos dois pontos, defesa em profundidade). `LimiteUtilizadoresAtingidoError` própria (`402`/`LIMITE_UTILIZADORES_ATINGIDO`), nunca reutiliza a classe de erro de `comercial`. Refinamento encontrado nos testes: aceitar um convite não liberta espaço, só a expiração natural liberta — corrigido na implementação, sem alterar as Decisões A-C. 8 testes novos (T19-T26), suite completa em 210/210 (202 herdados + 8 novos). UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados para validação manual (Passos 34/37) | CTO (Claude) + Fundadora/CEO |
| 3.15 | 2026-07-11 | **Passo 34 (Validação Manual UC-01 + UC-02) concluído e formalmente aprovado pela Fundadora/CEO** — terceiro passo do M6, com registo próprio (docs/04-implementation-blueprint/33-validacao-uc-01-uc-02.md), sem Especificação Técnica prévia (âmbito já fixado pela Proposta do M6). Validação real no browser via o próprio ecrã de registo público, continuidade direta para UC-02. UC-01 e UC-02: todos os itens (fluxo principal, alternativas, exceções, regras de negócio) confirmados no sistema real, incluindo a Exceção E1 do UC-02 (limite de Utilizadores) ao vivo via API e UI. 4 achados registados, nenhum bloqueante (campo "setor" nunca recolhido no registo; envio real de email não observável, limitação já conhecida; Fluxo Alternativo 2a do UC-02 sem interface, consistente com exclusão já decidida no M5; Exceção E2 do UC-02 sem implementação, Questão em Aberto já registada). Zero bugs, zero erros de consola | CTO (Claude) + Fundadora/CEO |
| 3.16 | 2026-07-11 | **Passo 35 (Validação Manual UC-03 + UC-04) concluído e formalmente aprovado pela Fundadora/CEO** — quarto passo do M6, com registo próprio (docs/04-implementation-blueprint/34-validacao-uc-03-uc-04.md), sem Especificação Técnica prévia. UC-04 executado primeiro para viabilizar a associação em UC-03. UC-03 e UC-04: todos os itens confirmados no sistema real, incluindo a Exceção E1 do UC-03 testada diretamente contra a API (contornando o dropdown já filtrado da UI) e RN-05 confirmada nos dois sentidos. 2 achados registados: (A, menor) Fluxo Alternativo 1a do UC-04 sem caminho de um único passo; (B, **substantivo**) FR-18 (referência bidirecional Processo↔Cliente) parcialmente implementado — Processo→Cliente é texto estático, Cliente→Processo não mostra nada — recomendado como item a corrigir num passo dedicado, fora do âmbito do M6. Zero bugs, zero erros de consola | CTO (Claude) + Fundadora/CEO |
| 3.17 | 2026-07-11 | **Passo 36 (Validação Manual UC-05 + UC-06) concluído e formalmente aprovado pela Fundadora/CEO** — quinto passo do M6, com registo próprio (docs/04-implementation-blueprint/35-validacao-uc-05-uc-06.md), sem Especificação Técnica prévia. Fixtures adicionais criados para UC-06 (Departamento, 2 Colaboradores via convite direto na BD, 4 Processos em atraso) para exercitar a heurística determinística (Passo 17) nos 4 desfechos possíveis de uma sugestão. UC-05: Fluxo Principal 1/3/5 e Exceção E1 (fornecedor indisponível, `502`, mensagem clara) confirmados ao vivo; Fluxo Principal 2/Alternativo 2a (RN-07) já cobertos por teste automatizado dedicado (T2, Passo 16); FR-28 confirmado na BD (`InteracaoIA`, dois eventos correlacionados por `requestId`). UC-06: os 4 desfechos confirmados — Confirmar (reatribuição real na BD, auditoria dupla `SugestaoIA/confirmar` + `Processo/atualizar`), Rejeitar, Deixar pendente, Staleness (Exceção E1, `409`, mensagem clara); RN-08 confirmada estruturalmente (sem mecanismo de confirmação em lote em toda a base de código). 1 achado registado, já conhecido (caminho de sucesso da pergunta livre não observável sem credencial real, já coberto por teste automatizado). Zero bugs, zero erros de consola | CTO (Claude) + Fundadora/CEO |
| 3.18 | 2026-07-11 | **Passo 37 (Validação Manual UC-07 + UC-08) concluído e formalmente aprovado pela Fundadora/CEO — sexto e último passo de validação do M6, os 8 Use Cases do MVP têm agora todos registo de validação manual.** Com registo próprio (docs/04-implementation-blueprint/36-validacao-uc-07-uc-08.md), sem Especificação Técnica prévia. Sem credenciais reais da Stripe neste ambiente — efeito do webhook simulado por escrita direta em `SubscricaoPlano` (o webhook em si já coberto por teste automatizado real desde o Passo 22). UC-07: Fluxo Principal 2-4 confirmados ao vivo (planos, checkout disparado corretamente e bloqueado por configuração ausente, estado atualizado após simular o webhook); Alternativo 3a confirmado (trial expirado entra no mesmo `estadoEfetivo: limitada` do UC-08); RN-09 confirmada (dados intactos após "expiração"). UC-08: RN-10 isolada de forma decisiva de RN-11 — com a Empresa `ativa` e `limiteUtilizadores` no limite, `POST /convites` bloqueado (`402`) enquanto leitura e outra ação de criação continuaram a funcionar no mesmo momento, provando que só a ação específica é bloqueada. 3 achados registados: (A, substantivo) UC-07 Fluxo Principal 1 (notificação proativa de fim de trial) nunca implementado, sem scheduler em todo o backend; (B, refinamento de Questão em Aberto já conhecida) Exceção E1 do UC-07 sem tratamento explícito no webhook (Passo 22, Decisão E); (C, menor) aviso a 90% do UC-08 Alternativo 1a só existe para `limiteUsoIA`, apesar de `limiteUtilizadores` ter uso real mensurável. Zero bugs, zero erros de consola | CTO (Claude) + Fundadora/CEO |
| 3.19 | 2026-07-11 | **Passo 38 (Relatório Final de Encerramento do M6) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) formalmente concluído.** Relatório de consolidação (docs/04-implementation-blueprint/37-relatorio-final-m6.md), sem introduzir factos novos além dos já validados nos Passos 32-37. Os dois objetivos da Proposta do M6 cumpridos com evidência real: NFR-17 com cobertura de teste automatizado genuína (Passo 32); os 8 Use Cases do MVP validados manualmente em ambiente real (Passos 34-37). Zero bugs encontrados em todo o M6. 11 achados consolidados e classificados: 2 substantivos (FR-18 referência bidirecional incompleta; ausência de notificação proativa de fim de trial), 5 já conhecidos/reconfirmados, 3 menores, 1 lacuna de atribuição de teste (isolamento multi-tenant). Questões em Aberto herdadas reconfirmadas sem alteração (bloqueador RGPD do Passo 26; PSD-001/PSD-002; inconsistência "9 vs 8 Use Cases" do Use Cases v1.0). **Todos os passos previstos para o M6 (32-38) implementados, validados e aprovados** — próximo Milestone por confirmar com a Fundadora/CEO | CTO (Claude) + Fundadora/CEO |
| 3.20 | 2026-07-11 | **Proposta do Milestone M7 (Deploy em Staging, ADR-007) aprovada pela Fundadora/CEO; Passo 39 (Repositório GitHub + Rate Limiting) parcialmente concluído** — primeiro passo do M7, com Especificação Técnica própria (docs/04-implementation-blueprint/38-especificacao-tecnica-passo-39-github-rate-limiting.md). Âmbito confirmado exclusivo a staging, sem alargar a produto ou produção. Três descobertas prévias registadas (ausência total de infraestrutura; divergência real entre o rate limiting implementado desde o Passo 3 e o decidido no ADR-007 §3.6; sobreposição documental Blueprint/Master Roadmap, só documental). Parte A (repositório GitHub) preparada mas **não executada** — conta/organização de destino deliberadamente por confirmar antes do push, decisão explícita da Fundadora/CEO. Parte B (rate limiting) concluída — novo modelo `TentativaLoginFalhada` (sem `empresaId`, tabela de bootstrap global); `AuthService.login()` reescrito com bloqueio progressivo por IP+conta em 3 camadas, cada uma usando a sua duração como janela de contagem própria; `trust proxy` configurado em `main.ts` (correção real de produção, não só de teste). 9 testes novos, suite completa em 219/219 (210 herdados + 9 novos); validado no browser (registo+login, tentativa falhada, 5ª tentativa a devolver 429, zero erros de consola) | CTO (Claude) + Fundadora/CEO |
| 3.21 | 2026-07-11 | **Passo 39 (Repositório GitHub + Rate Limiting) formalmente concluído — Parte A executada.** A Fundadora/CEO forneceu o destino (`https://github.com/adaalvess/NEXA.git`, privado); `git remote add` + `git push -u origin main` executados com sucesso — primeiro push da história do repositório. Descoberta operacional real: a primeira tentativa bloqueou indefinidamente à espera de autenticação interativa do Git Credential Manager (confirmado por `GIT_TRACE=1`, `401` do GitHub antes da chamada a `git credential-manager get`) — comunicado à Fundadora/CEO em vez de repetir às cegas; após confirmação de autenticação concluída do lado dela, o push repetido teve sucesso imediato. Repositório remoto ativo — pré-requisito estrutural para o Passo 44 (CI/CD via GitHub Actions) | CTO (Claude) + Fundadora/CEO |
| 3.22 | 2026-07-12 | **Passo 40 (Base de Dados Neon em Staging) formalmente concluído e aprovado pela Fundadora/CEO** — segundo passo do M7, com Especificação Técnica própria (docs/04-implementation-blueprint/39-especificacao-tecnica-passo-40-neon-staging.md) e relatório de execução completo (docs/04-implementation-blueprint/40-relatorio-passo-40-neon-staging.md). Desvio de arquitetura detetado e corrigido antes de qualquer configuração: o projeto Neon inicial estava em `aws-us-east-1` (EUA), violando ADR-007 §3.2/NFR-21 — eliminado e recriado em `aws-eu-central-1` (Frankfurt), só depois de confirmação explícita. Schema replicado na íntegra (17/17 migrações — schema, RLS, trigger de imutabilidade); os 4 roles de BD recriados com os privilégios já validados localmente, confirmando empiricamente que o Neon permite `CREATE ROLE ... BYPASSRLS` (risco técnico da especificação, resolvido). Teste de fumo real (não simulado) confirmou registo, login, isolamento multi-tenant e imutabilidade do Registo de Auditoria. Correção de segurança encontrada: `.gitignore` não cobria `.env.staging`, corrigida antes de escrever qualquer segredo em disco. Achado resolvido pela Fundadora/CEO: retenção de PITR do plano Neon Free (6h) abaixo do mínimo de 7 dias do ADR-007 §3.8 — aceite temporariamente só para staging, registado permanentemente no ADR-007 (v1.2, nova Questão em Aberto Q4), upgrade obrigatório antes de produção | CTO (Claude) + Fundadora/CEO |
| 3.23 | 2026-07-14 | **Passo 41 (Backend em Render, Staging) formalmente concluído e aprovado pela Fundadora/CEO** — terceiro passo do M7, com Especificação Técnica própria (docs/04-implementation-blueprint/41-especificacao-tecnica-passo-41-render-backend.md) e relatório de execução completo (docs/04-implementation-blueprint/42-relatorio-passo-41-render-backend.md). Confirmado que não existe CLI oficial da Render — provisionamento feito via API REST, mesma disciplina do Passo 40. Web Service `nexa-api-staging` criado (região `frankfurt`, UE), ligado à Neon de staging. Descoberta técnica real, corrigida antes do fecho: o primeiro deploy falhou no build por lacuna de scaffolding dupla — sem `postinstall`, `npm install` nunca corria `prisma generate` (cliente Prisma ficava no stub por defeito, sem os enums customizados do schema); sem `tsconfig.build.json`, `nest build` compilava também `test/*.e2e-spec.ts`, amplificando o problema em erros fatais. Corrigido com dois padrões idiomáticos (`postinstall: prisma generate`; `tsconfig.build.json` padrão do scaffold NestJS), sem alteração de arquitetura; efeito colateral (`dist/src/main.js` → `dist/main.js`) corrigido no `startCommand` e no `render.yaml`. Teste de fumo real contra o backend em Render (não simulado) confirmou `GET /health`, registo, login e isolamento multi-tenant. `render.yaml` commitado (Infrastructure as Code) | CTO (Claude) + Fundadora/CEO |
