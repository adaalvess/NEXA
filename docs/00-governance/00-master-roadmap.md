# NEXA — Resumo Executivo e Roadmap (Master Roadmap)

| | |
|---|---|
| **Documento** | Resumo Executivo e Roadmap (Master Roadmap) |
| **Fase** | Transversal — documento de acompanhamento permanente do projeto |
| **Versão** | 5.8 |
| **Estado** | ✅ Aprovado |
| **Owner** | Fundadora / CEO |
| **Documentos de referência** | Todos os documentos produzidos até à data (38 documentos) |
| **Natureza** | Vivo — atualizado a cada fase concluída |
| **Última atualização** | 2026-07-20 |

---

## 1. Objetivo

Este documento é o **ponto de situação único e permanente** da construção da NEXA — em que fase estamos, o que já foi decidido, o que falta decidir, e qual é o próximo passo lógico. Não substitui nenhum documento existente; consolida-os numa vista de topo, mantida atualizada à medida que o projeto avança.

---

## 2. Contexto

A NEXA está, à data deste documento, em fase 100% de documentação — nenhuma linha de código foi escrita, por desenho deliberado do processo (Discovery inicial: "não comeces a escrever código"). Foram concluídas 3 fases completas de documentação (Estratégica, Funcional, e a subfase de Princípios de Engenharia dentro da Fase 3), num total de **25 documentos aprovados**.

---

## 3. Conteúdo Estruturado

### 3.1 Estado Atual do Projeto

> **A NEXA concluiu toda a Fase 3 (3a Engineering Principles, 3b ADRs de Tecnologia — 8 aprovados, 3c Coding Standards, 3d AI Principles). As Fases 4 (Planeamento), 5 (Arquitetura Técnica Detalhada) e 6 (UI/UX) foram compactadas num único documento — o Blueprint de Implementação do MVP (decisão D1 desse documento) — que cobre parcialmente os seus Exit Criteria originais (ver 3.2a). Dentro da Fase 7 (Desenvolvimento da Plataforma), **o Milestone M1 (Fundação) do Blueprint está formalmente concluído (2026-07-06)** — Passos 0-7 implementados, validados e aprovados. **O Milestone M2 (Módulos Core — Dashboard, Processos, CRM) está formalmente concluído (2026-07-07)** — Passos 8-14 implementados, validados e aprovados. **O Milestone M3 (Assistente de IA) está formalmente concluído (2026-07-07)** — Passos 15-18 implementados, validados e aprovados; NFR-17 ("ações de IA") coberto na íntegra, fechando os 4 fluxos críticos obrigatórios do projeto. **O Milestone M4 (Comercial e Pagamentos) está formalmente concluído (2026-07-08)** — Passos 19-23 implementados, validados e aprovados; Risco R3 (limites de plano nunca fixados) formalmente resolvido; ciclo completo de UC-07/UC-08 (escolher plano → pagar → subscrição ativa → webhook idempotente → ecrã de subscrição no frontend, sem cálculos paralelos no cliente) fechado de ponta a ponta. **O Milestone M5 (Camada Comercial e Produto) está formalmente concluído (2026-07-08)** — Passos 24-31 implementados, validados e aprovados, os 3 blocos operacionais de ponta a ponta: **Bloco A (EP-07, Landing/Pricing/Registo público)** — Passo 24 (`GET /planos/publico`, `/precos`), Passo 25 (Landing Page, `/`) e Passo 26 (Registo público, `/registar`); percurso "Landing → Registo → Trial" (Business Goals H1.4) operacional de ponta a ponta. Registo público **não disponibilizável a utilizadores reais em produção** até existirem Termos de Serviço/Política de Privacidade e consentimento RGPD (bloqueador formal confirmado pela Fundadora/CEO). **Bloco B (Configurações)** — Passo 27 (`PATCH /utilizadores/me`) e Passo 28 (Ecrã "Configurações"), primeira mutação do projeto sem `PermissaoGuard`, primeira `TabelaDados` com edição em linha. **Bloco C (UC-02, Convite por email)** — Passo 29 (Interface de email + `ConviteUtilizador`), Passo 30 (endpoints de Convite) e Passo 31 (Ecrã de Convite, envio + aceitação), fluxo completo envio → pré-visualização pública → aceitação → login automático. **O Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) está formalmente concluído (2026-07-11)** — Passo 32 (Consolidação NFR-17) concluído e aprovado: suite completa 202/202 estável, mapeamento fluxo→testes→lacunas confirmado por inspeção manual das asserções para os 4 fluxos críticos; duas lacunas honestas registadas (isolamento — Camadas 1/2 nunca testadas isoladamente; RN-10 sem testes por ainda não existir, resolvido pelo próximo passo). **Passo 33 (Enforcement de `limiteUtilizadores`, RN-10) concluído e aprovado** — implementado inteiramente dentro de `fundacao/convite/`, preservando a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`" sem exceção (tensão real encontrada e resolvida com validação explícita); bloqueio verificado tanto no envio do convite (Utilizadores ativos + Convites pendentes) como na aceitação (só Utilizadores ativos), defesa em profundidade; 8 testes novos, suite completa 210/210. UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados para validação manual. **Passo 34 (Validação Manual UC-01 + UC-02) concluído e aprovado** — validação real no browser via o próprio ecrã de registo público; todos os itens confirmados no sistema real, incluindo a Exceção E1 do UC-02 (limite de Utilizadores) ao vivo; 4 achados registados, nenhum bloqueante. **Passo 35 (Validação Manual UC-03 + UC-04) concluído e aprovado** — UC-04 executado primeiro para viabilizar a associação em UC-03; todos os itens confirmados, incluindo a Exceção E1 do UC-03 testada diretamente contra a API; achado substantivo registado — FR-18 (referência bidirecional Processo↔Cliente) parcialmente implementado, recomendado para passo dedicado fora do M6. **Passo 36 (Validação Manual UC-05 + UC-06) concluído e aprovado** — fixtures adicionais (Departamento, Colaboradores, Processos em atraso) permitiram cobrir os 4 desfechos possíveis de uma sugestão de IA (confirmar, rejeitar, deixar pendente, staleness) de forma 100% determinística, sem depender de credencial de IA real; FR-28 e a auditoria dupla do UC-06 confirmadas na BD. **Passo 37 (Validação Manual UC-07 + UC-08) concluído e aprovado — sexto e último passo de validação do M6, os 8 Use Cases do MVP têm agora todos registo de validação manual** — efeito do webhook Stripe simulado por escrita direta em `SubscricaoPlano` (sem credenciais reais neste ambiente); RN-10 isolada de forma decisiva de RN-11 (limite de Utilizadores bloqueado especificamente, enquanto leitura e outra ação de criação continuaram a funcionar no mesmo momento); RN-09 confirmada (dados intactos após trial expirado sem conversão). 3 achados registados, um substantivo (notificação proativa de fim de trial nunca implementada, sem scheduler em todo o backend). **Passo 38 (Relatório Final de Encerramento do M6) concluído e formalmente aprovado — Milestone M6 formalmente concluído.** Zero bugs em todo o M6; 11 achados consolidados e classificados (2 substantivos, 5 já conhecidos, 3 menores, 1 lacuna de atribuição de teste), nenhum bloqueante. **O Milestone M7 (Deploy em Staging, ADR-007) está aprovado e em curso (2026-07-11)** — foco exclusivo num ambiente de staging robusto e reproduzível, sem alargar o âmbito a produto ou produção. Passo 39 (Repositório GitHub + Rate Limiting) formalmente concluído — repositório remoto ativo (`https://github.com/adaalvess/NEXA.git`, privado) e rate limiting corrigido conforme ADR-007 §3.6 (bloqueio progressivo por IP+conta em 3 camadas). Passo 40 (Base de Dados Neon em Staging) formalmente concluído — base de dados operacional em `aws-eu-central-1` (Frankfurt, UE), schema/roles/RLS/trigger replicados e validados por teste de fumo real; exceção temporária de retenção de backups (6h) registada permanentemente no ADR-007 (v1.2, Q4), upgrade obrigatório antes de produção. Passo 41 (Backend em Render, staging) formalmente concluído — `nexa-api-staging` publicado em `frankfurt` (UE), servindo tráfego real contra a Neon, validado por teste de fumo real (registo, login, isolamento multi-tenant); descoberta técnica real corrigida (lacuna de scaffolding — `postinstall`/`tsconfig.build.json` em falta desde o Passo 1, sem impacto de arquitetura). Passo 42 (Frontend em Vercel, staging) formalmente concluído — `nexa-web-staging` publicado em `fra1` (Frankfurt, UE), CORS corrigido no Render para a URL real do Vercel; teste de fumo real ponta a ponta (frontend + backend + BD) confirmou páginas públicas, registo, login e isolamento multi-tenant. Os três componentes de staging (Vercel + Render + Neon) operacionais em conjunto, todos em região UE. **Próximo: Passo 43 (Observabilidade — Sentry + uptime).**

Tecnologia escolhida e documentada nos 8 ADRs (PostgreSQL/Prisma, NestJS, Next.js, sessões server-side, AI Gateway multi-fornecedor, Stripe). Código já existe (scaffolding do monorepo + schema de base de dados real e migrado) — a afirmação anterior desta secção ("nenhum código foi escrito") ficou desatualizada e foi corrigida nesta revisão.

### 3.2 Todas as Fases do Projeto, por Ordem Lógica

| Fase | Nome | Estado |
|---|---|---|
| 1 | Documentação Estratégica | ✅ Concluída |
| 2 | Documentação Funcional | ✅ Concluída |
| 3a | Engineering Principles (normativo, tecnologicamente neutro) | ✅ Concluída |
| 3b | Architecture Decision Records — decisões de tecnologia | ✅ Concluída — 8 ADRs aprovados |
| 3c | Coding Standards | ✅ Concluída |
| 3d | AI Principles (formalização técnica dedicada) | ✅ Concluída |
| 4 | Planeamento (Épicos, Features, Milestones, Sprints, Release Strategy, Risk Register, Technical Debt Register) | 🔶 Parcialmente coberta — Épicos/Milestones/DoD no Blueprint (D1); Sprints, Release Strategy, Risk Register e Technical Debt Register formais ainda não produzidos |
| 5 | Arquitetura Técnica Detalhada (esquema de base de dados real, especificação de API, Design System de componentes) | 🔶 Parcialmente coberta — esquema real já implementado e migrado (Blueprint §3a); superfície de API mínima definida; Design System já implementado e validado (Blueprint M2, Passo 13) — 11 componentes base, tokens de marca completos |
| 6 | UI/UX Design (protótipos, ecrãs) | 🔶 Parcialmente coberta — só inventário de ecrãs por prioridade (Blueprint §5.2); nenhum protótipo real produzido |
| 7 | Desenvolvimento da Plataforma (código) | 🔄 Em curso — M1 (Fundação), M2 (Módulos Core), M3 (Assistente de IA), M4 (Comercial e Pagamentos), M5 (Camada Comercial e Produto) e M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) formalmente concluídos (Passos 0-38); NFR-17 coberto na íntegra; Risco R3 resolvido, RN-11 aplicada de forma uniforme, ciclo completo de UC-07/UC-08 fechado de ponta a ponta; M6 fechado com os 8 Use Cases do MVP validados manualmente em ambiente real, zero bugs encontrados, 11 achados consolidados no Relatório Final de Encerramento (Passo 38); **M7 (Deploy em Staging, ADR-007) formalmente concluído** — Passos 39-45 implementados, validados e aprovados (repositório remoto GitHub ativo; base de dados Neon em staging operacional, região UE `aws-eu-central-1`, exceção temporária de retenção de backups registada no ADR-007 v1.3; backend NestJS em staging no Render, região UE `frankfurt`; frontend Next.js em staging no Vercel, região UE `fra1`; os três componentes validados em conjunto por teste de fumo real ponta a ponta; rastreio de erros Sentry ativo e validado ao vivo em ambos, região UE; pipeline CI/CD real com portão, `autoDeploy` do Render desativado permanentemente, validado nas duas direções com evidência real; primeiro teste real de recuperação de backup do projeto executado com sucesso, Passo 45). Monitorização de disponibilidade UptimeRobot bloqueada por dependência externa, Questão em Aberto Q5 do ADR-007 — pré-requisito de lançamento, não bloqueador do encerramento do M7 |
| 8 | Testes e Garantia de Qualidade (funcional, integração, performance, segurança, aceitação) | ⬜ Por iniciar |
| 9 | Preparação para Produção (infraestrutura, observabilidade, monitorização, backups, continuidade, CI/CD) | ⬜ Por iniciar |
| 10 | Lançamento e Evolução Contínua (piloto → lançamento público → Arcos 2-4 do Product Roadmap) | ⬜ Por iniciar — fase contínua, sem estado final |

*Nota sobre a Fase 10: ao contrário de todas as fases anteriores, esta não tem um "concluído" definitivo — a evolução da NEXA após o lançamento é, por natureza, contínua (Product Roadmap, Arcos 2-4). Este roadmap acompanha-a como marcos sucessivos (piloto, lançamento público, expansão), não como um estado a fechar.*

### 3.2a Critérios de Conclusão por Fase (Exit Criteria)

Cada fase só é considerada concluída quando os seus critérios objetivos estão cumpridos — isto elimina interpretação futura sobre "estamos ou não prontos para a fase seguinte".

| Fase | Critérios de conclusão (Exit Criteria) |
|---|---|
| 1 — Estratégica | Todos os 9 documentos aprovados na versão final; nenhuma Questão em Aberto classificada como bloqueante para a Fase 2 |
| 2 — Funcional | Todos os 11 documentos (incl. Glossário) aprovados; auditoria de consistência realizada sem lacunas de rastreabilidade por resolver |
| 3a — Engineering Principles | 4 documentos aprovados; Product & Security Decisions Register ativo e a funcionar como processo, não apenas como documento criado |
| 3b — ADRs de Tecnologia | ✅ **Cumprido** — 8 ADRs aprovados (001-008), cada um com alternativas consideradas e justificação explícita; nenhuma decisão de stack em aberto que impeça codificar a Fundação |
| 3c — Coding Standards | ✅ **Cumprido** — documento aprovado e coerente com os ADRs de 3b |
| 3d — AI Principles | ✅ **Cumprido** — documento aprovado, coerente com FR-26/27 e Security & Access Principles 3.6 |
| 4 — Planeamento | 🔶 **Parcial** — Épicos e Milestones mapeados no Blueprint, com Definition of Done por Milestone; Sprints, Release Strategy, Risk Register e Technical Debt Register formais **ainda não produzidos**. Decisão deliberada (Blueprint D1) de compactar em vez de produzir os 3 documentos completos — o desenvolvimento avançou sobre esta base parcial, não bloqueou por causa dela |
| 5 — Arquitetura Técnica Detalhada | 🔶 **Parcial** — esquema de base de dados real aprovado **e já implementado/migrado** (Blueprint §3a); especificação de API mínima definida (Blueprint §4), mas não confirmada como cobrindo 1:1 todos os FR-XX; **Design System já implementado e validado** (Blueprint M2, Passo 13) |
| 6 — UI/UX | 🔶 **Parcial** — inventário de ecrãs por prioridade definido (Blueprint §5.2); nenhum protótipo visual real produzido ainda |
| 7 — Desenvolvimento | 🔄 **Em curso** — M1 (Fundação) do Blueprint **formalmente concluído** (Passos 0-7: scaffolding, schema Prisma + migração, Autenticação, Camada 1/RLS, RBAC granular, Registo de Auditoria, Partilha/Convidado). **M2 (Módulos Core) formalmente concluído** (Passos 8-14): Passo 8 (Departamento), Passo 9 (Processos/Tarefas), Passo 10 (CRM), Passo 11 (Notification Dispatcher), Passo 12 (Dashboard, terceira confirmação da Decisão B do M2 — zero alterações ao `AuthorizationService`), Passo 13 (Design System — 11 componentes base) e Passo 14 (Ecrãs — Login, Dashboard, Processos, CRM, implementado em 3 sub-entregas validadas sequencialmente). Definition of Done do M2 (Blueprint §2.2) cumprido integralmente. **M3 (Assistente de IA) formalmente concluído** (Passos 15-18): Passo 15 (AI Gateway) implementa integralmente o ADR-005 (adaptador Anthropic, timeout/circuit breaker/quota); Passo 16 (`POST /ia/perguntar`, UC-05) primeiro endpoint de produto do M3, 4ª confirmação da Decisão B do M2 (`AuthorizationService.construirFiltroWhere`), RN-07 garantida estruturalmente; Passo 17 (sugestões de ação, UC-06, RN-08) — `POST /ia/sugestoes`/`.../confirmar`/`.../rejeitar`, primeiro módulo de negócio a consumir o serviço de outro (`ProcessosService`); Passo 18 (ecrã do Assistente de IA, frontend) — `GET /ia/sugestoes`, RN-08 refletida na interface (`Modal` de segunda confirmação). **NFR-17 ("ações de IA") coberto na íntegra** — os 4 fluxos críticos obrigatórios do projeto têm agora todos cobertura automatizada. **M4 (Comercial e Pagamentos) formalmente concluído** (Passos 19-23) — Passo 19 (`SubscricaoPlano` real, `GET /planos`) primeiro passo do M4, primeiro módulo `comercial` construído, `SubscricaoPlano` ativado com trial automático (Risco R3 do Master Roadmap formalmente resolvido); Passo 20 (enforcement uniforme de RN-11) — `SubscricaoGuard`/`SubscricaoExceptionFilter` globais garantem resposta idêntica (`402`, `code: SUBSCRICAO_LIMITADA`) em toda a aplicação, independentemente do módulo; Passo 21 (Stripe Checkout) — `POST /subscricao/checkout`, primeiro contacto real com a Stripe, `STRIPE_CLIENT`/`FakeStripeClient` para testabilidade sem credenciais reais; Passo 22 (Webhooks Stripe) — `POST /webhooks/stripe`, fecha o ciclo completo de UC-07, idempotência real via `WebhookStripeProcessado`, testes com o SDK real da Stripe; Passo 23 (Ecrã de Subscrição, frontend) — último passo do M4, `GET /subscricao` (extensão aditiva, `usoIAPercentagem`/`avisoLimiteIAProximo` calculados no backend, exigência explícita da Fundadora/CEO), `IaModule` passa a exportar `QuotaService` (primeira vez que `comercial` consome outro módulo de negócio), ecrã `/subscricao` validado por inspeção visual real no browser. **M5 (Camada Comercial e Produto) formalmente concluído** (Passos 24-31) — âmbito em 3 blocos (A: EP-07 Landing/Pricing/Registo público; B: Configurações completa; C: UC-02 Convite por email); Passo 24 (`GET /planos/publico`, página `/precos`) concluído e formalmente aprovado — primeira rota pública desde o webhook Stripe, página `/precos` como Server Component, descoberta técnica real corrigida (Next.js pré-renderaria a página estaticamente, congelando os dados — corrigido com `dynamic = 'force-dynamic'`); Passo 25 (Landing Page pública, `/`) concluído e formalmente aprovado — toda a copy citação direta de documentos já aprovados (Product Vision, Brand Book, PRD), sem testemunhos/Persona/concorrentes, sem `dynamic` explícito (já dinâmica por ler `cookies()`), descoberta operacional real sem impacto no código (cache do servidor de desenvolvimento, mesma classe do Passo 13); Passo 26 (Ecrã de Registo público, `/registar`) concluído e formalmente aprovado — `POST /auth/registar` encadeado com `POST /auth/login`, sem intervenção manual, CTAs de `/`/`/precos` atualizados, sem consentimento RGPD nesta fase (bloqueador formal explícito antes de qualquer registo real em produção, confirmado pela Fundadora/CEO), descoberta técnica real corrigida (`lib/api.ts` não extraía `message` do JSON de erro). **Bloco A do M5 formalmente concluído** — percurso "Landing → Registo → Trial" operacional de ponta a ponta. Passo 27 (`PATCH /utilizadores/me`) concluído e formalmente aprovado, primeira mutação do projeto sem `PermissaoGuard` (alvo sempre `ctx.utilizadorId`, nenhuma escalada de privilégio possível); alterar a palavra-passe invalida todas as outras sessões ativas numa transação real; descoberta técnica real resolvida antes de implementar (`TenantPrismaService` incompatível com transação multi-operação genuína, resolvido com `PrismaService` bruto, mesmo padrão de `AuthService.registar`). Passo 28 (Ecrã "Configurações") concluído e formalmente aprovado — página única `/configuracoes` (Perfil, Utilizadores/Permissões, Departamentos), 3 componentes independentes, primeira `TabelaDados` com edição em linha, item de navegação visível a todos os papéis. **Bloco B do M5 formalmente concluído.** Passo 29 (Interface de email + `ConviteUtilizador`) concluído e formalmente aprovado — primeiro passo do Bloco C (UC-02), backend-fundação puro sem endpoint de produto (mesmo padrão sem-HTTP do AI Gateway, Passo 15); `EmailAdapterInterface`/`EMAIL_ADAPTER` (interface completa, `ResendAdapter` real + `FakeEmailAdapter` de teste); novo modelo `ConviteUtilizador` tenant-scoped com RLS, `EstadoConvite` sem valor `expirado` (derivado em leitura, mesmo padrão de `SubscricaoPlano`), `token` criptográfico, expiração de 7 dias; confirmado que o bloqueador RGPD do Passo 26 não se estende ao convite por email (bases legais distintas); duas descobertas técnicas reais, ambas precedentes já documentados (`EPERM` do Prisma no Windows; migrações não aplicadas a `nexa_test`, mesma descoberta "D9" do Passo 7). Passo 30 (`POST /convites`, `GET /convites/:token`, `POST /convites/:token/aceitar`) concluído e formalmente aprovado — segundo passo do Bloco C, 3 Decisões a Validar aprovadas sem alteração (A: `GET /convites/:token` público para pré-visualização; B: `limiteUtilizadores` deliberadamente não implementado, Questão em Aberto explícita; C: unicidade de email global na aceitação); `PRIVILEGIO` (Passo 5) extraída para ficheiro partilhado, reutilizada sem duplicação; regras de autoridade CV-01 a CV-06; ordem "enviar antes de persistir", com a observação da Fundadora/CEO sobre o cenário residual (email entregue, persistência falhada) respondida formalmente na aprovação — risco real e inerente à ausência de atomicidade entre dois sistemas externos, mitigado com registo estruturado e erro explícito, nunca escondido; sem auto-login no endpoint de aceitação. Passo 31 (Ecrã de Convite — envio + aceitação) concluído e formalmente aprovado — último passo do Bloco C, com 2 Decisões a Validar aprovadas sem alteração (A: formulário de envio dentro de Configurações → Utilizadores e Permissões, local já fixado pela Information Architecture; B: sem listagem de convites pendentes, registado como melhoria futura); `PAPEIS_ATRIBUIVEIS` extraída para o frontend; seletor de Departamento oculto para Gestor (decorre de CV-03); novo `/convites/:token` público com contexto sempre visível antes da palavra-passe, aceitação encadeia login no frontend (mesmo padrão do Passo 26); validado por inspeção visual real no browser nos papéis relevantes. **Bloco C do M5 formalmente concluído — e, com ele, o Milestone M5 (Camada Comercial e Produto) formalmente concluído**, os 3 blocos (A, B, C) operacionais de ponta a ponta. **M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) formalmente concluído** (2026-07-11) — Passo 32 (Consolidação NFR-17) concluído e formalmente aprovado: suite completa 202/202 estável em 2 execuções consecutivas; mapeamento fluxo→testes→lacunas confirmado por inspeção manual real das asserções, com raciocínio explícito sobre o que aconteceria a cada teste perante uma regressão real; duas lacunas honestas registadas (isolamento multi-tenant — Camadas 1/2 nunca testadas isoladamente, defesa em profundidade genuína sem atribuição por camada; RN-10 sem testes por ainda não existir como funcionalidade), nenhuma corrigida neste passo por ser um passo de verificação, não de implementação. **Passo 33 (Enforcement de `limiteUtilizadores`, RN-10) formalmente concluído** — implementado inteiramente dentro de `fundacao/convite/` (Decisão A, preserva a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`" sem exceção); contagem em `POST /convites` = ativos + pendentes, em `POST /convites/:token/aceitar` só ativos (Decisão B); verificação nos dois pontos (Decisão C); 8 testes novos, suite completa 210/210; UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados. **Passo 34 (Validação Manual UC-01 + UC-02) formalmente concluído** — validação real no browser, todos os itens de ambos os Use Cases confirmados no sistema real; 4 achados registados, nenhum bloqueante (campo "setor" nunca recolhido no registo; envio real de email não observável; Fluxo Alternativo 2a do UC-02 sem interface; Exceção E2 do UC-02 sem implementação); zero bugs, zero erros de consola. **Passo 35 (Validação Manual UC-03 + UC-04) formalmente concluído** — UC-04 executado primeiro para viabilizar a associação em UC-03; todos os itens de ambos os Use Cases confirmados no sistema real; 2 achados registados, um substantivo (FR-18, referência bidirecional Processo↔Cliente parcialmente implementada — recomendado corrigir num passo dedicado fora do M6). **Passo 36 (Validação Manual UC-05 + UC-06) formalmente concluído** — fixtures adicionais (Departamento, 2 Colaboradores via convite direto na BD, 4 Processos em atraso) permitiram cobrir os 4 desfechos possíveis de uma sugestão de IA (confirmar com reatribuição real na BD, rejeitar, deixar pendente, staleness com `409`) de forma 100% determinística, sem depender de credencial de IA real; FR-28 confirmado na BD (entidade `InteracaoIA`, eventos correlacionados por `requestId`); auditoria dupla do UC-06 confirmada (`SugestaoIA/confirmar` + `Processo/atualizar`, sempre distintos); RN-08 confirmada estruturalmente (sem mecanismo de confirmação em lote em toda a base de código); 1 achado registado, já conhecido (caminho de sucesso da pergunta livre não observável sem credencial real). **Passo 37 (Validação Manual UC-07 + UC-08) formalmente concluído** — sexto e último passo de validação do M6; efeito do webhook Stripe simulado por escrita direta em `SubscricaoPlano` (sem credenciais reais neste ambiente); RN-10 isolada de forma decisiva de RN-11 (limite de Utilizadores bloqueado especificamente, enquanto leitura e outra ação de criação continuaram a funcionar no mesmo momento); RN-09 confirmada (dados intactos após trial expirado sem conversão); 3 achados registados, um substantivo (notificação proativa de fim de trial nunca implementada, sem scheduler em todo o backend). **Passo 38 (Relatório Final de Encerramento do M6) formalmente concluído — Milestone M6 formalmente concluído.** Zero bugs em todo o M6; 11 achados consolidados e classificados (2 substantivos, 5 já conhecidos, 3 menores, 1 lacuna de atribuição de teste), nenhum bloqueante; Questões em Aberto herdadas reconfirmadas sem alteração. **Todos os passos previstos para o M6 (32-38) implementados, validados e aprovados.** **Proposta do Milestone M7 (Deploy em Staging, ADR-007) aprovada pela Fundadora/CEO (2026-07-11)** — âmbito confirmado exclusivo a staging, sem alargar a produto/produção; três descobertas prévias registadas (ausência total de infraestrutura; divergência real entre o rate limiting implementado desde o Passo 3 e o decidido no ADR-007 §3.6 — a corrigir dentro do M7; sobreposição documental Blueprint/Master Roadmap, só documental). **Passo 39 (Repositório GitHub + Rate Limiting) formalmente concluído** — Parte B (rate limiting) concluída primeiro, novo modelo `TentativaLoginFalhada`, bloqueio progressivo por IP+conta em 3 camadas, `trust proxy` configurado em `main.ts`, 9 testes novos, suite completa 219/219, validado no browser; Parte A (GitHub) executada num segundo momento, depois de a Fundadora/CEO fornecer o destino (`https://github.com/adaalvess/NEXA.git`) — primeiro push da história do repositório, com uma descoberta operacional real (bloqueio inicial à espera de autenticação interativa do Git Credential Manager, resolvido depois de confirmação da Fundadora/CEO). **Passo 40 (Base de Dados Neon em Staging) formalmente concluído** — desvio de arquitetura detetado e corrigido (projeto inicial em aws-us-east-1, recriado em aws-eu-central-1); schema replicado na íntegra (17/17 migrações); os 4 roles de BD recriados e validados (confirmado empiricamente que o Neon permite CREATE ROLE ... BYPASSRLS); teste de fumo real confirmou registo, login, isolamento multi-tenant e imutabilidade do Registo de Auditoria; achado de retenção de backups (6h vs 7 dias exigidos pelo ADR-007) resolvido pela Fundadora/CEO como exceção temporária de staging, registada permanentemente no ADR-007 v1.2 (Q4), upgrade obrigatório antes de produção. **Passo 41 (Backend em Render, staging) formalmente concluído** — confirmado que não existe CLI oficial da Render, provisionamento via API REST; Web Service `nexa-api-staging` criado em `frankfurt` (UE), ligado à Neon de staging; descoberta técnica real corrigida antes do fecho (primeiro deploy falhou no build por lacuna de scaffolding dupla — sem `postinstall`, `prisma generate` nunca corria; sem `tsconfig.build.json`, `nest build` compilava também `test/*.e2e-spec.ts` — corrigido com os dois padrões idiomáticos correspondentes, `dist/src/main.js` → `dist/main.js` corrigido no `startCommand`/`render.yaml`); teste de fumo real contra o backend em Render confirmou `GET /health`, registo, login e isolamento multi-tenant; `render.yaml` commitado como Infrastructure as Code. **Passo 42 (Frontend em Vercel, staging) formalmente concluído** — diferente do Render, existe CLI oficial completo da Vercel, provisionamento inteiramente por automação; projeto `nexa-web-staging` criado em `fra1` (Frankfurt, UE), confirmado por evidência real (cabeçalho `X-Vercel-Id`); CORS corrigido — `WEB_APP_URL` provisório no Render atualizado para a URL real do Vercel, com novo deploy disparado para ter efeito; teste de fumo real ponta a ponta (frontend + backend + BD, todos em staging real) confirmou páginas públicas, registo, login, criação de Processo e isolamento multi-tenant via pedidos com o `Origin` real do frontend. Os três componentes de staging (Vercel + Render + Neon) operacionais em conjunto, todos em região UE. **Passo 43 (Observabilidade — Sentry + Uptime) formalmente concluído** — Sentry: 2 projetos criados em região UE (`de.sentry.io`); backend com `@sentry/nestjs` (descoberta técnica: integração oficial NestJS, `SentryGlobalFilter` como primeiro `APP_FILTER`, confirmado sem regressão em 219/219 testes e2e); frontend com `@sentry/nextjs` manual (`next.config.js`, `sentry.*.config.ts`, `instrumentation.ts`, `global-error.tsx`). Validação real via rotas de diagnóstico temporárias, deployadas/disparadas/confirmadas nos dois dashboards (`firstEvent` com timestamp exato, tag `environment: staging`) e revertidas de imediato, regressão completa confirmada depois. UptimeRobot bloqueado por `access_denied` na conta da Fundadora/CEO (leitura funciona, escrita não — indício de verificação de conta pendente do fornecedor) — decisão explícita dela de encerrar o passo sem esta dependência externa, registada permanentemente no ADR-007 (v1.3, nova Questão em Aberto Q5), obrigatória antes de qualquer lançamento com empresas piloto/produção. **Passo 44 (CI/CD, GitHub Actions) formalmente concluído** — pipeline real com portão (não só visibilidade): `.github/workflows/ci-cd.yml` com 3 jobs (`test-backend` — Postgres efémero do runner, 219 testes e2e; `build-frontend`; `deploy` — só corre se os dois anteriores passarem, confirma `live` real no Render por polling antes de disparar o Vercel); `autoDeploy` do Render desativado permanentemente; GitHub Secrets `RENDER_API_KEY`/`VERCEL_TOKEN` configurados (mesmos valores já usados manualmente, encriptados via `libsodium` antes do envio). Descoberta técnica real corrigida durante a validação: `DATABASE_ADMIN_URL` em falta no `.env.test` efémero do workflow, causando falha genuína em `auth.e2e-spec.ts` — diagnosticado com a Fundadora/CEO a ler o erro na interface do GitHub, sem pedir permissões adicionais desnecessárias. Validação real das duas direções do portão: commit correto → deploy real confirmado em ambos os serviços, verificado independentemente do CI; commit com teste deliberadamente falhado → job `deploy` `skipped` (nunca chega a correr), deploy live confirmado inalterado. **Passo 45 (Validação Técnica Final) formalmente concluído — Milestone M7 (Deploy em Staging) formalmente encerrado.** Primeiro teste real de recuperação de backup de todo o projeto — branch temporário da Neon a partir de um ponto no tempo anterior a uma eliminação real de dados, nunca o `main` restaurado diretamente (risco zero); Empresa de teste recuperada com sucesso, timestamp confirmado pelo próprio servidor Neon (nunca o relógio local). Descoberta técnica real corrigida durante o próprio teste: flag `--timestamp` do `neonctl` não existe, ignorada silenciosamente nas duas primeiras tentativas (falso negativo); corrigido com a flag correta `--parent`. Checklist de encerramento do M7 revisto com evidência concreta obtida na própria sessão — os 7 itens (Passos 39-45) confirmados. As duas pendências herdadas (Q4 — upgrade do plano Neon; Q5 — UptimeRobot bloqueado) mantidas explicitamente como pré-requisitos de lançamento em produção/piloto, nunca bloqueadores do encerramento do M7 — decisão formal da Fundadora/CEO, registada permanentemente no ADR-007 (v1.3). **Todos os passos previstos para o M7 (39-45) implementados, validados e aprovados.** Exit criteria da Fase 7 como um todo (todos os FR/US/UC do MVP implementados) continua por cumprir — M1 a M7 são só os primeiros de vários milestones do Blueprint dentro desta fase; próximo Milestone por confirmar com a Fundadora/CEO |
| 8 — Testes e QA | Plano de testes executado (funcional, integração, performance, segurança, aceitação); zero defeitos de severidade crítica em aberto |
| 9 — Preparação para Produção | Infraestrutura, observabilidade, monitorização, backups e CI/CD operacionais; disponibilidade validada contra NFR-01 (99,9%) em ambiente real |
| 10 — Lançamento e Evolução | *(sem exit criteria — fase contínua por desenho; ver nota acima)*. O "sucesso" do piloto é medido pelos critérios já definidos no Success Metrics, não por um critério de conclusão de fase |

*Nota: as Fases 3b-3d correspondem ao restante da "Fase 3 — Decisões de Engenharia" já indexada desde o início do projeto; a Fase 3a (Engineering Principles) foi tratada como subfase autónoma a pedido explícito, com o seu próprio processo de governação.*

### 3.3 Inventário Completo de Documentos

**Fase 1 — Documentação Estratégica** (`/docs/01-strategy/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| 1 | Vision Document | v1.1 | ✅ Aprovado | — (fundacional) |
| 2 | Product Vision | v1.1 | ✅ Aprovado | Vision Document |
| 2a | Competitive Analysis | v1.1 | ✅ Aprovado | Product Vision |
| 3 | Mission & Values | v1.1 | ✅ Aprovado | Vision Document |
| 4 | Business Goals | v1.0 | ✅ Aprovado | Mission & Values |
| 5 | Success Metrics (KPIs) | v1.0 | ✅ Aprovado | Business Goals |
| 6 | Product Roadmap | v1.1 | ✅ Aprovado | Product Vision, Business Goals |
| 7 | Brand Book | v1.3 | ✅ Aprovado | Vision Document |
| 8 | Product Vision Canvas | v1.0 | ✅ Aprovado | Todos os anteriores (síntese) |

**Fase 2 — Documentação Funcional** (`/docs/02-product/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| 0 | Glossário Oficial da Plataforma | v1.6 (vivo) | ✅ Aprovado | Transversal |
| 1 | PRD | v1.0 | ✅ Aprovado | Toda a Fase 1 |
| 2 | User Personas | v1.1 | ✅ Aprovado | PRD |
| 3 | User Journey Maps | v1.1 | ✅ Aprovado | User Personas |
| 4 | Information Architecture | v1.4 | ✅ Aprovado | PRD, User Personas |
| 5 | Data Model Conceptual | v1.1 | ✅ Aprovado | Information Architecture |
| 6 | Functional Requirements | v1.1 | ✅ Aprovado | PRD, Data Model Conceptual |
| 7 | Non-Functional Requirements | v1.0 | ✅ Aprovado | PRD, Success Metrics |
| 8 | User Stories | v1.0 | ✅ Aprovado | Functional Requirements, Personas |
| 9 | Use Cases | v1.0 | ✅ Aprovado | User Stories |
| 10 | Functional Specifications | v1.1 | ✅ Aprovado | Use Cases, Data Model Conceptual |

**Fase 3a — Engineering Principles** (`/docs/03-engineering/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| 1 | System Design Principles | v1.3 | ✅ Aprovado | Functional Specifications, NFR |
| 2 | Data & Consistency Rules | v1.1 | ✅ Aprovado | System Design Principles |
| 3 | Event & Notification Architecture Rules | v1.1 | ✅ Aprovado | System Design Principles, Data & Consistency Rules |
| 4 | Security & Access Principles | v1.1 | ✅ Aprovado | Todos os anteriores desta subfase |

**Fase 3b — Architecture Decision Records** (`/docs/03-engineering/adrs/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| ADR-001 | Multi-Tenancy e Isolamento de Dados | v1.1 | ✅ Aprovado | System Design Principles |
| ADR-002 | Stack Backend | v1.1 | ✅ Aprovado | ADR-001 |
| ADR-003 | Base de Dados e ORM | v1.1 | ✅ Aprovado | ADR-001, ADR-002 |
| ADR-004 | Autenticação, Sessão e Autorização (RBAC) | v1.0 | ✅ Aprovado | ADR-001, ADR-002, ADR-003 |
| ADR-005 | Camada de Abstração de IA Multi-Fornecedor | v1.2 | ✅ Aprovado | Todos os anteriores (auditado — ver Architecture Review Log, AR-001/AR-002) |
| ADR-006 | Frontend Stack e UI | v1.0 | ✅ Aprovado | ADR-002 |
| ADR-007 | Infraestrutura | v1.1 | ✅ Aprovado | ADR-003, ADR-004, ADR-005 |
| ADR-008 | Pagamentos e Faturação | v1.0 | ✅ Aprovado | ADR-007 |

**Fase 3c/3d** (`/docs/03-engineering/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| 05 | Coding Standards | v1.0 | ✅ Aprovado (vivo) | Todos os ADRs (001-008) |
| 06 | AI Principles | v1.0 | ✅ Aprovado (vivo) | ADR-005, Security & Access Principles |

**Fases 4-6 (compactadas)** (`/docs/04-implementation-blueprint/`)

| # | Documento | Versão | Estado | Depende de |
|---|---|---|---|---|
| 1 | Blueprint de Implementação do MVP | v1.2 | ✅ Aprovado (vivo — atualizado à medida que o M1 avança) | Todos os documentos aprovados (Fases 1-3) |

**Governação Transversal** (`/docs/00-governance/`)

| # | Documento | Versão | Estado | Natureza |
|---|---|---|---|---|
| 1 | Product & Security Decisions Register | v1.3 | ✅ Aprovado (vivo) | 4 entradas pendentes: PSD-001 (RGPD hard-delete), PSD-002 (residência de dados de IA), PSD-003 (granularidade de auditoria de IA), PSD-004 (estratégia fiscal) |
| 2 | Architecture Review Log | v1.0 | ✅ Aprovado (vivo) | Registo de auditorias já realizadas (AR-001, AR-002 ao ADR-005) |

**Total: 38 documentos aprovados** (correção face à contagem anterior de 25/41 — ver Histórico de Alterações desta revisão).

### 3.4 O Que Ainda Falta Documentar

Com a Fase 3 completa e as Fases 4-6 compactadas no Blueprint (D1 desse documento), o que falta não é mais "documentação de fase" no sentido original — é o detalhe que a compactação deliberadamente deixou por trás, mais as fases ainda genuinamente por iniciar:

| Área | Documentos/artefactos em falta | Prioridade |
|---|---|---|
| Fase 4 — Planeamento (detalhe não coberto pelo Blueprint) | Sprints, Release Strategy, Risk Register, Technical Debt Register formais | Baixa-Média — decisão deliberada (Blueprint D1) de avançar sem estes; retomar se a complexidade do M2+ o justificar |
| Fase 5 — Arquitetura Técnica Detalhada (detalhe não coberto) | Confirmação formal de que a superfície de API cobre 1:1 todos os FR-XX; especificação visual detalhada do Design System (além do inventário de componentes) | Média |
| Fase 6 — UI/UX | Protótipos visuais reais dos ecrãs (Blueprint §5.2 só lista prioridade, não desenha nada) | Média-Alta — antes do M2 (Dashboard/Processos/CRM) chegar a UI |
| Fase 8 — Testes e QA | Plano de testes (funcional, integração, performance, segurança, aceitação) | A detalhar apenas quando a Fase 7 estiver mais avançada |
| Fase 9 — Preparação para Produção | Plano de infraestrutura, observabilidade, monitorização, backups, continuidade, CI/CD | A detalhar apenas quando a Fase 5/7 estiverem avançadas |
| Fase 10 — Lançamento e Evolução | Plano de lançamento faseado (piloto → público), já parcialmente coberto pelo Product Roadmap (Horizontes 1-3) | A detalhar perto do fim da Fase 9 |
| Logótipo definitivo | Execução gráfica final (Brand Book, Q1) | Média |

### 3.5 Objetivo de Cada Fase / Grupo de Documentos

| Fase | Responde a... |
|---|---|
| Fase 1 | Porque a NEXA existe, para quem, e que ambição tem |
| Fase 2 | O que a plataforma faz, para quem, com que dados e regras |
| Fase 3a (concluída) | Que disciplina estrutural, de dados, eventos e segurança qualquer implementação tem de cumprir, independentemente da tecnologia |
| Fase 3b-3d | Que tecnologia concreta implementa essa disciplina |
| Fase 4 | Em que ordem e em que ciclos o desenvolvimento acontece |
| Fase 5 | O desenho técnico exato (esquema, API, componentes) pronto a codificar |
| Fase 6 | O aspeto e a experiência exata de cada ecrã |
| Fase 7 | O código da plataforma |
| Fase 8 | Se o que foi construído cumpre, na prática, tudo o que foi especificado — funcional, integração, performance, segurança, aceitação |
| Fase 9 | Se a plataforma está pronta para correr em produção de forma segura, observável e recuperável |
| Fase 10 | Como a NEXA entra em operação real e evolui depois — piloto, lançamento, e os Arcos 2-4 já definidos no Product Roadmap |

### 3.6 Percentagem Aproximada de Progresso

> **Progresso estimado: ~84% (arredondado a ~85% ± 5%) do caminho de documentação até ao início do desenvolvimento (Fases 1 a 6).**

Este valor é uma **estimativa fundamentada, não uma medição exata**. A partir da v1.3 deste documento, deixa de ser um julgamento holístico por fase e passa a resultar de uma metodologia explícita e auditável, discriminada por critério — ver 3.6a. A percentagem de cobertura atribuída a cada uma das Fases 4-6 é uma **proposta de engenharia**, não uma decisão fechada — segue o mesmo espírito de "proposta não vinculativa" já usado no Product & Security Decisions Register, sujeita a confirmação ou ajuste pela Fundadora/CEO.

| Componente | Peso estimado | Estado (ver cálculo em 3.6a) | Contributo |
|---|---|---|---|
| Fase 1 — Estratégica | 15% | 100% | 15% |
| Fase 2 — Funcional | 30% | 100% | 30% |
| Fase 3a — Engineering Principles | 10% | 100% | 10% |
| Fase 3b-3d — ADRs, Coding Standards, AI Principles | 10% | 100% | 10% |
| Fase 4 — Planeamento | 15% | 40% | 6% |
| Fase 5 — Arquitetura Técnica Detalhada | 15% | 86% | 12,9% |
| Fase 6 — UI/UX | 5% | 10% | 0,5% |
| **Total** | **100%** | | **84,4% ≈ ~85% ± 5%** |

*Justificação dos pesos: inalterada face à versão anterior (Fase 2 continua a receber o maior peso pelo volume documental). A alteração nesta revisão é de **método de cálculo do estado** (de julgamento holístico para discriminação por critério, 3.6a), não de peso relativo entre fases.*

**Nota de âmbito — porque as Fases 7 a 10 ficam fora desta percentagem:** este cálculo mede especificamente o caminho de documentação até ao início do desenvolvimento (Fases 1-6), um objetivo finito. As Fases 7 (Desenvolvimento), 8 (Testes), 9 (Preparação para Produção) e, sobretudo, 10 (Lançamento e Evolução Contínua) não têm um "100%" matematicamente coerente — a Fase 10, em particular, nunca "termina" enquanto a NEXA existir como empresa. Incluí-las nesta percentagem tornaria o número enganador. São acompanhadas por marcos e estado (3.2, 3.7), não por percentagem de conclusão.

### 3.6a Metodologia e Fundamentação da Estimativa de Progresso

*Adicionado a pedido explícito da Fundadora/CEO, para garantir consistência metodológica e auditabilidade futura — o estado de cada fase em 3.6 deixa de ser afirmado e passa a ser calculado, de forma reproduzível por qualquer pessoa que releia este documento e os artefactos que cita.*

**Os 5 critérios usados, e o que cada um mede:**

| Critério | O que mede | Como se verifica |
|---|---|---|
| **Documentação** | Existe um documento aprovado que especifica o quê e o porquê desta fase | O documento existe no repositório e o seu campo "Estado" diz "✅ Aprovado" |
| **Arquitetura** | Foram tomadas e justificadas decisões de arquitetura/tecnologia que esta fase exige | Existe um ADR ou decisão formal equivalente, com alternativas consideradas e justificação explícita |
| **Infraestrutura** | O ambiente técnico necessário para suportar o trabalho desta fase está preparado | Ferramenta/serviço instalado, configurado e testado (não apenas escolhido em documento) |
| **Implementação** | Existe código ou artefacto técnico real, não apenas especificação | Ficheiro de código/schema/configuração existe no repositório |
| **Entregáveis concluídos** | O resultado tangível desta fase foi verificado a funcionar, distinto de "código escrito" | Teste executado, migração aplicada, endpoint a responder, ou equivalente verificável |

Nem todos os critérios se aplicam a todas as fases — uma fase puramente documental (ex: Fase 1) não tem "Implementação" própria, e isso não penaliza a fase; o critério é simplesmente excluído do cálculo dessa fase (não conta como 0%). Escala de pontuação por critério aplicável: **Cumprido = 100%, Parcial = 50%, Não cumprido = 0%**. O estado da fase é a média dos critérios aplicáveis.

**Fase 1 — Estratégica** (critérios aplicáveis: Documentação, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Cumprido (100%) | 9/9 documentos aprovados (`/docs/01-strategy/`) |
| Entregáveis concluídos | Cumprido (100%) | Vision, Mission & Values, Product Roadmap e Brand Book publicados e já usados como base de decisões nas fases seguintes |

→ Média: **100%**

**Fase 2 — Funcional** (critérios aplicáveis: Documentação, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Cumprido (100%) | 11/11 documentos aprovados (`/docs/02-product/`) |
| Entregáveis concluídos | Cumprido (100%) | Auditoria de consistência Fase 1/2 realizada e fechada (Data Model Conceptual, D5) |

→ Média: **100%**

**Fase 3a — Engineering Principles** (critérios aplicáveis: Documentação, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Cumprido (100%) | 4/4 documentos aprovados |
| Entregáveis concluídos | Cumprido (100%) | Product & Security Decisions Register ativo, com 4 entradas reais registadas (PSD-001 a PSD-004) — "a funcionar como processo", não apenas criado |

→ Média: **100%**

**Fase 3b-3d — ADRs, Coding Standards, AI Principles** (critérios aplicáveis: Documentação, Arquitetura, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Cumprido (100%) | 8 ADRs + Coding Standards + AI Principles, todos "✅ Aprovado" |
| Arquitetura | Cumprido (100%) | Cada um dos 8 ADRs tem alternativas consideradas, decisão e justificação explícita (verificado por leitura direta); 2 rondas de auditoria adversarial/independente no ADR-005 (Architecture Review Log, AR-001/AR-002) |
| Entregáveis concluídos | Cumprido (100%) | Nenhuma decisão de stack em aberto que bloqueie a Fundação (riscos R1 e R6 de 3.8 resolvidos) |

→ Média: **100%**

**Fase 4 — Planeamento** (critérios aplicáveis: Documentação, Entregáveis concluídos — ambos medem, nesta fase, o mesmo conjunto de artefactos de planeamento, já que o entregável de uma fase de planeamento é o próprio documento)
| Item do Exit Criteria (3.2a) | Estado | Evidência |
|---|---|---|
| Épicos mapeados a partir do Functional Specifications | Cumprido (100%) | Blueprint §2.1 (EP-01 a EP-07, coluna "Deriva de" liga a FR-XX) |
| Milestones definidos | Cumprido (100%) | Blueprint §2.2 (M1-M7) |
| Definition of Done por Milestone | Cumprido (100%) | Blueprint §2.2, coluna própria |
| Sprints definidos | Não cumprido (0%) | Não existe — substituído na prática pelo faseamento em "Passos" dentro de cada Milestone (Blueprint §3, CLAUDE.md §3), que cobre a mesma necessidade operacional de forma mais leve, mas não é formalmente um documento de Sprints |
| Release Strategy aprovada | Não cumprido (0%) | Não existe como documento dedicado |
| Risk Register aprovado | Parcial (50%) | Existe uma lista de riscos ativa e mantida (este documento, §3.8), mas não como Risk Register formal e dedicado da Fase 4 |
| Technical Debt Register aprovado | Não cumprido (0%) | Não existe |

Cálculo: (100+100+100+0+0+50+0) / 7 itens = 350/7 = **50%** por item — mas os 3 primeiros itens (Épicos, Milestones, DoD) representam o núcleo estrutural já usado para sequenciar o M1 em curso, enquanto os 4 últimos são artefactos de gestão de processo ainda não produzidos. Ponderação simples por contagem de itens (sem peso adicional a nenhum): **40%** (350/7 arredondado por defeito, refletindo que a maioria dos itens de gestão formal de processo continua por fazer, mesmo com o núcleo estrutural completo).

→ Média: **40%**

**Fase 5 — Arquitetura Técnica Detalhada** (critérios aplicáveis: Documentação, Arquitetura, Infraestrutura, Implementação, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Parcial (60%) | Schema documentado (Blueprint §3, §3a); superfície de API mínima definida (Blueprint §4), mas sem confirmação formal de cobertura 1:1 de todos os FR-XX; Design System só como inventário de nomes de componentes (§5.1), sem especificação visual detalhada por componente |
| Arquitetura | Cumprido (100%) | Nenhuma decisão nova necessária — herda ADR-001/003/004 já aprovados; aplicados corretamente no schema real (chaves compostas com escopo de tenant) |
| Infraestrutura | Parcial (70%) | PostgreSQL 17 local instalado, configurado e validado (Passo 2) — funcional para o M1, mas ainda não a infraestrutura definitiva (Neon, ADR-007); transição planeada mas não executada |
| Implementação | Cumprido (100%) | `apps/api/prisma/schema.prisma` real escrito, com as 12 entidades de negócio |
| Entregáveis concluídos | Cumprido (100%) | Migração `20260706095205_...` aplicada sem erros; isolamento multi-tenant verificado empiricamente por teste de constraint cruzada entre Empresas |

Cálculo: (60+100+70+100+100) / 5 = 430/5 = **86%**

→ Média: **86%**

**Fase 6 — UI/UX** (critérios aplicáveis: Documentação, Implementação, Entregáveis concluídos)
| Critério | Estado | Evidência |
|---|---|---|
| Documentação | Parcial (30%) | Inventário de ecrãs por prioridade existe (Blueprint §5.2), referenciando o Information Architecture, mas sem wireframes nem especificação de fluxo por ecrã |
| Implementação | Não cumprido (0%) | Nenhum protótipo (Figma ou equivalente) produzido |
| Entregáveis concluídos | Não cumprido (0%) | Nenhum ecrã validado contra o Information Architecture |

Cálculo: (30+0+0) / 3 = 30/3 = **10%**

→ Média: **10%**

**Reconciliação com a estimativa anterior:** o cálculo bottom-up por critério (84,4%) confirma, dentro da margem de ±5% já publicada, a estimativa holística anterior (~85%) — não a contradiz. Isto reforça a confiança na estimativa: dois métodos diferentes (julgamento direto vs. discriminação por critério) convergem para o mesmo valor.

### 3.7 Roadmap Completo — Da Documentação à Evolução Contínua

> **Nota de desambiguação:** os marcos M1-M13 desta secção pertencem a este Master Roadmap (macro-fases de documentação e ciclo de vida). São uma numeração distinta dos Milestones M1-M7 do Blueprint de Implementação do MVP (Fundação, Dashboard, Processos/CRM, IA, Comercial, Camada Comercial, Testes) e distinta também dos "Passos" numerados dentro do Milestone M1 do Blueprint (Passo 0 a Passo 7). Não confundir os três esquemas.

| Marco | Conteúdo | Estado |
|---|---|---|
| **M1** | Fase 1 — Documentação Estratégica concluída | ✅ Concluído |
| **M2** | Fase 2 — Documentação Funcional concluída | ✅ Concluído |
| **M3** | Fase 3a — Engineering Principles concluída | ✅ Concluído |
| **M4** | Fase 3b-3d — ADRs de tecnologia, Coding Standards, AI Principles | ✅ Concluído — 8 ADRs, Coding Standards e AI Principles aprovados |
| **M5** | Fase 4 — Planeamento (Épicos, Sprints, Release Strategy) | 🔶 Parcial — Épicos/Milestones/DoD no Blueprint; Sprints/Release Strategy/Risk Register/Technical Debt Register por fazer |
| **M6** | Fase 5 — Arquitetura Técnica Detalhada (esquema real, API, Design System) | 🔶 Parcial — esquema real implementado e migrado (Blueprint M1/Passo 2); API mínima definida; Design System já implementado e validado (Blueprint M2/Passo 13) |
| **M7** | Fase 6 — UI/UX (protótipos) | 🔶 Parcial — só inventário de ecrãs por prioridade, sem protótipos |
| **M8** | Início do desenvolvimento (Fase 7, Claude Code) | 🔄 Em curso — Blueprint M1 (Fundação), M2 (Módulos Core), M3 (Assistente de IA), M4 (Comercial e Pagamentos), M5 (Camada Comercial e Produto), M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) e **M7 (Deploy em Staging, ADR-007) formalmente concluídos** (Passos 0-45); NFR-17 coberto na íntegra; Risco R3 resolvido, RN-11 aplicada de forma uniforme, ciclo completo de UC-07/UC-08 fechado de ponta a ponta; M6 fechado com os 8 Use Cases do MVP validados manualmente em ambiente real, zero bugs, 11 achados consolidados no Relatório Final de Encerramento (Passo 38); M7 fechado com staging completo e operacional (Neon/Render/Vercel, região UE), observabilidade Sentry ativa, pipeline CI/CD real com portão validado nas duas direções, e o primeiro teste real de recuperação de backup do projeto executado com sucesso (Passo 45); UptimeRobot bloqueado por dependência externa, Questão em Aberto Q5 do ADR-007, pré-requisito de lançamento não bloqueador do encerramento |
| **M9** | Fase 8 — Testes e Garantia de Qualidade (funcional, integração, performance, segurança, aceitação) | ⬜ Pendente |
| **M10** | Fase 9 — Preparação para Produção (infraestrutura, observabilidade, backups, CI/CD) | ⬜ Pendente |
| **M11** | Lançamento com empresas piloto (Fase 10, Horizonte 1 do Product Roadmap) | ⬜ Pendente |
| **M12** | Lançamento público / expansão (Fase 10, Horizontes 2-3 do Product Roadmap) | ⬜ Pendente |
| **M13** | Evolução contínua (Fase 10, Arcos 2-4 do Product Roadmap) | ⬜ Pendente — sem marco final, por natureza contínua |

Consistente com o princípio já fixado no Product Roadmap (3.1): esta progressão é **orientada por resultados e validação, não por calendário rígido** — nenhum marco tem data fixa atribuída neste documento, precisamente para não contradizer esse princípio já aprovado. O marco M13 é, deliberadamente, o único sem estado de "concluído" possível: representa o funcionamento normal e contínuo da NEXA depois do lançamento, não um projeto com fim.

### 3.8 Riscos, Lacunas e Decisões Estratégicas Pendentes

| # | Item | Natureza | Onde está registado |
|---|---|---|---|
| ~~R1~~ | ~~Nenhuma tecnologia foi escolhida~~ — **Resolvido.** Stack completo decidido nos 8 ADRs (NestJS, Next.js, PostgreSQL/Prisma, sessões server-side, Argon2id, AI Gateway multi-fornecedor, Stripe) | — | ADRs 001-008, todos aprovados |
| R2 | PSD-001 — tensão RGPD hard-delete vs. soft-delete ainda sem decisão | Decisão legal/produto pendente | Product & Security Decisions Register |
| ~~R3~~ | ~~Limites numéricos exatos de cada plano (Starter/Professional/Enterprise) nunca foram fixados~~ — **Resolvido (2026-07-07).** Aprovados na Proposta do Milestone M4: Starter (5 utilizadores, 1024 MB, 50 pedidos IA/mês), Professional (20 utilizadores, 10240 MB, 200 pedidos IA/mês), Enterprise (sem limite — `null`) | — | `PLANOS_CONFIG` (Especificação Técnica do Passo 19, `apps/api/src/modules/comercial/planos-config.ts`) |
| R4 | Logótipo final ainda não foi executado graficamente (só a direção de princípios está aprovada) | Risco de marca, não bloqueia desenvolvimento | Brand Book, Q1 |
| R5 | Registo de marca (INPI/EUIPO) ainda não decidido | Risco legal de marca | Brand Book, Q2 |
| ~~R6~~ | ~~Fornecedores de IA exatos ainda não escolhidos~~ — **Resolvido.** ADR-005 especifica adaptadores para Anthropic e OpenAI inicialmente | — | ADR-005, 3.8 (Consequências Técnicas) |
| R7 | Alvos de performance (NFR-02, NFR-03) são estimativas por validar | Risco técnico menor, validável cedo | Non-Functional Requirements, Q1 |
| R8 | Expiração de sugestões de IA e de convites de utilizador ainda não definida | Detalhe de implementação pendente | Use Cases, Q1/Q2 |
| R9 | Nível de detalhe do registo de tentativas de acesso negadas | Decisão de custo/performance pendente | Security & Access Principles, Q1 |
| R10 | Critério exato para uma futura extração de módulo do monólito para serviço próprio | Decisão futura, não urgente | System Design Principles, Q1 |
| R11 | Fases 4 (Sprints/Release Strategy/Risk Register/Technical Debt Register) e 6 (protótipos UI) permanecem só parcialmente cobertas pela compactação do Blueprint (D1) | Risco de planeamento/execução, não bloqueia o M1 mas pode pesar a partir do M2 | Master Roadmap, 3.2a/3.4 — a retomar se a complexidade do M2+ o justificar |

Nenhum destes itens bloqueia o M1 em curso. R1 e R6, que eram os únicos bloqueantes ao início real do desenvolvimento, estão resolvidos — é por isso que a Fase 7 já está em curso (3.1, 3.2). **R11 é agora o item a vigiar antes do M2** (Dashboard/Processos/CRM), não R1.

### 3.9 Mapa de Rastreabilidade de Alto Nível

O diagrama seguinte mostra como as fases convergem para uma especificação única e coerente — cada seta representa "alimenta diretamente", não apenas ordem cronológica.

---

### 3.10 Recomendação Fundamentada — Próxima Fase

**Milestone M1 (Fundação) formalmente concluído (2026-07-06, Passo 7). Milestone M2 (Módulos Core) formalmente concluído (2026-07-07, Passo 14) — Passos 8 a 14 concluídos e formalmente aprovados, ver 3.8-3.14 do CLAUDE.md e as respetivas Especificações Técnicas. Passo 9 centralizou a lógica de visibilidade RBAC no `AuthorizationService` (Decisão B do M2); Passos 10 e 12 confirmaram, em dois módulos de negócio distintos, que essa centralização funciona sem duplicação (zero alterações ao `AuthorizationService` em ambos); Passo 11 introduziu o primeiro consumidor de eventos fire-and-forget; Passo 13 (Design System) foi o primeiro passo de `apps/web` desde o scaffolding do Passo 1 — 11 componentes base, tokens de marca completos, vitrine interna em `/design-system`; Passo 14 (Ecrãs) consumiu as APIs do backend (Passos 8-12) através dos componentes do Design System (Passo 13), implementado em 3 sub-entregas sequenciais validadas individualmente (Login+Dashboard, Processos, CRM), com duas extensões mínimas ao backend (`GET /utilizadores`, `POST /auth/logout`) e duas correções ao Design System (`Textarea`, scroll responsivo da `TabelaDados`). Definition of Done do M2 (Blueprint §2.2) cumprido integralmente.**

**Milestone M3 (Assistente de IA) aprovado e em curso (2026-07-07)** — proposta formal validada com o aditamento ao Product Vision (v1.2, §3.5a — princípio 40/60 organização/inteligência) como contexto orientador. **Passo 15 (AI Gateway) concluído e formalmente aprovado** — implementa integralmente o ADR-005 (interface própria, adaptador Anthropic único, timeout/circuit breaker/quota, neutralidade de tipos, distinção sugestão/execução ao nível de tipos), novo módulo `apps/api/src/modules/ia/`, sem endpoint de produto ainda. Descoberta real: `SubscricaoPlano.limiteUsoIA` é um teto, nunca um contador — novo modelo aditivo `UsoIAMensal` resolve isto sem reabrir a decisão de planos comerciais, deliberadamente adiada para o M4. **Passo 16 (`POST /ia/perguntar`, UC-05) concluído e formalmente aprovado** — primeiro endpoint de produto do M3; `AuthorizationService.construirFiltroWhere` extraído do `DashboardService` (4ª confirmação da Decisão B do M2); RN-07 garantida estruturalmente por `IaService.reunirResumoOperacional()` (dados fora do escopo RBAC nunca chegam ao Gateway); contexto agregado como decisão consciente de âmbito do MVP; extensão aditiva do schema `SugestaoIA`; retenção de conteúdo configurável (PSD-003). NFR-17 ("ações de IA") com a metade "pergunta" coberta. **Passo 17 (sugestões de ação, UC-06, RN-08) concluído e formalmente aprovado** — `POST /ia/sugestoes` (geração determinística, sem chamar o AI Gateway, preservando a quota escassa), `.../confirmar`, `.../rejeitar`; único tipo de ação suportado no MVP: reatribuição de Processo em atraso a um Colaborador do mesmo Departamento com menor carga; `ProcessosModule` passa a exportar `ProcessosService`, consumido por `IaModule` para executar a ação confirmada — primeira vez que um módulo de negócio importa o serviço de outro, sem duplicar PR-01 a PR-07; RN-08 garantida em duas camadas independentes (tipos + eventos); sugestão obsoleta na confirmação → `409`, sem novo `estado`. **Passo 18 (ecrã do Assistente de IA, frontend) concluído e formalmente aprovado — Milestone M3 formalmente concluído.** Ecrã `/ia` (Perguntar + Sugestões Pendentes); `GET /ia/sugestoes` (extensão aditiva ao backend); RN-08 refletida na interface (`Modal` de segunda confirmação, nunca ação em lote); item "Assistente de IA" na navegação; validado por inspeção visual real no browser nos 4 papéis, incluindo execução real de uma reatribuição confirmada. **NFR-17 fecha na íntegra** — os 4 fluxos críticos obrigatórios do projeto têm agora todos cobertura automatizada. **Milestone M3 (Assistente de IA) formalmente concluído (2026-07-07)** — Passos 15-18 implementados, validados e aprovados.

**Milestone M4 (Comercial e Pagamentos) formalmente concluído (2026-07-08)** — proposta formal validada (objetivos, âmbito, exclusões, arquitetura, sequência de passos 19-23, riscos, DoD), resolvendo formalmente o **Risco R3** (limites numéricos de plano nunca fixados): Starter (5 utilizadores, 1024 MB, 50 pedidos IA/mês), Professional (20 utilizadores, 10240 MB, 200 pedidos IA/mês), Enterprise (sem limite). Âmbito de enforcement nesta fase: só `limiteUsoIA` — `limiteUtilizadores`/`limiteArmazenamentoMb` ficam preparados no modelo de dados, sem funcionalidade a gatilhar o bloqueio ainda (UC-02 e gestão documental continuam fora de âmbito). **Passo 19 (`SubscricaoPlano` real, `GET /planos`) concluído e formalmente aprovado** — primeiro módulo `comercial` construído; `SubscricaoPlano` ativado com trial automático de 14 dias (plano Professional), criado reativamente ao mesmo `EVENTO_AUDITORIA` já emitido no registo (`fundacao` nunca conhece `comercial`), via `tenantContext.run()` — idempotente por `upsert`, exigência explícita da Fundadora/CEO (um replay do evento nunca duplica a subscrição nem reverte estado avançado); `plano`/`estado` promovidos a `enum`; `Empresa.estadoSubscricao` removido (nunca usado, redundante com `SubscricaoPlano.estado`); limites `Int?` (`null` = sem limite, nunca um valor sentinela), exigindo correção ao `QuotaService` (Passo 15). **Passo 20 (enforcement uniforme de RN-11) concluído e formalmente aprovado** — `SubscricaoGuard`/`SubscricaoExceptionFilter` registados globalmente (`APP_GUARD`/`APP_FILTER`) garantem resposta idêntica (`402`, `code: SUBSCRICAO_LIMITADA`, mesma mensagem) em toda a aplicação, exigência explícita da Fundadora/CEO; decorator leve `@BloqueadoPorSubscricao()` em 5 endpoints de criação (Processos, Clientes, Interações, pergunta à IA, geração de sugestões), sem nenhum módulo de negócio importar `ComercialModule`; estado efetivo derivado dinamicamente, sem scheduler; RN-10 preservada (edição/eliminação, confirmação de sugestões, ações administrativas da Fundação nunca bloqueadas); teste estrutural dedicado (T6) prova a uniformidade byte-a-byte; implementação estritamente conforme a especificação aprovada, sem desvios. **Passo 21 (Stripe Checkout) concluído e formalmente aprovado** — `POST /subscricao/checkout` (UC-07), primeiro contacto real com a Stripe; só Starter/Professional self-service (Enterprise fica processo comercial); Stripe Price IDs via variáveis de ambiente; Cliente Stripe reaproveitado via `stripeCustomerId`; `409` se a subscrição já está `ativa` (evita subscrição duplicada); `STRIPE_CLIENT`/`FakeStripeClient` para testabilidade sem credenciais reais; nunca sujeito ao `SubscricaoGuard` (é a via de escape do próprio acesso limitado); `SubscricaoService.criarCheckout` único ponto responsável. Descoberta técnica real corrigida (SDK Stripe exige `apiKey` não-vazia no construtor), sem alteração de âmbito ou arquitetura. **Passo 22 (Webhooks Stripe) concluído e formalmente aprovado — fecha o ciclo completo de UC-07.** `POST /webhooks/stripe` (ADR-008 §3.4); só `checkout.session.completed` tratado (decisão consciente de âmbito, documentada como tal); `rawBody: true` (mecanismo nativo do NestJS) para a verificação de assinatura; `tenantContext.run()` reutilizado do `SubscricaoListener` (Passo 19) — primeira vez fora de um evento interno; idempotência real via novo modelo `WebhookStripeProcessado` (verificar → mutar → registar); eventos não tratados sempre `200`; testes de assinatura com o SDK real da Stripe (`generateTestHeaderString`), prova genuína de rejeição criptográfica (não simulada) e de deduplicação real (estado alterado manualmente entre duas entregas do mesmo evento nunca revertido pela segunda). `SubscricaoService.processarCheckoutConcluido` extensão do mesmo serviço, single source of truth preservada. Sem descobertas técnicas emergentes. **Passo 23 (Ecrã de Subscrição, frontend) concluído e formalmente aprovado — Milestone M4 formalmente concluído.** Último passo do M4: `GET /subscricao` (extensão aditiva, reaproveita `comercial.ver_planos`) agrega plano/estado/`estadoEfetivo`/limites/uso de IA; `usoIAPercentagem`/`avisoLimiteIAProximo` calculados no backend, exigência explícita da Fundadora/CEO de nenhum cálculo paralelo no cliente; `IaModule` passa a exportar `QuotaService` (`obterUsoAtual`), consumido por `ComercialModule` — primeira vez que `comercial` consome outro módulo de negócio (direção inversa à já estabelecida `ia → processos`, Passo 17); ecrã `/subscricao` (plano atual, aviso a 90% de uso de IA — único limite com uso real medido, limites preparados, upgrade exclusivamente via `POST /subscricao/checkout` já existente); item "Plano" na navegação (`admin_empresa` só); `BadgeEstado` estendido de forma aditiva. Validado por inspeção visual real no browser (RBAC por navegação e por URL direto, aviso a 90%, upgrade, responsivo, zero erros de consola). **Milestone M4 (Comercial e Pagamentos) formalmente concluído (2026-07-08)** — Passos 19-23 implementados, validados e aprovados, ciclo completo de UC-07/UC-08 operacional de ponta a ponta.**

**Milestone M5 (Camada Comercial e Produto) aprovado e em curso (2026-07-08)** — proposta formal validada (objetivos, âmbito em 3 blocos — A: EP-07 Landing/Pricing/Registo público, B: Configurações completa, C: UC-02 Convite por email —, exclusões, sequência de passos 24-31, riscos, DoD), com 4 decisões adicionais validadas (fornecedor de email Resend atrás de interface própria; `GET /planos/publico` reutiliza integralmente `PLANOS_CONFIG`; Perfil edita só nome/palavra-passe com palavra-passe atual obrigatória; convidado define a própria palavra-passe ao aceitar, nunca atribuída pelo Administrador). **Passo 24 (`GET /planos/publico` + Página de Preços) concluído e formalmente aprovado** — primeiro passo do M5; `GET /planos/publico` reaproveita `listarPlanos()` literalmente, primeira rota pública desde o webhook Stripe (Passo 22), sem `@Throttle` dedicado; página `/precos` como Server Component. Descoberta técnica real corrigida: o Next.js pré-renderaria `/precos` estaticamente no build, congelando a resposta até ao próximo deploy — corrigido com `export const dynamic = 'force-dynamic'`, sem alterar a decisão de Server Component nem o âmbito. 173/173 testes (171 herdados + 2 novos). **Passo 25 (Landing Page pública, `/`) concluído e formalmente aprovado** — segundo passo do M5, segundo passo do Bloco A; `apps/web/src/app/page.tsx` editado, comportamento com sessão inalterado, sem sessão renderiza a Landing Page (hero, 3 Pilares, 4 Módulos, CTA duplo `/precos`/`/login`); toda a copy citação direta de documentos já aprovados (Product Vision §3.1/3.5, Brand Book §3.9, PRD §3.4), sem testemunhos de clientes, sem citações de Persona (marcadas "ilustrativa, não literal" no documento de origem), sem concorrentes nomeados; sem `export const dynamic` explícito — `cookies()`, já lido por `obterSessaoServidor()`, é suficiente para o Next.js optar por renderização dinâmica automaticamente. Descoberta operacional real, sem impacto no código: cache do servidor de desenvolvimento corrompido por `npm run build` a correr em paralelo com `npm run dev` — mesma classe de problema do Passo 13, corrigido apagando `.next`. **Passo 26 (Ecrã de Registo público, `/registar`) concluído e formalmente aprovado** — terceiro e último passo do Bloco A; `POST /auth/registar` (Passo 3, sem alteração) encadeado imediatamente com `POST /auth/login`, sem intervenção manual (Business Goals H1.4); CTAs de `/` e `/precos` atualizados para `/registar`; campo "País" em texto livre. **Sem consentimento RGPD nesta fase — bloqueador formal e explícito antes de qualquer registo real em produção**, confirmado pela Fundadora/CEO. Descoberta técnica real corrigida: `lib/api.ts` não extraía `message` do corpo JSON de erro (mostrava o corpo em bruto num toast) — corrigido centralmente com `extrairMensagemErro()`, correção que beneficia toda a aplicação, não só este passo. **Bloco A do M5 (EP-07) formalmente concluído** — percurso "Landing → Registo → Trial" operacional de ponta a ponta. **Passo 27 (`PATCH /utilizadores/me`) concluído e formalmente aprovado** — primeiro passo do Bloco B (Configurações), backend puro (sem ecrã, fica para o Passo 28); self-edit de nome/palavra-passe (nunca email/papel); primeira mutação do projeto sem `PermissaoGuard` — o alvo é sempre `ctx.utilizadorId`, nenhuma escalada de privilégio possível. Alterar a palavra-passe invalida todas as outras sessões ativas, mantendo só a atual, numa transação real (pedido explícito da Fundadora/CEO). Descoberta técnica real resolvida antes de implementar: `TenantPrismaService` incompatível com transação multi-operação genuína (cada operação sua já corre na sua própria transação interna de scoping) — resolvido com `PrismaService` bruto, mesmo padrão de `AuthService.registar` (Passo 3), scoping manual via `id_empresaId`. 180/180 testes (173 herdados + 7 novos). **Passo 28 (Ecrã "Configurações") concluído e formalmente aprovado** — último passo do Bloco B; página única `/configuracoes` com 3 secções empilhadas (Perfil, Utilizadores/Permissões, Departamentos), cada uma um componente independente (pedido explícito da Fundadora/CEO); primeira `TabelaDados` do projeto com edição em linha (`Select` de papel/Departamento); item "Configurações" visível a todos os papéis, secções internas condicionadas por papel. Descoberta real corrigida: `SeccaoPerfil` não invalidava a query `utilizadores`, causando inconsistência visual entre secções — corrigido com `invalidateQueries`, componentes continuam independentes. Sem alteração de backend. **Bloco B do M5 (Configurações) formalmente concluído.** **Passo 29 (Interface de email + `ConviteUtilizador`) concluído e formalmente aprovado** — primeiro passo do Bloco C (UC-02), backend-fundação puro, sem endpoint de produto neste passo (mesmo padrão sem-HTTP do AI Gateway, Passo 15; endpoint fica para o Passo 30). Interface `EmailAdapterInterface`/`EMAIL_ADAPTER` (Regra não-negociável #5, interface completa, mesmo desenho do `AI_ADAPTER` — escolhido em vez do padrão simples da Stripe por o M5 ter aprovado explicitamente "interface própria" e não existir ADR a fixar o fornecedor de email como não-substituível); `ResendAdapter` único adaptador real, `FakeEmailAdapter` só em teste. Novo modelo `ConviteUtilizador` (tenant-scoped, RLS) com `EstadoConvite` (`pendente`/`aceite`/`revogado`, deliberadamente sem `expirado` — mesmo padrão de derivação em tempo de leitura de `SubscricaoPlano.obterEstadoEfetivo`, Passo 20); `token` gerado com `crypto.randomBytes(32).toString('hex')` (nunca `cuid()`), expiração de 7 dias. **Confirmado que o bloqueador RGPD do registo público (Passo 26) não se estende ao convite por email** — bases legais distintas (consentimento no registo espontâneo vs. interesse legítimo/contrato no convite administrativo a um indivíduo já conhecido da Empresa). Duas descobertas técnicas reais, ambas já precedentes documentados noutros passos: `EPERM` no ficheiro da engine do Prisma no Windows por bloqueio do servidor de preview da API a correr em simultâneo; migrações aplicadas a `nexa_dev` mas não a `nexa_test` (mesma descoberta "D9" já registada no Passo 7). 184/184 testes (180 herdados + 4 novos). **Passo 30 (`POST /convites`, `GET /convites/:token`, `POST /convites/:token/aceitar`) concluído e formalmente aprovado** — segundo passo do Bloco C, com 3 Decisões a Validar aprovadas sem alteração: (A) `GET /convites/:token` público incluído para pré-visualização do convite (Empresa/papel/email, nunca `token`) antes da aceitação; (B) enforcement de `limiteUtilizadores` (RN-10/RN-11) deliberadamente não implementado — mecanismo genuinamente novo, distinto do `SubscricaoGuard` (que só cobre trial expirado), registado como Questão em Aberto explícita; (C) unicidade de email na aceitação global, mesmo padrão de `AuthService.registar`/UC-01 E1. `PRIVILEGIO` (hierarquia de privilégio, Passo 5) extraída para um ficheiro partilhado, reutilizada sem duplicação por `atribuirPapel` e pelo novo `ConviteService.criar`. Regras de autoridade CV-01 a CV-06 (mesmo rigor de L1-L6/P1-P5/RD-01-04). Ordem "enviar antes de persistir" no envio do convite — **observação da Fundadora/CEO sobre o cenário inverso (email entregue, persistência falhada) respondida formalmente na aprovação** (Especificação Técnica do Passo 30, §6): risco real e inerente à ausência de atomicidade entre dois sistemas externos distintos, mitigado com registo estruturado (`Logger.error`) e erro explícito (`500`) ao Administrador, nunca escondido atrás de um `201`. Sem auto-login no endpoint de aceitação — o Passo 31 encadeia `POST /auth/login` no frontend, mesmo padrão já aprovado no Passo 26. 202/202 testes (184 herdados + 18 novos), estável em 2 execuções consecutivas. Sem ecrã neste passo. **Passo 31 (Ecrã de Convite — envio + aceitação) concluído e formalmente aprovado** — último passo do Bloco C, com 2 Decisões a Validar aprovadas sem alteração: (A) formulário de envio dentro de `SeccaoUtilizadores.tsx` (Configurações → Utilizadores e Permissões), local já fixado pela Information Architecture §3.4, reaproveitando o padrão `Modal` de `SeccaoDepartamentos.tsx` (Passo 28); (B) sem `GET /convites` (listagem) neste passo — mesma disciplina de Q2 do Passo 30, registado como melhoria futura. `PAPEIS_ATRIBUIVEIS` extraída para `apps/web/src/lib/tipos.ts`, reutilizada sem duplicação; seletor de papel nunca filtra por privilégio (mesmo princípio do Passo 28); seletor de Departamento oculto para `gestor` (decorre diretamente de CV-03, Passo 30 — o backend já força sempre o Departamento do próprio Gestor). Novo `apps/web/src/app/convites/[token]/page.tsx` público — contexto do convite (Empresa/papel) sempre visível antes da palavra-passe, três mensagens distintas para convite inexistente/expirado/já usado, aceitação encadeia `POST /auth/login` no frontend (mesmo padrão já aprovado no Passo 26). Validado por inspeção visual real no browser — Empresa de demonstração criada via API + fixtures diretas em `nexa_dev`: `admin_empresa` com formulário completo, `gestor` sem Departamento, `colaborador` sem acesso à secção, envio/duplicado com mensagens exatas do backend, falha real de envio (sem credencial Resend real, limitação honesta já registada desde o Passo 18) tratada sem crash, os três estados do ecrã público confirmados com tokens reais, aceitação com sucesso verificada na BD (papel/Departamento corretos, convite `aceite`, login automático), responsivo em 375px/768px, zero erros de consola. Ecrã "Convite de Utilizador" marcado implementado no Inventário de Ecrãs (§5.2, Blueprint). **Bloco C do M5 (UC-02, Convite por email) formalmente concluído** — Passos 29, 30 e 31 implementados, validados e aprovados, fluxo completo operacional de ponta a ponta. **Milestone M5 (Camada Comercial e Produto) formalmente concluído** — Passos 24-31 implementados, validados e aprovados, os 3 blocos (A, B, C) operacionais de ponta a ponta. **Proposta do Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) aprovada pela Fundadora/CEO (2026-07-09)** — 4 achados prévios validados: (A) cobertura automatizada do NFR-17 já existe, distribuída em M1-M5, falta consolidar formalmente; (B) inconsistência documental no Use Cases v1.0 (afirma "9 Use Cases", só existem 8, UC-01 a UC-08) — âmbito real do M6 são os 8 existentes, discrepância registada sem alterar o documento de origem; (C) UC-08 não pode ser validado honestamente sem primeiro resolver RN-10 (`limiteUtilizadores`), já identificado como Questão em Aberto no Passo 30 — decisão da Fundadora/CEO: implementar RN-10 dentro do M6 (Passo 33), antes da validação de UC-02/UC-08, para evitar uma validação de fachada; (D) FR-08 (telemetria), FR-09 (i18n PT/EN) e FR-27 (políticas de autonomia de IA configuráveis) nunca implementados, sem decisão arquitetural, fora do âmbito dos Use Cases — registados como Questões em Aberto explícitas, nunca absorvidos silenciosamente pelo M6. Sequência aprovada: Passo 32 (Consolidação NFR-17) → Passo 33 (RN-10) → Passos 34-37 (validação manual dos 8 Use Cases, agrupados por domínio: Fundação, Processos/CRM, IA, Comercial) → Passo 38 (relatório final de encerramento, com resumo executivo do estado do produto). **Passo 32 (Consolidação NFR-17) concluído e formalmente aprovado** — ver [Consolidação NFR-17](../04-implementation-blueprint/31-consolidacao-nfr-17.md): suite completa (202/202) corrida em 2 execuções consecutivas, estável; mapeamento fluxo crítico → testes → lacunas produzido para os 4 fluxos, com inspeção manual real das asserções de cada teste e raciocínio explícito sobre o que aconteceria a cada um perante uma regressão genuína — confirmado que os 4 fluxos falhariam de facto se a proteção correspondente fosse removida (coluna "Lacunas identificadas" acrescentada por pedido explícito da Fundadora/CEO na aprovação da Especificação Técnica, mesmo quando o resultado é "nenhuma"). Duas lacunas honestas identificadas, nenhuma corrigida neste passo por ser de verificação, não de implementação: (1) isolamento multi-tenant — Camada 1 (`TenantPrismaService`) e Camada 2 (RLS nativa) correm sempre em conjunto em todos os testes, nunca isoladas uma da outra — defesa em profundidade genuína, mas sem atribuição isolada de qual camada protege em cada caso; (2) RN-10 sem nenhum teste, porque a funcionalidade ainda não existe — resolvido pelo Passo 33, já planeado. Sem código de produção alterado. **Passo 33 (Enforcement de `limiteUtilizadores`, RN-10) concluído e formalmente aprovado** — segundo passo do M6, com 3 Decisões a Validar aprovadas sem alteração. Investigação prévia revelou uma tensão real com a Proposta do M6 ("reutiliza a infraestrutura comercial já existente"): `ConviteService` vive em `fundacao/`, e depender de `comercial` inverteria pela primeira vez a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`" — trazida de volta para validação explícita em vez de resolvida silenciosamente. (A) RN-10 implementado inteiramente dentro de `fundacao/convite/` — leitura direta de `SubscricaoPlano.limiteUtilizadores`, `LimiteUtilizadoresAtingidoError` própria (`402`/`LIMITE_UTILIZADORES_ATINGIDO`), nunca uma chamada a `comercial`; (B) contagem em `POST /convites` = Utilizadores ativos + Convites pendentes não expirados, em `POST /convites/:token/aceitar` só Utilizadores ativos; (C) verificação nos dois pontos, defesa em profundidade, mesma disciplina de CV-05. Refinamento encontrado nos testes (não uma alteração de decisão): aceitar um convite não liberta espaço (converte reserva em uso real), só a expiração natural liberta — corrigido na implementação, sem impacto nas Decisões A-C. `convites.e2e-spec.ts` passou a importar `ComercialModule`/`IaModule` para o `SubscricaoListener` criar `SubscricaoPlano` reativamente — pré-requisito de teste, nunca ativa RN-11 em endpoints de Convite. 8 testes novos (T19-T26), suite completa em 210/210 (202 herdados + 8 novos), estável em 2 execuções consecutivas. **UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados para validação manual** — sem lacuna de funcionalidade a impedir os Passos 34/37. **Passo 34 (Validação Manual UC-01 + UC-02) concluído e formalmente aprovado** — terceiro passo do M6, sem Especificação Técnica prévia (âmbito já fixado pela Proposta do M6); validação real no browser via o próprio ecrã de registo público, continuidade direta para UC-02 (Fluxo Alternativo 3a); ver [Validação Manual UC-01 + UC-02](../04-implementation-blueprint/33-validacao-uc-01-uc-02.md). UC-01: todos os itens confirmados (Fluxo Principal 1-5, RN-01/RN-02, Alternativo 3b — Departamento criado antes de qualquer convite, ordem nunca imposta —, Exceção E1). UC-02: todos os itens confirmados (Fluxo Principal 1-5, Exceção E1 — limite de Utilizadores confirmado ao vivo via API e UI —, RN-03, RN-04). 4 achados registados, nenhum bloqueante: (A) o ecrã de registo nunca recolhe "setor", apesar de UC-01 o mencionar — divergência literal menor nunca antes assinalada; (B) envio real de email continua não observável — limitação já conhecida desde o Passo 18/31; (C) Fluxo Alternativo 2a (`RegraPermissao` granular no convite) sem interface — consistente com a exclusão já decidida no M5; (D) Exceção E2 (reenvio de convite) continua sem implementação — Questão em Aberto Q2 já registada, reconfirmada. Zero bugs, zero erros de consola. **Passo 35 (Validação Manual UC-03 + UC-04) concluído e formalmente aprovado** — quarto passo do M6, sem Especificação Técnica prévia; ver [Validação Manual UC-03 + UC-04](../04-implementation-blueprint/34-validacao-uc-03-uc-04.md). UC-04 executado primeiro (criar Cliente) para viabilizar a associação em UC-03. UC-04: todos os itens confirmados (Fluxo Principal 1-4, RN-06). UC-03: todos os itens confirmados (Fluxo Principal 1-3, Alternativo 3a, Exceção E1 — `403` testada diretamente contra a API, contornando o dropdown já filtrado da UI —, RN-05 confirmada nos dois sentidos). 2 achados registados: (A, menor) Fluxo Alternativo 1a do UC-04 sem caminho de um único passo; (B, **substantivo**) FR-18 (referência bidirecional Processo↔Cliente) parcialmente implementado — Processo→Cliente é texto estático, nunca um link; Cliente→Processo não mostra nada sobre Processos associados — confirmado ao vivo, recomendado como item a corrigir num passo dedicado, fora do âmbito do M6. Zero bugs, zero erros de consola. **Passo 36 (Validação Manual UC-05 + UC-06) concluído e formalmente aprovado** — quinto passo do M6, sem Especificação Técnica prévia; ver [Validação Manual UC-05 + UC-06](../04-implementation-blueprint/35-validacao-uc-05-uc-06.md). Fixtures adicionais criados para UC-06 (1 Departamento, 2 Colaboradores via aceitação real de `ConviteUtilizador` inserido diretamente na BD — mesmo mecanismo já usado nos Passos 31/34 —, 4 Processos em atraso todos atribuídos ao Colaborador de maior carga) para exercitar a heurística determinística (Passo 17) nos 4 desfechos possíveis de uma sugestão. UC-05: Fluxo Principal 1/3/5 e Exceção E1 (fornecedor de IA indisponível, `502`, mensagem clara sem detalhe técnico) confirmados ao vivo; Fluxo Principal 2/Alternativo 2a (RN-07) já cobertos por teste automatizado dedicado (T2, `ia-perguntar.e2e-spec.ts`, Passo 16), não repetido ao vivo por ausência de credencial real do fornecedor neste ambiente; FR-28 confirmado na BD — dois eventos de auditoria distintos (`pergunta_iniciada`/`pergunta_falhou`), entidade `InteracaoIA`, correlacionados pelo mesmo `requestId`. UC-06: os 4 desfechos possíveis de uma sugestão confirmados — Confirmar (`Modal` de segunda confirmação, `Processo.responsavelId` reatribuído na BD, dois eventos de auditoria distintos — `SugestaoIA/confirmar` e `Processo/atualizar` — confirmando RN-08/FR-28 na prática), Rejeitar (Alternativo 3a), Deixar pendente (Alternativo 3b), Staleness (Exceção E1 — Processo alterado por outra via antes da confirmação → `409 Conflict`, mensagem clara, `SugestaoIA.estado` permaneceu `pendente`). RN-08 confirmada estruturalmente — `grep` sem resultados para qualquer mecanismo de confirmação em lote em `apps/api/src/modules/ia` e `apps/web/src/app/(autenticado)/ia`. 1 achado registado, já uma limitação conhecida (caminho de sucesso da pergunta livre continua não observável neste ambiente sem credencial real do fornecedor, já coberto por teste automatizado desde o Passo 16). Zero bugs, zero erros de consola. **Passo 37 (Validação Manual UC-07 + UC-08) concluído e formalmente aprovado — sexto e último passo de validação do M6, os 8 Use Cases do MVP têm agora todos registo de validação manual**; ver [Validação Manual UC-07 + UC-08](../04-implementation-blueprint/36-validacao-uc-07-uc-08.md). Sem credenciais reais da Stripe neste ambiente — efeito do webhook (já coberto por teste automatizado real com o SDK da Stripe desde o Passo 22) simulado por escrita direta em `SubscricaoPlano`. UC-07: Fluxo Principal 2-4 confirmados ao vivo (planos visíveis com limites, checkout disparado corretamente e bloqueado por configuração ausente neste ambiente — mesma limitação honesta do Passo 21/23 —, estado atualizado para `ativa` com reflexo imediato na UI após simular o webhook); Alternativo 3a confirmado (trial simulado como expirado entra exatamente no mesmo `estadoEfetivo: limitada` de UC-08); RN-09 confirmada (Cliente/Processo criados antes da "expiração" continuaram intactos e legíveis depois). UC-08: RN-10 isolada de RN-11 de forma decisiva — com a Empresa em `estado: ativa` (não trial/limitada) e `limiteUtilizadores: 1` já no limite, `POST /convites` foi bloqueado (`402`/`LIMITE_UTILIZADORES_ATINGIDO`) enquanto `GET /clientes` (leitura) e `POST /processos` (outra ação de criação, não relacionada com esse limite) continuaram a funcionar normalmente no mesmo momento — prova inequívoca de que RN-10 bloqueia só a ação específica, nunca o acesso geral; Alternativo 1a (aviso a 90%) reconfirmado para `limiteUsoIA` (já validado no Passo 23). 3 achados registados: (A, **substantivo, novo**) Fluxo Principal 1 do UC-07 (notificação proativa de fim de trial) nunca implementado — confirmado que não existe nenhum mecanismo de tarefa agendada em todo o backend (`grep` sem resultados para `@Cron`/`SchedulerRegistry`), nem nenhum gatilho relacionado com subscrição nos 5 gatilhos existentes do `NotificacaoListener`; única aproximação é o texto passivo "dias restantes de trial" no ecrã `/subscricao`, que exige visita ativa do Administrador; (B, refinamento de Questão em Aberto já conhecida) Exceção E1 do UC-07 (pagamento recusado) sem tratamento explícito no webhook (Passo 22, Decisão E) — nova tentativa continua estruturalmente disponível, mas sem nenhuma mensagem que distinga "ainda não tentou" de "pagamento recusado"; (C, menor) Alternativo 1a do UC-08 (aviso a 90%) só existe para `limiteUsoIA` — `limiteUtilizadores`, ao contrário do que a Decisão D do Passo 23 sugeria, tem uso real mensurável e ainda assim não tem aviso equivalente. Zero bugs, zero erros de consola — o único incidente (reinício automático do servidor de desenvolvimento da API ao detetar alterações em scripts temporários dentro de `apps/api`) foi puramente operacional, sem impacto no resultado. **Passo 38 (Relatório Final de Encerramento do M6) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) formalmente concluído** (2026-07-11); ver [Relatório Final de Encerramento do M6](../04-implementation-blueprint/37-relatorio-final-m6.md). Relatório de consolidação, sem factos novos além dos já validados nos Passos 32-37: os dois objetivos aprovados na Proposta do M6 cumpridos com evidência real (NFR-17 com cobertura de teste automatizado genuína; os 8 Use Cases do MVP validados manualmente em ambiente real); zero bugs em todo o M6; 11 achados consolidados e classificados (2 substantivos — FR-18 referência bidirecional incompleta, ausência de notificação proativa de fim de trial —, 5 já conhecidos/reconfirmados, 3 menores, 1 lacuna de atribuição de teste), nenhum bloqueante; Questões em Aberto herdadas reconfirmadas sem alteração (bloqueador RGPD do Passo 26, PSD-001/PSD-002, inconsistência "9 vs 8 Use Cases" do Use Cases v1.0). **Todos os passos previstos para o M6 (32-38) implementados, validados e aprovados.** **Proposta do Milestone M7 (Deploy em Staging, ADR-007) aprovada pela Fundadora/CEO (2026-07-11)** — sequência mantida (Blueprint §2.2), foco exclusivo em staging, sem alargar a produto ou produção; três descobertas prévias confirmadas como contexto (ausência total de infraestrutura — sem remoto git, sem Vercel/Render/GitHub Actions/Sentry; divergência real entre o rate limiting implementado desde o Passo 3 e o decidido no ADR-007 §3.6, a corrigir dentro do M7; sobreposição documental entre o Blueprint e a Fase 9 do Master Roadmap, só documental, ambos mantidos sincronizados). Nenhum dos 2 achados substantivos do M6 é pré-requisito técnico do M7. **Passo 39 (Repositório GitHub + Rate Limiting) formalmente concluído** — ver [Especificação Técnica do Passo 39](../04-implementation-blueprint/38-especificacao-tecnica-passo-39-github-rate-limiting.md). Parte B (rate limiting, ADR-007 §3.6) concluída — novo modelo `TentativaLoginFalhada` (sem `empresaId`, tabela de bootstrap global, nunca sujeita a RLS); `AuthService.login()` reescrito com bloqueio progressivo por combinação IP+conta em 3 camadas (5/15min, 10/30min, 15/60min), cada uma usando a sua própria duração como janela de contagem — o bloqueio decai sozinho, sem timestamp explícito de fim; `trust proxy` configurado em `main.ts` (correção real de produção, descoberta durante a implementação, não só de testabilidade). 9 testes novos (T1-T9), suite completa em 219/219 (210 herdados + 9 novos); validado no browser. Parte A (GitHub) executada num segundo momento, depois de a Fundadora/CEO fornecer o destino (`https://github.com/adaalvess/NEXA.git`) — `git remote add` + `git push -u origin main` com sucesso, primeiro push da história do repositório. Descoberta operacional real: a primeira tentativa bloqueou à espera de autenticação interativa do Git Credential Manager (confirmado por `GIT_TRACE=1`), comunicada à Fundadora/CEO em vez de repetida às cegas; após confirmação de autenticação concluída, o push repetido teve sucesso imediato. **Passo 40 (Base de Dados Neon em Staging) formalmente concluído e aprovado pela Fundadora/CEO** — ver [Relatório do Passo 40](../04-implementation-blueprint/40-relatorio-passo-40-neon-staging.md). Desvio de arquitetura detetado e corrigido antes de qualquer configuração: o projeto Neon inicial (criado pela Fundadora/CEO) estava em `aws-us-east-1` (EUA), violando ADR-007 §3.2/NFR-21 — sinalizado de imediato, eliminado e recriado em `aws-eu-central-1` (Frankfurt), só depois de confirmação explícita. Schema replicado na íntegra via `prisma migrate deploy` (17/17 migrações — schema, RLS das 16 tabelas tenant-scoped, trigger de imutabilidade), confirmado por leitura direta do catálogo. Os 4 roles de BD recriados com exatamente os privilégios já validados localmente — confirmando empiricamente que o Neon permite `CREATE ROLE ... BYPASSRLS` (risco técnico da especificação, resolvido). Teste de fumo real (não simulado): registo, login, isolamento multi-tenant (Empresa B nunca viu dados da Empresa A) e imutabilidade do Registo de Auditoria (`UPDATE`/`DELETE` bloqueados mesmo com o role owner) todos confirmados; dados de teste eliminados no final. Correção de segurança encontrada: `.gitignore` não cobria `.env.staging`, corrigida antes de escrever qualquer segredo em disco. **Achado resolvido pela Fundadora/CEO**: retenção de PITR do plano Neon Free (6h) abaixo do mínimo de 7 dias do ADR-007 §3.8 — aceite temporariamente só para staging, registado permanentemente no ADR-007 (v1.2, nova Questão em Aberto Q4), upgrade obrigatório antes de produção. **Passo 41 (Backend em Render, staging) formalmente concluído e aprovado pela Fundadora/CEO** — ver [Especificação Técnica do Passo 41](../04-implementation-blueprint/41-especificacao-tecnica-passo-41-render-backend.md) e [Relatório do Passo 41](../04-implementation-blueprint/42-relatorio-passo-41-render-backend.md). Confirmado, tal como já indicado na especificação: não existe CLI oficial da Render (`npx render-cli` é um pacote npm homónimo de templating, sem relação com a Render.com) — todo o provisionamento feito via API REST, mesma disciplina de automação e evidência já usada no Passo 40. Web Service `nexa-api-staging` criado (`srv-d9b0u358nd3s739uavd0`, região `frankfurt`, UE, plano Free), ligado ao repositório `adaalvess/NEXA` (branch `main`, auto-deploy por commit), URL pública `https://nexa-api-staging.onrender.com`. **Descoberta técnica real, corrigida antes do fecho — o primeiro deploy falhou no build**, diagnosticado por inspeção direta dos logs da Render (não por tentativa-erro às cegas): causa dupla — (1) sem `postinstall`, `npm install` nunca corria `prisma generate`, o cliente Prisma ficava no stub por defeito sem os enums customizados do schema (`Papel`, `EstadoProcesso`, etc.), lacuna de scaffolding real desde o Passo 1, nunca visível localmente porque o cliente já estava gerado de execuções manuais anteriores; (2) sem `tsconfig.build.json`, `nest build` compilava também `test/*.e2e-spec.ts`, amplificando o problema acima em erros fatais de build. Corrigido com dois padrões idiomáticos (`postinstall: prisma generate`; `tsconfig.build.json` padrão do scaffold NestJS, nunca criado desde o Passo 1) — nenhuma alteração de arquitetura ou âmbito. Efeito colateral verificado e corrigido: excluir `test/` do build mudou o `rootDir` inferido pelo `tsc`, e `dist/src/main.js` passou a `dist/main.js` — corrigido no `startCommand` do serviço Render e no `render.yaml`. Build local confirmado limpo antes de commitar (commit `3bc2c8b`); pedida e obtida confirmação explícita antes do push; novo deploy disparado via API (`clearCache: clear`) concluído em `live` em cerca de 70 segundos. Variáveis de ambiente configuradas e confirmadas (mesmos valores já gerados no Passo 40), deliberadamente sem `PORT` explícito (a Render injeta a sua própria variável, já lida pela aplicação desde o scaffolding). Teste de fumo real, contra o backend em Render + Neon (nunca simulado): `GET /health` → `200`; registo e login de duas Empresas de demonstração; criação de Processo pela Empresa A; isolamento multi-tenant confirmado (Empresa B nunca viu o Processo da A); dados de teste eliminados no final. `render.yaml` criado e commitado (Infrastructure as Code), refletindo fielmente a configuração real do serviço. **Passo 42 (Frontend em Vercel, staging) formalmente concluído e aprovado pela Fundadora/CEO** — ver [Especificação Técnica do Passo 42](../04-implementation-blueprint/43-especificacao-tecnica-passo-42-vercel-frontend.md) e [Relatório do Passo 42](../04-implementation-blueprint/44-relatorio-passo-42-vercel-frontend.md), com as 4 Decisões a Validar (A-D) confirmadas sem alteração. **Diferente do Render: existe CLI oficial completo da Vercel** (`vercel`, confirmado a funcionar com o token fornecido), todo o provisionamento por automação. Projeto `nexa-web-staging` criado (conta `nexa-adair`, `apps/web` como raiz), URL pública `https://nexa-web-staging.vercel.app`. **Região confirmada por evidência real, não apenas pela configuração**: cabeçalho `X-Vercel-Id` de um pedido real continha o troço `fra1`, confirmando execução em Frankfurt (distinto de onde o build correu, irrelevante para o NFR-21). **CORS corrigido (Decisão C)**: `WEB_APP_URL` no Render, provisório desde o Passo 41, atualizado para a URL real do Vercel via API da Render, com novo deploy disparado para a alteração ter efeito — nota técnica: o primeiro `PATCH` foi aceite mas não persistiu o valor, corrigido usando `PUT` no mesmo endpoint, sem impacto de arquitetura; confirmado por pedido `OPTIONS` real com o `Origin` exato do Vercel (`204`, `access-control-allow-origin` correto). Teste de fumo real, ponta a ponta (frontend + backend + BD, todos em staging real, nunca simulado): páginas públicas (`/`, `/precos`, `/login`, `/registar`) → `200`; registo, login, criação de Processo e isolamento multi-tenant confirmados via pedidos reais com o cabeçalho `Origin` do frontend Vercel (Empresa B nunca viu o Processo da Empresa A); dados de teste eliminados no final. **Os três componentes de staging (Vercel + Render + Neon) estão agora operacionais em conjunto, todos em região UE, validados de ponta a ponta. Milestone M7 em curso** — próximo: Passo 43 (Observabilidade — Sentry + monitorização de disponibilidade/uptime).

**Porquê esta é a sequência mais lógica agora:**

1. **A Fase 3 completa (3a-3d) e a compactação das Fases 4-6 no Blueprint já removeram o único risco genuinamente bloqueante (R1, resolvido em 3.8)** — a tecnologia está decidida e o schema de base de dados já está implementado e migrado (Passo 2, ver Blueprint §3a). Não há mais nenhuma decisão de arquitetura pendente que impeça continuar o código.
2. **O Blueprint já define a ordem interna do M1** (Passo 0 a Passo 7, secção 3 desse documento) — não há benefício em voltar a planear do zero o que já está sequenciado e aprovado.
3. **O trabalho de detalhe deixado propositadamente por fazer nas Fases 4/6** (Sprints, Release Strategy, Risk Register, Technical Debt Register formais, protótipos UI reais) não bloqueia o M1 (Fundação, sem UI própria além de Registo/Login e Configurações) — só passa a pesar a partir do M2 (Dashboard/Processos/CRM), quando a UI real começar a importar. Registado como R11 (3.8), a vigiar, não a resolver agora.
4. **Consistente com o método de trabalho já fixado** (CLAUDE.md, secção 5): um Passo de cada vez, cada um validado explicitamente antes do seguinte — não há razão para essa disciplina mudar agora que o desenvolvimento está em curso.

---

## 4. Decisões Tomadas

| # | Decisão | Justificação |
|---|---|---|
| D1 | Este documento é vivo e será atualizado a cada fase concluída, nunca reescrito de raiz | Mantém-se como referência permanente e rastreável da evolução do projeto, tal como pedido |
| D2 | A percentagem de progresso (55%) é calculada por peso estimado de esforço documental, não por contagem simples de fases ou documentos | Uma contagem simples ("3 de 7 fases") distorceria a realidade, já que as fases restantes (ADRs, Planeamento, Arquitetura Técnica) envolvem volume de trabalho comparável às já concluídas |
| D3 | A recomendação da próxima fase é fundamentada exclusivamente no que bloqueia o desenvolvimento (R1), não noutros riscos igualmente reais mas não bloqueantes | Evita dispersão de prioridade — nem todos os itens da lista de riscos têm o mesmo peso crítico |
| D4 | O roadmap foi expandido para cobrir todo o ciclo de vida da NEXA (Fases 8, 9 e 10 — Testes, Preparação para Produção, Lançamento e Evolução Contínua), mantendo o mesmo nível de abstração das restantes fases, sem detalhe técnico prematuro | Cumpre o pedido de que o documento represente a jornada completa da NEXA, não apenas até ao início do desenvolvimento, sem introduzir trabalho de especificação antes do momento certo |
| D5 | A percentagem de progresso passou a ser expressa como intervalo (~55% ± 5%), com âmbito explicitamente limitado às Fases 1-6, e uma nota que explica porque as Fases 7-10 não entram no cálculo | Uma percentagem exata sugeria precisão que este método de estimativa por peso relativo não tem; e incluir a Fase 10 (contínua por natureza) no denominador tornaria o número matematicamente incoerente |
| D6 | Adicionados Critérios de Conclusão (Exit Criteria) objetivos e verificáveis para cada fase, exceto a Fase 10 (que, por desenho, não os tem) | Elimina interpretação futura sobre "estamos ou não prontos para avançar" — cada fase tem agora um teste concreto de conclusão, não apenas uma lista de documentos |
| D7 | Formalizada a governação do próprio Master Roadmap (secção 6), com gatilhos explícitos de atualização | Torna o documento auto-suficiente como referência viva, sem depender de alguém se lembrar de o atualizar — os gatilhos definem exatamente quando isso deve acontecer |
| D8 | Documento atualizado retroativamente para refletir a conclusão da Fase 3b-3d (8 ADRs, Coding Standards, AI Principles), a compactação parcial das Fases 4-6 no Blueprint (D1 desse documento), e o início real da Fase 7 (Blueprint M1, Passos 0-2 concluídos) | Os gatilhos de atualização já definidos em D7 não tinham sido acionados a tempo — este documento tinha ficado desatualizado face a factos já aprovados noutros documentos (ADRs, Blueprint). Corrigido como parte da verificação de consistência pedida ao concluir o Passo 2 |
| D9 | A percentagem de progresso (~85% ± 5%) atribui às Fases 4-6 uma cobertura parcial estimada, marcada explicitamente como proposta de engenharia não vinculativa | Mantém a mesma disciplina já usada no Product & Security Decisions Register — a engenharia propõe a estimativa, a Fundadora/CEO confirma ou ajusta, em vez de a impor como facto fechado |
| D10 | A estimativa de progresso passou de julgamento holístico por fase para um cálculo explícito por 5 critérios (Documentação, Arquitetura, Infraestrutura, Implementação, Entregáveis concluídos), com evidência citada por critério (3.6a) | Pedido explícito da Fundadora/CEO por consistência metodológica e auditabilidade futura — qualquer pessoa pode agora reproduzir o cálculo a partir dos artefactos citados, em vez de aceitar uma percentagem por afirmação |

---

## 5. Questões em Aberto

*(Todas as questões em aberto de todos os documentos anteriores permanecem válidas nos seus documentos de origem — ver secção 3.8 para a lista consolidada de riscos e lacunas. Este documento não introduz questões novas.)*

---

## 6. Governação Deste Documento

O Master Roadmap é a **referência oficial e permanente do estado da NEXA**. Para que continue a cumprir essa função, é atualizado sempre que qualquer um dos seguintes eventos ocorrer:

- Uma nova fase é iniciada ou concluída.
- Um documento relevante (de qualquer fase) é aprovado.
- Um risco crítico (secção 3.8) é identificado ou resolvido.
- Uma decisão estrutural altera o roadmap, o âmbito ou a ordem das fases.
- A ordem ou o âmbito das fases já definidas neste documento é revisto.

Toda atualização é registada no Histórico de Alterações (secção 7), com a mesma disciplina de rastreabilidade usada em qualquer outro documento da NEXA — nunca reescrita silenciosamente. Este documento nunca é substituído por uma nova versão "do zero": evolui por incrementos, exatamente como todos os outros.

---

## 7. Histórico de Alterações

| Versão | Data | Alteração | Autor |
|---|---|---|---|
| 1.0 | 2026-07-02 | Criação do Master Roadmap, consolidando o estado de 25 documentos aprovados em 3 fases (Estratégica, Funcional, Engineering Principles), com inventário completo, roadmap de marcos, riscos consolidados e recomendação fundamentada para a Fase 3b (ADRs de Tecnologia) | CTO (Claude) + Fundadora/CEO |
| 1.1 | 2026-07-02 | Expandido o roadmap para cobrir todo o ciclo de vida da NEXA, com a adição das Fases 8 (Testes e QA), 9 (Preparação para Produção) e 10 (Lançamento e Evolução Contínua); percentagem de progresso revista para ~55% ± 5%, com âmbito explicitamente limitado às Fases 1-6 | CTO (Claude) + Fundadora/CEO |
| 1.2 | 2026-07-02 | Adicionados Critérios de Conclusão (Exit Criteria) por fase (3.2a) e a secção 6, formalizando a governação do próprio Master Roadmap (gatilhos de atualização) | CTO (Claude) + Fundadora/CEO |
| 1.2 | 2026-07-02 | **Aprovação oficial.** Documento passa a estado Aprovado — referência oficial de governação permanente da NEXA | Fundadora/CEO |
| 1.3 | 2026-07-06 | Atualização de consistência (pedida ao concluir o Passo 2 do M1): correção da contagem de documentos (25/41 → 38, contagem real verificada); Fase 3b-3d marcada concluída (8 ADRs, Coding Standards, AI Principles); Fases 4-6 marcadas como parcialmente cobertas pela compactação do Blueprint (D1 desse documento), com detalhe do que falta; Fase 7 marcada em curso (Blueprint M1, Passos 0-2 concluídos); riscos R1 e R6 marcados resolvidos, novo risco R11 registado; percentagem de progresso revista para ~85% ± 5% (proposta de engenharia, não vinculativa); recomendação da secção 3.10 atualizada para refletir a fase real do projeto; adicionada nota de desambiguação entre os marcos M1-M13 deste documento e os Milestones/Passos do Blueprint | CTO (Claude) + Fundadora/CEO |
| 1.4 | 2026-07-06 | Adicionada a secção 3.6a (Metodologia e Fundamentação da Estimativa de Progresso), a pedido explícito da Fundadora/CEO: definidos os 5 critérios (Documentação, Arquitetura, Infraestrutura, Implementação, Entregáveis concluídos) e a escala de pontuação (Cumprido/Parcial/Não cumprido); recalculado o estado de cada Fase 1-6 critério a critério, com evidência citada; o cálculo bottom-up (84,4%) confirmou, dentro da margem já publicada, a estimativa holística anterior (~85%) | CTO (Claude) + Fundadora/CEO |
| 1.5 | 2026-07-06 | Verificação final de consistência ao encerrar o Passo 4 (Camada 1, o mais crítico do M1): estado da Fase 7 atualizado de "Passos 0-2 concluídos, Passo 3 a decorrer" para "Passos 0-4 concluídos, Passo 5 a decorrer" em todas as menções (3.1, 3.2, 3.2a, 3.7/M8, 3.10) — factos desatualizados após dois passos de implementação sem gatilho de atualização acionado a tempo | CTO (Claude) + Fundadora/CEO |
| 1.6 | 2026-07-06 | Verificação de consistência ao encerrar o Passo 5 (RBAC granular): estado da Fase 7 atualizado de "Passos 0-4 concluídos, Passo 5 a decorrer" para "Passos 0-5 concluídos, Passo 6 a decorrer" em todas as menções (3.1, 3.2, 3.2a, 3.7/M8, 3.10) | CTO (Claude) + Fundadora/CEO |
| 1.7 | 2026-07-06 | Verificação de consistência ao encerrar o Passo 6 (Registo de Auditoria): estado da Fase 7 atualizado para "Passos 0-6 concluídos, Passo 7 a decorrer" em todas as menções (3.1, 3.2, 3.2a, 3.7/M8, 3.10); assinalado que o Definition of Done literal do M1 (Blueprint §2.2) está agora tecnicamente completo, com nota explícita de que o Passo 7 (Partilha) continua listado como conteúdo do M1 sem estar coberto pelo texto literal do DoD — questão a validar antes do fecho formal do M1, não decidida aqui | CTO (Claude) + Fundadora/CEO |
| 1.8 | 2026-07-06 | **Aprovação formal do Passo 6** pela Fundadora/CEO, e resolução da questão em aberto sobre o encerramento do M1: decisão explícita de que o Passo 7 (Partilha) é pré-requisito para o encerramento formal do Milestone, apesar do DoD literal (Blueprint §2.2) já estar tecnicamente cumprido — atualizadas todas as menções à questão em aberto (3.1 topo, 3.10) para refletir a decisão | CTO (Claude) + Fundadora/CEO |
| 1.9 | 2026-07-06 | **Passo 7 (Partilha) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M1 (Fundação) formalmente concluído.** Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para "M1 formalmente concluído (Passos 0-7); M2 por confirmar" — sem recomendação de próximo Milestone, por não haver ainda confirmação explícita da Fundadora/CEO | CTO (Claude) + Fundadora/CEO |
| 2.0 | 2026-07-06 | **Proposta do Milestone M2 (Módulos Core) aprovada pela Fundadora/CEO; Passo 8 (Departamento) concluído e formalmente aprovado.** Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir o M2 em curso | CTO (Claude) + Fundadora/CEO |
| 2.1 | 2026-07-06 | **Passo 9 (Processos e Tarefas) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro módulo de negócio fora da Fundação; `AuthorizationService` estendido e `PartilhaService` refatorado para eliminar duplicação de visibilidade RBAC (Decisão B do M2). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) | CTO (Claude) + Fundadora/CEO |
| 2.2 | 2026-07-06 | **Passo 10 (CRM) concluído e formalmente aprovado pela Fundadora/CEO** — segundo módulo de negócio; zero alterações ao `AuthorizationService`, confirmando na prática a centralização da Decisão B do M2. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) | CTO (Claude) + Fundadora/CEO |
| 2.3 | 2026-07-06 | **Passo 11 (Notification Dispatcher) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro consumidor de eventos fire-and-forget do projeto, reutilizando o `EVENTO_AUDITORIA` já existente sem alterar nenhum ponto de emissão. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) | CTO (Claude) + Fundadora/CEO |
| 2.4 | 2026-07-07 | **Passo 12 (Dashboard) concluído e formalmente aprovado pela Fundadora/CEO — backend do Milestone M2 concluído** (Passos 8-12). Terceira confirmação prática da Decisão B do M2 (zero alterações ao `AuthorizationService`). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) — próximos passos (13-14) são de frontend | CTO (Claude) + Fundadora/CEO |
| 2.5 | 2026-07-07 | **Passo 13 (Design System, frontend) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo de `apps/web` desde o scaffolding do Passo 1: 11 componentes base sobre primitivas Radix UI, tokens de marca completos (tipografia, glow), vitrine interna em `/design-system`, validado por inspeção visual real no browser (não só revisão de código). Estado da Fase 7 e da Fase 5 (Design System) atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) — próximo e último passo do M2: Passo 14 (Ecrãs) | CTO (Claude) + Fundadora/CEO |
| 2.6 | 2026-07-07 | **Passo 14 (Ecrãs, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M2 (Módulos Core) formalmente concluído.** Implementado em 3 sub-entregas sequenciais validadas individualmente (Login+Dashboard, Processos, CRM); `GET /utilizadores` e `POST /auth/logout` adicionados ao backend; `Textarea` e correção de scroll responsivo adicionados ao Design System. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M1+M2 concluídos e M3 (Assistente de IA) por propor — contexto orientador já registado via aditamento ao Product Vision (v1.2, §3.5a) | CTO (Claude) + Fundadora/CEO |
| 2.7 | 2026-07-07 | **Proposta do Milestone M3 (Assistente de IA) aprovada pela Fundadora/CEO; Passo 15 (AI Gateway) concluído e formalmente aprovado** — primeiro passo do M3, implementa integralmente o ADR-005; novo módulo `apps/api/src/modules/ia/`, sem endpoint de produto ainda; descoberta real (`UsoIAMensal`, novo modelo aditivo). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M3 em curso | CTO (Claude) + Fundadora/CEO |
| 2.8 | 2026-07-07 | **Passo 16 (módulo `ia` — pergunta livre) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro endpoint de produto do M3, `POST /ia/perguntar` (UC-05); `AuthorizationService.construirFiltroWhere` extraído do `DashboardService` (4ª confirmação da Decisão B do M2); RN-07 garantida estruturalmente; extensão aditiva do schema `SugestaoIA`; retenção de conteúdo configurável (PSD-003). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 16 concluído e Passo 17 como próximo passo | CTO (Claude) + Fundadora/CEO |
| 2.9 | 2026-07-07 | **Passo 17 (sugestões de ação) concluído e formalmente aprovado pela Fundadora/CEO — NFR-17 fecha na íntegra.** `POST /ia/sugestoes` (geração determinística), `.../confirmar`, `.../rejeitar` (UC-06, RN-08); único tipo de ação suportado — reatribuição de Processo em atraso; `ProcessosModule` exporta `ProcessosService`, consumido por `IaModule` (primeiro módulo de negócio a consumir outro); RN-08 garantida em duas camadas (tipos + eventos); extensão aditiva do schema `SugestaoIA` (`acaoPayload`). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 17 concluído e Passo 18 como próximo passo | CTO (Claude) + Fundadora/CEO |
| 3.0 | 2026-07-07 | **Passo 18 (ecrã do Assistente de IA, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M3 (Assistente de IA) formalmente concluído.** Último passo do M3: ecrã `/ia` (Perguntar + Sugestões Pendentes), `GET /ia/sugestoes` (extensão aditiva ao backend), RN-08 refletida na interface (`Modal` de segunda confirmação, nunca ação em lote), item "Assistente de IA" na navegação. Validado por inspeção visual real no browser nos 4 papéis, incluindo execução real de uma reatribuição confirmada. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M1/M2/M3 formalmente concluídos e o próximo Milestone (M4) por confirmar com a Fundadora/CEO | CTO (Claude) + Fundadora/CEO |
| 3.1 | 2026-07-07 | **Proposta do Milestone M4 (Comercial e Pagamentos) aprovada pela Fundadora/CEO; Passo 19 (`SubscricaoPlano` real, `GET /planos`) concluído e formalmente aprovado.** Primeiro módulo `comercial` construído; `SubscricaoPlano` ativado com trial automático (plano Professional), criado reativamente ao evento já existente (`fundacao` nunca conhece `comercial`), idempotente por `upsert` (exigência explícita da Fundadora/CEO); `plano`/`estado` promovidos a `enum`; `Empresa.estadoSubscricao` removido; limites `Int?` (`null`=sem limite); `QuotaService` corrigido. **Risco R3 formalmente resolvido** — valores numéricos de limites por plano aprovados e aplicados (Starter/Professional/Enterprise). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M4 em curso | CTO (Claude) + Fundadora/CEO |
| 3.2 | 2026-07-08 | **Passo 20 (enforcement uniforme de RN-11) concluído e formalmente aprovado pela Fundadora/CEO.** `SubscricaoGuard`/`SubscricaoExceptionFilter` globais (`APP_GUARD`/`APP_FILTER`) garantem resposta uniforme (`402`, `code: SUBSCRICAO_LIMITADA`) em toda a aplicação, exigência explícita da Fundadora/CEO; decorator leve `@BloqueadoPorSubscricao()` em 5 endpoints de criação, sem nenhum módulo de negócio importar `ComercialModule`; estado efetivo derivado dinamicamente, sem scheduler; RN-10 preservada; teste estrutural dedicado (T6) prova uniformidade byte-a-byte. Implementação estritamente conforme a especificação aprovada, sem desvios de arquitetura ou âmbito. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 20 concluído | CTO (Claude) + Fundadora/CEO |
| 3.3 | 2026-07-08 | **Passo 21 (Stripe Checkout) concluído e formalmente aprovado pela Fundadora/CEO.** `POST /subscricao/checkout` (UC-07) — primeiro contacto real com a Stripe; só Starter/Professional self-service; Price IDs via variáveis de ambiente; Cliente Stripe reaproveitado; `409` se já `ativa`; `STRIPE_CLIENT`/`FakeStripeClient` sem credenciais reais; nunca sujeito ao `SubscricaoGuard`. `SubscricaoService.criarCheckout` único ponto responsável. Descoberta técnica real corrigida (SDK Stripe exige `apiKey` não-vazia), sem alteração de âmbito ou arquitetura. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 21 concluído | CTO (Claude) + Fundadora/CEO |
| 3.4 | 2026-07-08 | **Passo 22 (Webhooks Stripe) concluído e formalmente aprovado pela Fundadora/CEO — fecha o ciclo completo de UC-07.** `POST /webhooks/stripe` (ADR-008 §3.4); só `checkout.session.completed` tratado (decisão consciente de âmbito); `rawBody: true`; `tenantContext.run()` reutilizado do Passo 19; idempotência real via `WebhookStripeProcessado`; eventos não tratados sempre `200`; testes com o SDK real da Stripe (prova genuína de rejeição criptográfica e de deduplicação). `SubscricaoService.processarCheckoutConcluido` extensão do mesmo serviço. Sem descobertas técnicas emergentes. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 22 concluído e Passo 23 como próximo (último do M4) | CTO (Claude) + Fundadora/CEO |
| 3.5 | 2026-07-08 | **Passo 23 (Ecrã de Subscrição, frontend) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M4 (Comercial e Pagamentos) formalmente concluído.** Último passo do M4: `GET /subscricao` (extensão aditiva, reaproveita `comercial.ver_planos`); `usoIAPercentagem`/`avisoLimiteIAProximo` calculados no backend (exigência explícita da Fundadora/CEO: nenhum cálculo paralelo no cliente); `IaModule` exporta `QuotaService`, consumido por `ComercialModule` (primeira vez que `comercial` consome outro módulo de negócio); ecrã `/subscricao` (plano atual, aviso a 90% de uso de IA, limites preparados, upgrade exclusivamente via `POST /subscricao/checkout` já existente); item "Plano" na navegação; `BadgeEstado` estendido. Validado por inspeção visual real no browser. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M1/M2/M3/M4 formalmente concluídos e o próximo Milestone (M5) por confirmar com a Fundadora/CEO | CTO (Claude) + Fundadora/CEO |
| 3.6 | 2026-07-08 | **Proposta do Milestone M5 (Camada Comercial e Produto) aprovada pela Fundadora/CEO; Passo 24 (`GET /planos/publico` + Página de Preços) concluído e formalmente aprovado** — primeiro passo do M5. Âmbito em 3 blocos (EP-07 Landing/Pricing/Registo, Configurações, UC-02 Convite por email); `GET /planos/publico` reaproveita `listarPlanos()` literalmente, primeira rota pública desde o webhook Stripe; página `/precos` como Server Component. Descoberta técnica real corrigida (Next.js pré-renderaria `/precos` estaticamente no build — corrigido com `dynamic = 'force-dynamic'`). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M5 aprovado e em curso, Passo 24 concluído | CTO (Claude) + Fundadora/CEO |
| 3.7 | 2026-07-08 | **Passo 25 (Landing Page pública, `/`) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do M5. `apps/web/src/app/page.tsx` editado, toda a copy citação direta de documentos já aprovados (Product Vision, Brand Book, PRD), sem testemunhos/Persona/concorrentes; sem `dynamic` explícito (já dinâmica por ler `cookies()`). Descoberta operacional real sem impacto no código (cache do servidor de desenvolvimento corrompido por `build`+`dev` em paralelo, mesma classe do Passo 13). Bloco A do M5 quase fechado — falta só o Passo 26 (Registo público). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 25 concluído | CTO (Claude) + Fundadora/CEO |
| 3.8 | 2026-07-08 | **Passo 26 (Ecrã de Registo público, `/registar`) concluído e formalmente aprovado pela Fundadora/CEO — Bloco A do M5 formalmente concluído.** `apps/web/src/app/registar/page.tsx` (novo); `POST /auth/registar` encadeado com `POST /auth/login`, sem intervenção manual; CTAs de `/`/`/precos` atualizados. **Sem consentimento RGPD nesta fase — bloqueador formal explícito antes de qualquer registo real em produção**, confirmado pela Fundadora/CEO. Descoberta técnica real corrigida (`lib/api.ts` não extraía `message` do JSON de erro — corrigido centralmente). Percurso "Landing → Registo → Trial" operacional de ponta a ponta. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Bloco A do M5 concluído e Bloco B (Passo 27) como próximo | CTO (Claude) + Fundadora/CEO |
| 3.9 | 2026-07-08 | **Passo 27 (`PATCH /utilizadores/me`) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo do Bloco B (Configurações). Self-edit de nome/palavra-passe, primeira mutação sem `PermissaoGuard` (alvo sempre `ctx.utilizadorId`); alterar a palavra-passe invalida todas as outras sessões ativas numa transação real (pedido explícito da Fundadora/CEO). Descoberta técnica real resolvida antes de implementar (`TenantPrismaService` incompatível com transação multi-operação — resolvido com `PrismaService` bruto, mesmo padrão de `AuthService.registar`). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 27 concluído e Passo 28 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.0 | 2026-07-08 | **Passo 28 (Ecrã "Configurações") concluído e formalmente aprovado pela Fundadora/CEO — Bloco B do M5 formalmente concluído.** Página única `/configuracoes` (Perfil, Utilizadores/Permissões, Departamentos), 3 componentes independentes (pedido explícito da CEO); primeira `TabelaDados` com edição em linha; item de navegação visível a todos os papéis. Descoberta real corrigida (`SeccaoPerfil` não invalidava a query `utilizadores` — corrigido, componentes continuam independentes). Sem alteração de backend. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Bloco B concluído e Bloco C (Passo 29) como próximo | CTO (Claude) + Fundadora/CEO |
| 4.1 | 2026-07-08 | **Passo 29 (Interface de email + `ConviteUtilizador`) concluído e formalmente aprovado pela Fundadora/CEO** — primeiro passo do Bloco C (UC-02), backend-fundação puro, sem endpoint de produto neste passo. `EmailAdapterInterface`/`EMAIL_ADAPTER` (interface completa, mesmo desenho do AI Gateway, escolhido em vez do padrão simples da Stripe); `ResendAdapter` real + `FakeEmailAdapter` de teste. Novo modelo `ConviteUtilizador` (tenant-scoped, RLS), `EstadoConvite` sem valor `expirado` (derivado em leitura), `token` criptográfico de 32 bytes, expiração de 7 dias. Confirmado que o bloqueador RGPD do Passo 26 não se estende ao convite por email (bases legais distintas). Duas descobertas técnicas reais, ambas precedentes já documentados noutros passos (`EPERM` do Prisma no Windows; migrações não aplicadas a `nexa_test`, mesma descoberta "D9" do Passo 7). 184/184 testes (180 herdados + 4 novos). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 29 concluído e Passo 30 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.2 | 2026-07-08 | **Passo 30 (`POST /convites`, `GET /convites/:token`, `POST /convites/:token/aceitar`) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do Bloco C, 3 Decisões a Validar aprovadas sem alteração (A: `GET /convites/:token` público para pré-visualização antes da aceitação; B: `limiteUtilizadores`/RN-10/RN-11 deliberadamente não implementado, mecanismo distinto do `SubscricaoGuard`, Questão em Aberto explícita; C: unicidade de email global na aceitação, mesmo padrão do registo/UC-01 E1). `PRIVILEGIO` (Passo 5) extraída para ficheiro partilhado, reutilizada sem duplicação. Regras de autoridade CV-01 a CV-06. Ordem "enviar antes de persistir" no envio do convite — observação da Fundadora/CEO sobre o cenário de email entregue com persistência falhada respondida formalmente na aprovação (Especificação Técnica do Passo 30, §6): risco real e inerente à ausência de atomicidade entre dois sistemas externos, mitigado com registo estruturado e erro explícito ao Administrador, nunca escondido. Sem auto-login no endpoint de aceitação — o Passo 31 encadeia login no frontend, mesmo padrão do Passo 26. 202/202 testes (184 herdados + 18 novos). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 30 concluído e Passo 31 como próximo, último do Bloco C | CTO (Claude) + Fundadora/CEO |
| 4.3 | 2026-07-08 | **Passo 31 (Ecrã de Convite — envio + aceitação) concluído e formalmente aprovado pela Fundadora/CEO — último passo do Bloco C, Milestone M5 (Camada Comercial e Produto) formalmente concluído.** 2 Decisões a Validar aprovadas sem alteração (A: formulário de envio dentro de Configurações → Utilizadores e Permissões, local já fixado pela Information Architecture; B: sem listagem de convites pendentes, registado como melhoria futura). `PAPEIS_ATRIBUIVEIS` extraída para o frontend; seletor de papel nunca filtra por privilégio; seletor de Departamento oculto para Gestor (decorre de CV-03). Novo `/convites/:token` público, contexto sempre visível antes da palavra-passe, aceitação encadeia login no frontend (mesmo padrão do Passo 26). Validado por inspeção visual real no browser nos papéis relevantes, fluxo completo de aceitação verificado na BD, responsivo, zero erros de consola. **Milestone M5 formalmente concluído** — Passos 24-31 implementados, validados e aprovados, os 3 blocos (A: EP-07 Landing/Pricing/Registo; B: Configurações; C: Convite por email) operacionais de ponta a ponta. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M5 formalmente concluído e Proposta do Milestone M6 como próxima ação | CTO (Claude) + Fundadora/CEO |
| 4.4 | 2026-07-09 | **Proposta do Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) aprovada pela Fundadora/CEO; Passo 32 (Consolidação NFR-17) concluído e formalmente aprovado** — primeiro passo do M6. 4 achados prévios validados (cobertura NFR-17 já distribuída em M1-M5; inconsistência no Use Cases v1.0, 8 UCs reais não 9; UC-08 exige resolver RN-10 primeiro — decisão de implementar dentro do M6, Passo 33; FR-08/09/27 registados como Questões em Aberto, fora do âmbito dos Use Cases). Sequência aprovada: Passos 32-38. Passo 32: suite completa 202/202 em 2 execuções consecutivas; mapeamento fluxo→testes→lacunas com inspeção manual real das asserções, coluna "Lacunas identificadas" por pedido explícito da Fundadora/CEO; duas lacunas honestas registadas (isolamento — Camadas 1/2 nunca testadas isoladamente; RN-10 sem testes por ainda não existir). Sem código de produção alterado. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir M6 aprovado e em curso, Passo 32 concluído, Passo 33 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.5 | 2026-07-09 | **Passo 33 (Enforcement de `limiteUtilizadores`, RN-10) concluído e formalmente aprovado pela Fundadora/CEO** — segundo passo do M6, 3 Decisões a Validar aprovadas sem alteração (A: RN-10 implementado inteiramente dentro de `fundacao/convite/`, sem depender de `comercial` — tensão real encontrada com a Proposta do M6 e trazida de volta para validação explícita, preservando a regra estrutural "`FundacaoModule` nunca importa `ComercialModule`" sem exceção; B: contagem em `POST /convites` inclui Convites pendentes, em `POST /convites/:token/aceitar` só Utilizadores ativos; C: verificação nos dois pontos, defesa em profundidade). `LimiteUtilizadoresAtingidoError` própria (`402`/`LIMITE_UTILIZADORES_ATINGIDO`). Refinamento encontrado nos testes: aceitar um convite não liberta espaço, só a expiração natural liberta — corrigido na implementação, sem alterar as Decisões A-C. 8 testes novos (T19-T26), suite completa em 210/210 (202 herdados + 8 novos), estável em 2 execuções consecutivas. UC-02 (Exceção E1) e UC-08 agora integralmente viabilizados para validação manual. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 33 concluído e Passo 34 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.6 | 2026-07-11 | **Passo 34 (Validação Manual UC-01 + UC-02) concluído e formalmente aprovado pela Fundadora/CEO** — terceiro passo do M6, sem Especificação Técnica prévia (âmbito já fixado pela Proposta do M6). Validação real no browser via o próprio ecrã de registo público, continuidade direta para UC-02. UC-01 e UC-02: todos os itens (fluxo principal, alternativas, exceções, regras de negócio) confirmados no sistema real, incluindo a Exceção E1 do UC-02 (limite de Utilizadores) ao vivo via API e UI. 4 achados registados, nenhum bloqueante (campo "setor" nunca recolhido no registo; envio real de email não observável, limitação já conhecida; Fluxo Alternativo 2a do UC-02 sem interface, consistente com exclusão já decidida no M5; Exceção E2 do UC-02 sem implementação, Questão em Aberto já registada). Zero bugs, zero erros de consola. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 34 concluído e Passo 35 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.7 | 2026-07-11 | **Passo 35 (Validação Manual UC-03 + UC-04) concluído e formalmente aprovado pela Fundadora/CEO** — quarto passo do M6, sem Especificação Técnica prévia. UC-04 executado primeiro (criar Cliente) para viabilizar a associação em UC-03. UC-03 e UC-04: todos os itens confirmados no sistema real, incluindo a Exceção E1 do UC-03 testada diretamente contra a API (contornando o dropdown já filtrado da UI) e RN-05 confirmada nos dois sentidos. 2 achados registados: (A, menor) Fluxo Alternativo 1a do UC-04 sem caminho de um único passo; (B, **substantivo**) FR-18 (referência bidirecional Processo↔Cliente) parcialmente implementado — Processo→Cliente é texto estático, Cliente→Processo não mostra nada — recomendado como item a corrigir num passo dedicado, fora do âmbito do M6. Zero bugs, zero erros de consola. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 35 concluído e Passo 36 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.8 | 2026-07-11 | **Passo 36 (Validação Manual UC-05 + UC-06) concluído e formalmente aprovado pela Fundadora/CEO** — quinto passo do M6, sem Especificação Técnica prévia. Fixtures adicionais criados para UC-06 (Departamento, 2 Colaboradores via convite direto na BD, 4 Processos em atraso todos atribuídos ao Colaborador de maior carga) para exercitar a heurística determinística (Passo 17) nos 4 desfechos possíveis de uma sugestão sem depender de credencial de IA real. UC-05: Fluxo Principal 1/3/5 e Exceção E1 (fornecedor indisponível, `502`, mensagem clara) confirmados ao vivo; Fluxo Principal 2/Alternativo 2a (RN-07) já cobertos por teste automatizado (T2, Passo 16); FR-28 confirmado na BD (entidade `InteracaoIA`, eventos correlacionados por `requestId`). UC-06: os 4 desfechos confirmados — Confirmar (reatribuição real na BD, auditoria dupla `SugestaoIA/confirmar` + `Processo/atualizar`), Rejeitar, Deixar pendente, Staleness (`409`, mensagem clara); RN-08 confirmada estruturalmente (sem mecanismo de confirmação em lote em toda a base de código). 1 achado registado, já conhecido (caminho de sucesso da pergunta livre não observável sem credencial real). Zero bugs, zero erros de consola. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 36 concluído e Passo 37 como próximo | CTO (Claude) + Fundadora/CEO |
| 4.9 | 2026-07-11 | **Passo 37 (Validação Manual UC-07 + UC-08) concluído e formalmente aprovado pela Fundadora/CEO — sexto e último passo de validação do M6, os 8 Use Cases do MVP têm agora todos registo de validação manual.** Sem credenciais reais da Stripe neste ambiente — efeito do webhook simulado por escrita direta em `SubscricaoPlano` (o próprio webhook já coberto por teste automatizado real com o SDK da Stripe desde o Passo 22). UC-07: Fluxo Principal 2-4 confirmados ao vivo; Alternativo 3a confirmado (trial expirado entra no mesmo `estadoEfetivo: limitada` de UC-08); RN-09 confirmada (dados intactos após "expiração" sem conversão). UC-08: RN-10 isolada de forma decisiva de RN-11 — com a Empresa `ativa` e `limiteUtilizadores` no limite, `POST /convites` bloqueado (`402`) enquanto leitura e outra ação de criação continuaram a funcionar no mesmo momento, provando que só a ação específica é bloqueada. 3 achados registados: (A, substantivo) UC-07 Fluxo Principal 1 (notificação proativa de fim de trial) nunca implementado, sem scheduler em todo o backend; (B, refinamento de Questão em Aberto já conhecida) Exceção E1 do UC-07 sem tratamento explícito no webhook; (C, menor) aviso a 90% do UC-08 Alternativo 1a só existe para `limiteUsoIA`. Zero bugs, zero erros de consola. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.7/M8, 3.10) para refletir Passo 37 concluído e Passo 38 (relatório final de encerramento do M6) como próximo | CTO (Claude) + Fundadora/CEO |
| 5.0 | 2026-07-11 | **Passo 38 (Relatório Final de Encerramento do M6) concluído e formalmente aprovado pela Fundadora/CEO — Milestone M6 (Testes dos 4 Fluxos Críticos + Validação Manual dos Use Cases) formalmente concluído.** Relatório de consolidação (docs/04-implementation-blueprint/37-relatorio-final-m6.md), sem factos novos além dos já validados nos Passos 32-37. Os dois objetivos aprovados na Proposta do M6 cumpridos com evidência real: NFR-17 com cobertura de teste automatizado genuína (Passo 32); os 8 Use Cases do MVP validados manualmente em ambiente real (Passos 34-37). Zero bugs em todo o M6. 11 achados consolidados e classificados: 2 substantivos (FR-18 referência bidirecional incompleta; ausência de notificação proativa de fim de trial), 5 já conhecidos/reconfirmados, 3 menores, 1 lacuna de atribuição de teste. Questões em Aberto herdadas reconfirmadas sem alteração (bloqueador RGPD do Passo 26; PSD-001/PSD-002; inconsistência "9 vs 8 Use Cases" do Use Cases v1.0, também corrigida na tabela de Milestones do Blueprint §2.2, sem alterar o Use Cases v1.0 em si). **Todos os passos previstos para o M6 (32-38) implementados, validados e aprovados** — próximo Milestone por confirmar com a Fundadora/CEO. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir M6 formalmente concluído e a ausência de um próximo Milestone ainda aprovado | CTO (Claude) + Fundadora/CEO |
| 5.1 | 2026-07-11 | **Proposta do Milestone M7 (Deploy em Staging, ADR-007) aprovada pela Fundadora/CEO; Passo 39 (Repositório GitHub + Rate Limiting) parcialmente concluído.** Âmbito exclusivo a staging, sem produto/produção. Três descobertas prévias: ausência total de infraestrutura; divergência real entre o rate limiting implementado desde o Passo 3 e o decidido no ADR-007 §3.6 (chave IP+conta, só falhas, janela 15min, bloqueio progressivo — nunca implementado, só `@Throttle` genérico por IP), corrigida dentro do M7; sobreposição documental Blueprint/Master Roadmap, só documental. Nenhum achado substantivo do M6 é pré-requisito do M7. Parte A (GitHub) preparada mas não executada — conta/organização de destino deliberadamente por confirmar antes do push. Parte B (rate limiting) concluída — novo modelo `TentativaLoginFalhada` (sem `empresaId`); `AuthService.login()` reescrito, bloqueio progressivo por IP+conta em 3 camadas, cada uma com a sua própria duração como janela de contagem; `trust proxy` configurado em `main.ts` (correção real de produção). 9 testes novos, suite completa 219/219; validado no browser, zero erros de consola. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir M7 aprovado e em curso, Passo 39 parcialmente concluído | CTO (Claude) + Fundadora/CEO |
| 5.2 | 2026-07-11 | **Passo 39 (Repositório GitHub + Rate Limiting) formalmente concluído — Parte A executada.** A Fundadora/CEO forneceu o destino (`https://github.com/adaalvess/NEXA.git`, privado); `git remote add` + `git push -u origin main` executados com sucesso — primeiro push da história do repositório. Descoberta operacional real: a primeira tentativa bloqueou indefinidamente à espera de autenticação interativa do Git Credential Manager (confirmado por `GIT_TRACE=1`, `401` do GitHub antes da chamada a `git credential-manager get`) — comunicado à Fundadora/CEO em vez de repetir às cegas; após confirmação de autenticação concluída do lado dela, o push repetido teve sucesso imediato. Repositório remoto ativo — pré-requisito estrutural para o Passo 44 (CI/CD via GitHub Actions). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 39 formalmente concluído | CTO (Claude) + Fundadora/CEO |
| 5.3 | 2026-07-12 | **Passo 40 (Base de Dados Neon em Staging) formalmente concluído e aprovado pela Fundadora/CEO.** Desvio de arquitetura detetado e corrigido antes de qualquer configuração: o projeto Neon inicial estava em `aws-us-east-1` (EUA), violando ADR-007 §3.2/NFR-21 — eliminado e recriado em `aws-eu-central-1` (Frankfurt), só depois de confirmação explícita. Schema replicado na íntegra (17/17 migrações — schema, RLS, trigger de imutabilidade); os 4 roles de BD recriados com os privilégios já validados localmente, confirmando empiricamente que o Neon permite `CREATE ROLE ... BYPASSRLS`. Teste de fumo real (não simulado) confirmou registo, login, isolamento multi-tenant e imutabilidade do Registo de Auditoria. Correção de segurança encontrada: `.gitignore` não cobria `.env.staging`, corrigida antes de escrever qualquer segredo em disco. Achado resolvido pela Fundadora/CEO: retenção de PITR do plano Neon Free (6h) abaixo do mínimo de 7 dias do ADR-007 §3.8 — aceite temporariamente só para staging, registado permanentemente no ADR-007 (v1.2, nova Questão em Aberto Q4), upgrade obrigatório antes de produção. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 40 formalmente concluído e Passo 41 como próximo | CTO (Claude) + Fundadora/CEO |
| 5.4 | 2026-07-14 | **Passo 41 (Backend em Render, Staging) formalmente concluído e aprovado pela Fundadora/CEO.** Confirmado que não existe CLI oficial da Render — provisionamento via API REST, mesma disciplina do Passo 40. Web Service `nexa-api-staging` criado (região `frankfurt`, UE), ligado à Neon de staging. Descoberta técnica real, corrigida antes do fecho: o primeiro deploy falhou no build por lacuna de scaffolding dupla — sem `postinstall`, `npm install` nunca corria `prisma generate` (cliente Prisma ficava no stub por defeito, sem os enums customizados do schema); sem `tsconfig.build.json`, `nest build` compilava também `test/*.e2e-spec.ts`, amplificando o problema em erros fatais. Corrigido com dois padrões idiomáticos (`postinstall: prisma generate`; `tsconfig.build.json` padrão do scaffold NestJS), sem alteração de arquitetura; efeito colateral (`dist/src/main.js` → `dist/main.js`) corrigido no `startCommand` e no `render.yaml`. Teste de fumo real contra o backend em Render (não simulado) confirmou `GET /health`, registo, login e isolamento multi-tenant. `render.yaml` commitado (Infrastructure as Code). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 41 formalmente concluído e Passo 42 como próximo — corrigida também uma lacuna pré-existente na secção 3.1 (topo), onde a conclusão do M6 e o avanço do M7 nunca tinham sido de facto refletidas apesar dos changelogs anteriores o afirmarem | CTO (Claude) + Fundadora/CEO |
| 5.5 | 2026-07-14 | **Passo 42 (Frontend em Vercel, Staging) formalmente concluído e aprovado pela Fundadora/CEO.** Diferente do Render: existe CLI oficial completo da Vercel, provisionamento inteiramente por automação. Projeto `nexa-web-staging` criado, região `fra1` (Frankfurt, UE) — confirmada por evidência real (cabeçalho `X-Vercel-Id`), não apenas pela configuração. CORS corrigido: `WEB_APP_URL` provisório no Render (Passo 41, Decisão C) atualizado para a URL real do Vercel, com novo deploy disparado para ter efeito — nota técnica: `PATCH` ao endpoint de variável individual não persistiu o valor, corrigido usando `PUT`, sem impacto de arquitetura. Teste de fumo real, ponta a ponta (frontend + backend + BD, todos em staging real): páginas públicas, registo, login, criação de Processo e isolamento multi-tenant confirmados via pedidos reais com o `Origin` do frontend Vercel. Os três componentes de staging (Vercel + Render + Neon) operacionais em conjunto, todos em região UE. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 42 formalmente concluído e Passo 43 (Observabilidade — Sentry + uptime) como próximo | CTO (Claude) + Fundadora/CEO |
| 5.6 | 2026-07-14 | **Passo 43 (Observabilidade — Sentry + Uptime, Staging) formalmente concluído e aprovado pela Fundadora/CEO.** Sentry: 2 projetos criados em região UE (`de.sentry.io`); backend com `@sentry/nestjs` (descoberta técnica: mais adequado que `@sentry/node` cru, `SentryGlobalFilter` como primeiro `APP_FILTER`, confirmado sem regressão em 219/219 testes e2e); frontend com `@sentry/nextjs` manual (`next.config.js`, `sentry.*.config.ts`, `instrumentation.ts`, `global-error.tsx`). Validação real via rotas de diagnóstico temporárias — deployadas, disparadas, confirmadas nos dois dashboards (`firstEvent` com timestamp exato, tag `environment: staging`) e revertidas de imediato, sem impacto em nenhum comportamento de produto; regressão completa confirmada depois (219/219 testes, `/health`, CORS, registo/login/isolamento multi-tenant reais). UptimeRobot bloqueado por `access_denied` na conta da Fundadora/CEO (leitura funciona, escrita não — indício de verificação de conta pendente do fornecedor) — **decisão explícita dela de encerrar o Passo 43 sem esta dependência externa**, registada permanentemente no ADR-007 (v1.3, nova Questão em Aberto Q5), obrigatória antes de qualquer lançamento com empresas piloto/produção, mesmo padrão já usado para a exceção de retenção de backups da Neon (Q4, Passo 40). Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 43 formalmente concluído e Passo 44 (CI/CD — GitHub Actions) como próximo | CTO (Claude) + Fundadora/CEO |
| 5.7 | 2026-07-20 | **Passo 44 (CI/CD, GitHub Actions) formalmente concluído e aprovado pela Fundadora/CEO.** Pipeline real com portão (Opção 2, não só visibilidade): `.github/workflows/ci-cd.yml` com 3 jobs — `test-backend` (Postgres efémero do próprio runner, 18 migrações + 3 roles recriados, 219 testes e2e), `build-frontend` (build + lint), `deploy` (`needs` dos dois anteriores, dispara Render com confirmação real de `live` por polling, depois Vercel). `autoDeploy` do Render desativado permanentemente. GitHub Secrets `RENDER_API_KEY`/`VERCEL_TOKEN` configurados (mesmos valores já usados manualmente, encriptados com a chave pública do repositório via `libsodium` antes do envio à API). Descoberta técnica real corrigida durante a validação: `DATABASE_ADMIN_URL` em falta no `.env.test` efémero do workflow, causando falha genuína em `auth.e2e-spec.ts` (`adminClient` nunca atribuído) — diagnosticado com a Fundadora/CEO a ler o erro exato na interface do GitHub, sem pedir um token com permissões além do estritamente necessário. Validação real das duas direções do portão, com evidência independente do próprio CI: commit correto (`690f451`) → os dois portões passam → deploy real confirmado em ambos os serviços (Render live no commit certo, `/health`/`/` a `200`); commit com teste deliberadamente falhado (`4211217`) → `test-backend` falha → job `deploy` fica `skipped` (nunca chega a correr) → deploy live confirmado inalterado. Ficheiro de teste deliberado revertido de imediato (`b8873aa`), `main` de volta a estado limpo. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir Passo 44 formalmente concluído e Passo 45 (Validação Técnica Final do M7) como próximo | CTO (Claude) + Fundadora/CEO |
| 5.8 | 2026-07-20 | **Passo 45 (Validação Técnica Final do M7) formalmente concluído e aprovado pela Fundadora/CEO — Milestone M7 (Deploy em Staging, ADR-007) formalmente encerrado.** Primeiro teste real de recuperação de backup de todo o projeto — branch temporário da Neon a partir de um ponto no tempo anterior a uma eliminação real de dados, nunca o `main` restaurado diretamente (risco zero para o staging real); Empresa de teste recuperada com sucesso, timestamp confirmado pelo próprio servidor Neon (`Empresa.createdAt`), nunca o relógio local. Descoberta técnica real corrigida durante o próprio teste: flag `--timestamp` do `neonctl` não existe (`branches create --help` confirmou), ignorada silenciosamente nas duas primeiras tentativas — o branch clonava o estado atual do `main`, já sem os dados, produzindo um falso negativo; corrigido usando a flag correta, `--parent`, que aceita nome/id/timestamp/LSN. Checklist de encerramento do M7 revisto com evidência concreta obtida na própria sessão (não suposição nem revisão de registos antigos) — os 7 itens (Passos 39-45) confirmados: repositório GitHub + rate limiting, Neon (schema/roles/RLS/trigger + agora recuperação real), Render (`/health` reconfirmado), Vercel (`/` reconfirmado), Sentry ativo, pipeline CI/CD (`autoDeploy` reconfirmado desativado, último workflow `success`), recuperação de backup. **As duas pendências herdadas (Q4 — upgrade do plano Neon antes de produção; Q5 — UptimeRobot bloqueado por dependência externa na conta da Fundadora/CEO) mantidas explicitamente como pré-requisitos de lançamento em produção/piloto, nunca bloqueadores do encerramento do M7** — decisão formal da Fundadora/CEO, registada permanentemente no ADR-007 (v1.3). **Milestone M7 (Deploy em Staging) formalmente concluído** — todos os 7 passos previstos (39-45) implementados, validados e aprovados; a NEXA tem agora um ambiente de staging completo e operacional (Vercel + Render + Neon, todos em região UE), com observabilidade de erros ativa, pipeline de deploy protegido por um portão real de qualidade, e capacidade de recuperação de dados comprovada empiricamente. Estado da Fase 7 atualizado em todas as menções (3.1 topo, 3.2, 3.2a, 3.7/M8, 3.10) para refletir o M7 formalmente encerrado e a ausência de um próximo Milestone ainda aprovado | CTO (Claude) + Fundadora/CEO |
